═══════════════════════════════════════════════════════════════════════════════
`sp playwright` — Spec & CLI Design (ephemeral Playwright FastAPI)
═══════════════════════════════════════════════════════════════════════════════

| Field    | Value                                                            |
|----------|------------------------------------------------------------------|
| Version  | v0.2.11                                                          |
| Date     | 2026-05-14                                                       |
| Role     | Architect → Dev                                                  |
| Status   | DESIGN — no code yet. Implementation is the Slice-3 companion.    |
| Builds on| team/roles/architect/reviews/05/14/v0.2.6__playwright-deployment-simplification.md |
|          | team/comms/briefs/v0.2.11__playwright-lambda-retirement-followups.md |
| Image    | `diniscruz/sg-playwright:<version>` (Docker Hub — built by `build-playwright-image.yml`) |

═══════════════════════════════════════════════════════════════════════════════
1. Goal
═══════════════════════════════════════════════════════════════════════════════

Make `sp playwright` a first-class spec CLI so a caller can spin up an
ephemeral, isolated instance of the Playwright FastAPI service on demand,
use it, and throw it away — the same shape as `sp vnc`, `sp docker`,
`sp elastic` today.

"Ephemeral" = no long-lived shared service. Each caller gets their own
instance, auto-terminating, billed only for the minutes it ran.

The thing being launched is just a container: `diniscruz/sg-playwright`.
Everything in this doc is plumbing around that one immutable image.

═══════════════════════════════════════════════════════════════════════════════
2. Two ephemeral models — pick per call, not per design
═══════════════════════════════════════════════════════════════════════════════

The spec supports BOTH. They share schemas and the CLI surface; only the
launch backend differs.

2.1 Pod-on-host  (fast, cheap, shared host)
  - The container runs as a *pod* on an already-running host via the
    host-plane pods API (`POST /pods` on `Fast_API__Host__Control`).
  - Boot time: seconds (image already pulled, or one pull then cached).
  - Use when: you have a host EC2 up (e.g. the vault-app stack box) and
    want N short-lived Playwright instances on it cheaply.
  - Isolation: process/container-level, shared kernel + host.
  - This is the default for "give me a Playwright API for 10 minutes".

2.2 EC2-node  (slow, fully isolated, dedicated box)
  - The container runs on its own dedicated EC2 instance, launched via
    the standard SG/Compute node path (`Spec__Service.create_node()` →
    `EC2__Platform` → user-data pulls + runs the image).
  - Boot time: ~30-60 s from a baked AMI, 3-10 min cold.
  - Use when: you need hard isolation, a public Function-URL-equivalent,
    or a box that outlives any single host.
  - Carries the standard auto-terminate timer (`max_hours`, default 1).

Architect note: model 2.1 is the one that makes "ephemeral versions of
this Playwright FastAPI" cheap and instant. Model 2.2 is the fallback for
isolation-sensitive callers. Build 2.1 first.

═══════════════════════════════════════════════════════════════════════════════
3. Where the code lives
═══════════════════════════════════════════════════════════════════════════════

Two trees, mirroring every other spec:

3.1 The spec — `sg_compute_specs/playwright/`
  Already partly here. After Slice 3 (flatten `core/`) it looks like the
  other specs. This doc ADDS the spec-side stack machinery:

    sg_compute_specs/playwright/
      manifest.py                          # EXISTS — Schema__Spec__Manifest__Entry
      version                              # EXISTS
      Dockerfile                           # EXISTS (this session) — the image
      requirements.txt                     # EXISTS (this session)
      schemas/                             # EXISTS (post-Slice-3 flatten)
      service/                             # EXISTS (post-Slice-3 flatten)
      fast_api/                            # EXISTS — the running FastAPI app
      ── ADD ──────────────────────────────────────────────────────────────
      schemas/stack/
        Schema__Playwright__Stack__Create__Request.py
        Schema__Playwright__Stack__Create__Response.py
        Schema__Playwright__Stack__Info.py
        Schema__Playwright__Stack__Delete__Response.py
        Schema__Playwright__Stack__List.py
      service/
        Playwright__Stack__Service.py      # orchestrator — create/list/info/delete/health
        Playwright__Pod__Spec.py           # builds the Schema__Pod__Start__Request
        Playwright__User_Data__Builder.py  # EC2-node path only (model 2.2)
        Playwright__Stack__Mapper.py       # pod/instance dict → Schema__*__Info

  NOTE: do not confuse `service/` (the *running app's* service classes —
  `Playwright__Service`, `Step__Executor`, …) with the new
  `Playwright__Stack__Service` (the *launcher*). Different layers: the
  stack service launches containers; the app service runs inside them.

3.2 The CLI plugin — `sgraph_ai_service_playwright__cli/playwright/`
  New plugin folder, exactly mirroring `…/vnc/`:

    sgraph_ai_service_playwright__cli/playwright/
      __init__.py
      plugin/
        Plugin__Manifest__Playwright.py    # extends Plugin__Manifest__Base
      fast_api/routes/
        Routes__Playwright__Stack.py       # create/list/info/delete/health routes
      cli/
        Renderers.py                       # Rich tables/panels for the typer cmds
      schemas/  collections/  primitives/  # CLI-side view schemas if needed

  Then register it: add `'playwright'` to `PLUGIN_FOLDERS` in
  `sgraph_ai_service_playwright__cli/core/plugin/Plugin__Registry.py`.
  `Fast_API__SP__CLI` discovers it automatically at startup — no manual
  wiring (see `Fast_API__SP__CLI.setup_routes()` line 62-63).

═══════════════════════════════════════════════════════════════════════════════
4. The contract — what each piece does
═══════════════════════════════════════════════════════════════════════════════

4.1 `Plugin__Manifest__Playwright`  (copy `Plugin__Manifest__Vnc` shape)

    class Plugin__Manifest__Playwright(Plugin__Manifest__Base):
        name                 = Safe_Str__Plugin__Name('playwright')
        display_name         = 'Playwright (browser automation API)'
        description          = 'Ephemeral Playwright FastAPI — declarative '
                               'step language, screenshots, sequences.'
        icon                 = '🎭'
        enabled              = True
        stability            = Enum__Plugin__Stability.STABLE
        boot_seconds_typical = 15            # pod path; ~45 for EC2-node
        nav_group            = Enum__Plugin__Nav_Group.BROWSERS

        def service_class(self):  return Playwright__Stack__Service
        def routes_classes(self): return [Routes__Playwright__Stack]
        def catalog_entry(self):  return Schema__Stack__Type__Catalog__Entry(
            type_id              = Enum__Stack__Type.PLAYWRIGHT,   # ADD this enum value
            create_endpoint_path = '/playwright/stack',
            list_endpoint_path   = '/playwright/stacks',
            info_endpoint_path   = '/playwright/stack/{name}',
            delete_endpoint_path = '/playwright/stack/{name}',
            health_endpoint_path = '/playwright/stack/{name}/health',
            default_instance_type= 't3.medium',
            ... )
        def event_topics_emitted(self):
            return ['playwright:stack.created', 'playwright:stack.deleted']

4.2 `Schema__Playwright__Stack__Create__Request`  (pure data, Type_Safe)

    backend        : Enum__Playwright__Backend = POD          # POD | EC2_NODE  (ADD this enum)
    region         : Safe_Str__AWS__Region                    # EC2_NODE path
    host_node      : Safe_Str__Node__Name                     # POD path — which host to place on
    instance_type  : Safe_Str__Text            = 't3.medium'  # EC2_NODE path
    max_hours      : Safe_Int__Max__Hours      = 1            # auto-terminate (EC2_NODE)
    image_tag      : Safe_Str__Image__Tag      = 'latest'     # diniscruz/sg-playwright:<tag>
    api_key        : Safe_Str__Api__Key                       # FAST_API__AUTH__API_KEY__VALUE
    proxy_url      : Safe_Str__Host            = ''           # optional mitmproxy passthrough
    stack_name     : Safe_Str__Stack__Name     = ''           # empty → auto-generated

  Reuse existing primitives from `sg_compute/primitives/` — do NOT invent
  new ones where one exists (`Safe_Str__Docker__Image`, `Safe_Str__Image__Tag`,
  `Safe_Int__Max__Hours`, `Safe_Str__Stack__Name`, …).

4.3 `Playwright__Stack__Service`  (the launcher — mandatory methods)

    def setup(self) -> 'Playwright__Stack__Service':
        # wire host-plane client + EC2 platform; return self

    def create_stack(self, request, creator='') -> ...__Create__Response:
        # backend == POD:
        #   build Schema__Pod__Start__Request via Playwright__Pod__Spec:
        #     image = 'diniscruz/sg-playwright:' + request.image_tag
        #     ports = { '8000/tcp': '<allocated>' }
        #     env   = { 'FAST_API__AUTH__API_KEY__VALUE': request.api_key,
        #               'SG_PLAYWRIGHT__DEFAULT_PROXY_URL': request.proxy_url }
        #     type_id = 'playwright'
        #   POST it to the host-plane pods API on request.host_node
        # backend == EC2_NODE:
        #   render user-data via Playwright__User_Data__Builder
        #   (docker run diniscruz/sg-playwright + auto-terminate timer)
        #   call create_node() through EC2__Platform
        # emit 'playwright:stack.created'; return the response

    def list_stacks(self, region)                  -> ...__List
    def get_stack_info(self, region, stack_name)   -> ...__Info | None
    def delete_stack(self, region, stack_name)     -> ...__Delete__Response
    def health(self, region, stack_name)           -> Schema__Health
        # HTTP GET <stack-url>/health/status through Health__HTTP__Probe

  POD-path lifecycle delegates to the host-plane API
  (`/pods`, `/pods/{name}`, `/pods/{name}/stop`, DELETE `/pods/{name}`) —
  it does NOT shell out to docker. Same reasoning as the vault-app v2
  plan: keeps the orchestration reusable on podman / k8s / Fargate.

4.4 `Routes__Playwright__Stack`  (logic-free — pure delegation)

  POST   /playwright/stack            → service.create_stack()
  GET    /playwright/stacks           → service.list_stacks()
  GET    /playwright/stack/{name}     → service.get_stack_info()
  DELETE /playwright/stack/{name}     → service.delete_stack()
  GET    /playwright/stack/{name}/health → service.health()

  Every route returns `.json()` on a Type_Safe schema. No raw dicts.

═══════════════════════════════════════════════════════════════════════════════
5. The user experience
═══════════════════════════════════════════════════════════════════════════════

Once `'playwright'` is in `PLUGIN_FOLDERS`, the CLI exposes it the same way
as every other spec — both as HTTP routes (above) and as typer commands:

    sp playwright create  --host-node vault-app-box --image-tag latest
                          → starts a pod, prints the pod URL + api-key header
    sp playwright list                  → Rich table of live Playwright stacks
    sp playwright info  happy-turing     → panel: url, backend, age, health
    sp playwright health happy-turing    → polls /health/status until ready
    sp playwright delete happy-turing    → stops + removes the pod

  EC2-node variant:
    sp playwright create --backend ec2-node --region eu-west-2 --max-hours 2

Caller then talks to the returned URL directly — it IS the Playwright
FastAPI (the 25-endpoint surface: /sequence/execute, /browser/*, /screenshot,
…), API-key gated by the value passed at create time.

═══════════════════════════════════════════════════════════════════════════════
6. What this reuses (do not rebuild)
═══════════════════════════════════════════════════════════════════════════════

| Need                       | Reuse                                            |
|----------------------------|--------------------------------------------------|
| The container image        | `diniscruz/sg-playwright` — built this session   |
| Pod lifecycle on a host    | `sg_compute/host_plane/` pods API + `Schema__Pod__Start__Request` |
| EC2 launch / SG / AMI / TTL| `sg_compute/` helpers + `EC2__Platform`          |
| Plugin discovery + routes  | `Plugin__Registry` + `PLUGIN_FOLDERS` + `Plugin__Manifest__Base` |
| Health polling             | `Health__HTTP__Probe` / `Health__Poller`         |
| Stack-name generation      | `Stack__Name__Generator` (adjective-scientist)   |
| Typer command shell        | the existing spec-cli framework (see `…/vnc/cli/`)|
| Schemas / primitives       | `sg_compute/primitives/`, `sg_compute/core/`     |

Net new code is small: 5 schemas, 1 launcher service (+ 3 helpers), 1
routes class, 1 plugin manifest, 2 enums, CLI renderers. Everything else
is composition of code that already exists.

═══════════════════════════════════════════════════════════════════════════════
7. Sequencing
═══════════════════════════════════════════════════════════════════════════════

1. Land Slice 3 first (flatten `core/`, reparent off `Agentic_FastAPI`) —
   the spec must be on the standard layout before adding stack machinery.
2. Add the POD-backend path end to end (schemas → `Playwright__Stack__Service`
   → routes → plugin manifest → register in `PLUGIN_FOLDERS`).
   Deploy-via-pytest: pod up on a host, /health responds, pod removed.
3. Add the EC2_NODE backend path (user-data builder + `create_node`).
4. CLI renderers + typer command polish.
5. Debrief under `team/claude/debriefs/`.

═══════════════════════════════════════════════════════════════════════════════
8. Open questions
═══════════════════════════════════════════════════════════════════════════════

Q1. Port allocation on the POD path — who picks the host port? Architect
    lean: host-plane allocates and returns it (the pods API already owns
    the host's port space); the spec service just asks for "a port".

Q2. How does the caller reach a POD-backend instance from outside the
    host? Via SSM port-forward to the host, or does the host publish a
    port range? Architect lean: SSM port-forward by default (matches the
    vault-app stack's "one external port" posture); a published range is
    an opt-in.

Q3. Does `sp playwright` need its own vault writes (`VAULT_WRITES`
    capability is in the spec manifest)? Architect lean: the running app
    already stages artefacts via the `VAULT` sink; the stack launcher
    itself writes nothing. Keep the launcher vault-free.
</content>
