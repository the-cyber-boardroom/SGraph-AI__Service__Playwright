# v0.1.96 — Command Cleanup, Migration Order, Open Questions

**Series:** 7 of 7 (final).
**Status:** PROPOSED.

This file ties the series together: what the top-level `sp` command list looks like after the split, the ordered phases for landing the change, and the questions needing operator sign-off before Dev starts.

---

## Top-level `sp` after the cleanup

Source for current state: `scripts/provision_ec2.py:1513–3031` and the `--help` output the operator pasted.

Three groups: **stack lifecycle**, **vault tooling**, **AMI tooling**, plus the four section subcommands.

### Stays at top level (stack lifecycle for the Playwright EC2)

`create`, `list`, `info`, `delete`, `connect`, `shell`, `env`, `exec`, `exec-c`, `logs`, `diagnose`, `forward`, `wait`, `clean`, `open`, `screenshot`, `smoke`, `health`, `ensure-passrole`.

`run` stays — it's the vault-runner entry point.

### Vault tooling — stays as a subgroup

`vault-clone`, `vault-list`, `vault-run`, `vault-commit`, `vault-push`, `vault-pull`, `vault-status` regroup under `sp vault` (long alias) / `sp v` (short). Top-level flat `vault-*` aliases are dropped.

| Old | New |
|---|---|
| `sp vault-clone` | `sp vault clone` |
| `sp vault-list` | `sp vault list` |
| `sp vault-run` | `sp vault run` |
| `sp vault-commit` | `sp vault commit` |
| `sp vault-push` | `sp vault push` |
| `sp vault-pull` | `sp vault pull` |
| `sp vault-status` | `sp vault status` |

### AMI tooling — stays as a subgroup

`bake-ami`, `wait-ami`, `tag-ami`, `list-amis`, `create-from-ami` regroup under `sp ami`. **Verbs match the just-shipped `sp el ami {list, create, delete, wait}` pattern** (commits `9e076e3`, `008dae1` on dev) so all four sister sections present the same AMI surface.

| Old | New |
|---|---|
| `sp bake-ami` | `sp ami create` |
| `sp wait-ami` | `sp ami wait` |
| `sp tag-ami` | `sp ami tag` |
| `sp list-amis` | `sp ami list` |
| `sp create-from-ami` | **stays as `sp create-from-ami`** (top-level, mirrors `sp el create-from-ami` pattern — different action: launches an instance from an AMI, not an AMI op itself). |

The convention emerging from `sp el`: **`sp {section} ami {verb}` for AMI ops, `sp {section} create-from-ami` at the section top level for the "launch instance from baked AMI" shortcut.** Doc 4-6 sister sections inherit this from day one.

### Removed (moved to a section)

| Removed top-level command | Replacement |
|---|---|
| `sp forward-prometheus` | `sp prom forward <name>` |
| `sp forward-browser` | `sp nvm forward <name>` or `sp nvm open <name>` |
| `sp forward-dockge` | **deleted** (Dockge dropped; revisit only if revived under `sp nvm`) |
| `sp metrics` | `sp prom metrics <url>` |

### Section subcommands

```
sp el | sp elastic           — Elasticsearch + Kibana (existing)
sp os | sp opensearch        — OpenSearch + Dashboards (NEW; doc 04)
sp prom | sp prometheus      — Prometheus + cAdvisor + node-exporter (NEW; doc 05)
sp nvm | sp browser-viewer   — Nginx + VNC + Mitmproxy (NEW; doc 06)
sp ob | sp observability     — AMP + AMG + OpenSearch (existing; v0.1.72)
```

### Final shape — what `sp --help` looks like

```
Commands:
  create              Provision the Playwright EC2 (playwright + agent-mitmproxy).
  list                List Playwright stacks.
  info                Show details for a Playwright stack.
  delete              Terminate.
  connect             SSM shell on the host.
  shell               SSM shell inside a container.
  env                 Print env exports.
  exec / exec-c       Run a command on host / inside the playwright container.
  logs                docker compose logs via SSM.
  diagnose            Deep system check.
  forward             Local port-forward.
  wait                Poll health endpoint.
  clean               Pre-bake cleanup.
  open                Open the launcher UI.
  screenshot          Quick screenshot smoke.
  smoke               URL-list screenshot smoke test.
  health              Health checks.
  run                 Vault-driven scenario runner.
  ensure-passrole     IAM policy fix.

Subcommands:
  vault   (v)         Vault clone/run/commit/push/pull/status/list.
  ami                 AMI bake/wait/tag/list/create-instance.
  el      (elastic)   Elasticsearch + Kibana stacks.
  os      (opensearch) OpenSearch + Dashboards stacks.
  prom    (prometheus) Prometheus + cAdvisor + node-exporter stacks.
  nvm     (browser-viewer) Browser-viewer stacks.
  ob      (observability) AMP/AMG/OpenSearch ephemeral stacks.
```

19 top-level commands → still a lot, but every one is **about the Playwright EC2 itself**, not about an embedded subsystem.

---

## Migration order

Per the operator's agreement, the order is:

### Phase A — API consolidation (foundation)

1. Promote `Stack__Naming` helpers from `Elastic__AWS__Client` to a shared `cli/aws/` module.
2. Land `Image__Build__Service` + Type_Safe result schemas; reduce `Build__Docker__SGraph_AI__Service__Playwright` and `Docker__SP__CLI` to thin wrappers.
3. Migrate the rest of `provision_ec2.py` userdata/SG/IAM logic into `Ec2__Service` + `Ec2__AWS__Client`. Reduce typer commands to wrappers.
4. Add the missing FastAPI routes for any EC2 op the CLI exposes (so the duality is complete before sister sections arrive).

Exit criterion: every existing `sp` command for the Playwright host has a FastAPI counterpart.

### Phase B — Carve out sister sections

Order chosen for **lowest blast radius first** — sister sections can be created before the Playwright EC2 is stripped, because they're additive.

5. **`sp os`** — folder, service, AWS client, schemas, compose, routes, tests. Land green on its own. fluent-bit moves later.
6. **`sp prom`** — same. Add the Playwright EC2 as a scrape target while it still has its embedded Prometheus, to validate the cross-host scrape works.
7. **`sp nvm`** — same. Clone `agent_mitmproxy` for the human-debug mitmproxy.

Each sister section is its own PR / commit + debrief, mirroring the `v0.1.32__agent-mitmproxy` pattern.

### Phase C — Strip the Playwright EC2

8. Confirm `sp prom` (or the operator's local Prometheus) is scraping Playwright successfully.
9. Confirm `sp nvm` is usable for the streaming-browser workflow.
10. Delete the moved containers from `provision_ec2.py:226–337`.
11. Bake a new AMI; tag the previous AMI as `superseded`.
12. Update the reality doc.

### Phase D — Command cleanup

13. Regroup `vault-*` under `sp vault`; `*-ami` under `sp ami`.
14. Delete `forward-prometheus`, `forward-browser`, `forward-dockge`, `metrics`.
15. Update operator docs / runbook references.

Each phase ships green and observable; any phase can be paused without blocking earlier ones.

---

## Open questions for sign-off

Tagged where they live; collected here for one-shot review.

**On scope (overview):**
- O1. ✅ ANSWERED 2026-04-25 — `sp el` stays CLI-only this round. No API routes added to elastic in this plan.
- O2. ✅ ANSWERED 2026-04-25 — true clean slate. No AMIs to preserve.

**On API consolidation (doc 02):**
- A1. `Image__Build__Service` location — `cli/image/` or repo-root `image/`?
- A2. Long-running EC2 ops — `202 Accepted` + status polling, or synchronous-with-timeout?
- A3. Generic `Routes__Ec2__Stack` shared by all sections, or per-section route modules?

**On stripping (doc 03):**
- S1. Is `agent-mitmproxy` admin FastAPI on the stripped Playwright EC2 still externally exposed?
- S2. Close port 8080 entirely on the stripped host, or keep it SG-restricted?
- S3. Delete moved volumes outright, or leave the definitions?

**On `sp os` (doc 04):**
- OS1. OpenSearch version pin?
- OS2. Single-node vs multi-node default?
- OS3. Auth model — basic + IP allowlist (matches `sp el`) or IAM/Cognito?
- OS4. Reuse or fork the elastic dashboard generator?

**On `sp prom` (doc 05):**
- P1. Bundle Grafana into `sp prom`?
- P2. Default retention?
- P3. Multi-target out of the box?

**On `sp nvm` (doc 06):**
- N1. Section name — keep `nvm` or pick `bv` / `vnc` / `viewer`?
- N2. Multiple browsers from day one, or chromium-only?
- N3. Profile persistence — AMI-baked / EBS / wiped?
- N4. Audit export on delete by default?
- N5. Default operator interceptor — shared or per-stack?

**On the cleanup (this doc):**
- C1. `sp vault` / `sp ami` regrouping — keep both flat aliases AND grouped form during a transition window, or hard cut?
- C2. `sp metrics <url>` — does it stay top-level for "any Prometheus endpoint" use, or is `sp prom metrics` enough?

---

## Once signed off

Dev opens a feature branch off `dev` matching `claude/refactor-playwright-image-{session}`. Phase A lands as the first commit; each subsequent phase as its own commit with a paired debrief in `team/claude/debriefs/`. Reality doc updated in the same commit as the last code change of each phase.
