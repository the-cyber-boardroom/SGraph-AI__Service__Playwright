---
title:      sg-playwright-vnc — interactive VNC browsers on the image we control
version:    v0.2.67
date:       2026-07-08
status:     PROPOSED — does not exist yet
role:       architect
supersedes: v0.2.65__content-proxy__interactive-browser-vnc-sidecar.md (jlesage/firefox sidecar)
audience:   dev (image + service), content_proxy team, QA
---

## 1. Why we are stepping back

The v0.2.65 plan wired `jlesage/firefox` containers into the content_proxy stack
(`cp-firefox-{i}` at `/browser/firefox/{i}`). It worked — the browser proxied
through mitmproxy and rendered transformed content — but every integration point
fought us because **we do not control that image**:

| Pain (all hit in production testing) | Root cause |
|---|---|
| `SECURE_CONNECTION=1` 307 loop | jlesage's own TLS layer fighting our edge |
| noVNC breaks under `/browser/firefox/{i}` sub-path | jlesage's baseimage-gui emits root-absolute URLs; not ours to fix |
| certutil NSS-lock boot hang (`preparing Firefox 2 profile`) | CA trust had to be injected into a foreign profile DB from the host |
| user.js proxy config from the host | no API; file-poking a foreign layout |
| Firefox only | second image + second integration for Chrome |

Meanwhile **sg-playwright already solves the hard parts** in code we own:

- `Browser__Launcher.build_launch_kwargs()` already honours
  `SG_PLAYWRIGHT__DEFAULT_PROXY_URL` — every launched browser goes through
  mitmproxy-int with zero profile hacking.
- `Page__Factory` already honours `SG_PLAYWRIGHT__IGNORE_HTTPS_ERRORS` — the
  mitmproxy CA problem (the certutil hang) **does not exist** on this path.
- `Schema__Browser__Config.headless` already exists — headed launch is a flag,
  not a feature.
- The base image (`mcr.microsoft.com/playwright/python:v1.58.0-noble`) ships
  **Chromium, Firefox AND WebKit** with all system deps.

The only thing missing is a screen to draw on and a way to look at it.

## 2. Proposal

A new image variant, **`diniscruz/sg-playwright-vnc`**, built FROM the existing
sg-playwright Dockerfile output plus a thin desktop layer:

```
sg-playwright (FastAPI :8000, Browser__Launcher, Step__Executor)   ← unchanged
  + Xvfb        (virtual X display :99)
  + openbox     (minimal window manager — windows get borders/move/close)
  + x11vnc      (VNC server exposing :99, localhost-only)
  + noVNC + websockify  (browser-based VNC client on :6080)
  + supervisord (starts the four above + the FastAPI service)
```

Ports: `8000` (existing API), `6080` (noVNC web client). VNC `5900` stays
container-internal (only websockify talks to it).

One env flag drives the mode:

- `SG_PLAYWRIGHT__DISPLAY_MODE=headless` (default) → today's behaviour, X stack
  not started. The base image and existing deployments are untouched.
- `SG_PLAYWRIGHT__DISPLAY_MODE=vnc` → supervisord starts Xvfb/openbox/x11vnc/
  noVNC; `DISPLAY=:99` is exported; `Browser__Launcher` defaults `headless=False`.

### 2.1 The unlock nobody else offers

Because the API and the display share one container, the **same browser** is
reachable both ways:

1. **Interactive** — user opens noVNC, drives a headed browser by hand through
   the mitmproxy (the jlesage use case, minus the pain).
2. **Watched automation** — a `/sequence` or `/pw` API call launches headed;
   the user literally watches the steps execute live in noVNC.
3. **Hybrid / takeover** — automation navigates + logs in, human takes the
   mouse; or human navigates somewhere, automation continues. This is the
   feature path for "customise the browser UI, theme, bookmarks": all of it
   becomes API endpoints on a service we already own.

### 2.2 Browser start for the interactive case

A headed browser must exist before the user connects. Small addition to the
service (PROPOSED):

- `POST /desktop/browser` `{engine: chromium|firefox, start_url}` — launches a
  headed browser on `:99` via the existing `Browser__Launcher` (kept alive as a
  session, not per-request).
- `SG_PLAYWRIGHT__AUTOSTART_BROWSER=chromium|firefox` — boot-time convenience:
  container starts with a browser already open (content_proxy uses this).

Engine per container is an env choice, not an image choice — **one image serves
both the Chrome and Firefox requests** from the original ask.

## 3. content_proxy integration (replaces the jlesage fleet)

- Compose: `cp-browser-{i}` from `sg-playwright-vnc` with
  `SG_PLAYWRIGHT__DEFAULT_PROXY_URL=http://mitmproxy-int:8080`,
  `SG_PLAYWRIGHT__IGNORE_HTTPS_ERRORS=true`,
  `SG_PLAYWRIGHT__DISPLAY_MODE=vnc`, `SG_PLAYWRIGHT__AUTOSTART_BROWSER=…`.
  No host profile dir, no certutil boot block, no CA wait loop — the whole
  `_firefox_block` in the user-data builder collapses to nothing.
- Edge: `handle_path /browser/{i}/* → cp-browser-{i}:6080`. Vanilla noVNC uses
  relative asset paths and a configurable websockify path, so sub-path routing
  is solvable in config we control (test in P1 before wiring).
- CLI: `--firefox N` becomes `--browsers N` (`--browser-engine chromium|firefox`);
  old flag kept as an alias during transition.
- Diagnose: `cp-browser-{i}` in `expected_containers`; the boot-ok Firefox-prep
  tolerance becomes dead code once `_firefox_block` is gone.

## 4. Customization roadmap (the "next features" this unblocks)

All PROPOSED, all natural Type_Safe endpoints once the browser is ours:

| Feature | Mechanism |
|---|---|
| Bookmarks | launch persistent context with a prepared profile; or CDP/`places.sqlite` written by the service pre-launch |
| Theme / UI chrome | Chromium `--force-dark-mode`, managed policies JSON; Firefox `user.js` + `userChrome.css` written by the service (our profile, no lock race — browser not yet started) |
| Start page / kiosk | `--kiosk`, `--app=<url>` launch args (already supported via `launch_args`) |
| Locale / timezone / viewport | existing `Schema__Browser__Config` surface |
| Session save/restore | persistent context dir per user, S3 sync (ties into the vault/S3-twin work) |

## 5. Phases

**P0 — strip the jlesage fleet from content_proxy (targeted removal, NOT git revert)**
content_proxy is the right spec to host the new image, and it should meet it
clean: the jlesage `--firefox` path is half-broken in production (noVNC
sub-path) and must not look like a supported feature while P1–P2 run. But the
fleet commit (`d520b16`) is already merged into `dev` with the keeper fixes
interleaved on the same lines (deadman-switch ordering, Caddy IP-inject,
`--edge-auth`, diagnose OK@, proxyauth/block_global) — a `git revert` would
tear those out or conflict on every hunk. So: one forward commit that deletes
exactly the jlesage-specific surface —

| Removed (jlesage-specific) | Kept (generic, the new image needs it) |
|---|---|
| `--firefox` flag, `firefox_count` schema field + `cp:firefox-count` tag | `--edge-auth` gate, edge template auth variants |
| `browser_block` (jlesage service def, :5800) in the compose template | compose/edge placeholder seams (`browser_block`, fleet routes slot) — emit `''` again |
| `_firefox_block` / `FIREFOX_ONE` / `FIREFOX_USER_JS` / nss-tools in user-data | deadman-first ordering, `CADDY_IP_INJECT`, proxyauth generation |
| `/browser/firefox/{i}` edge routes | `expected_containers` firefox param dropped; mechanism stays |
| `boot-ok` firefox-prep tolerance + its tests | diagnose renderer OK@ column, off-TTY handling |

Result: committed compose/Caddyfile render byte-identical to the pre-fleet
state (the drift-guard tests prove it), P3 lands on a clean base, and the
jlesage recipe survives in git history + the superseded v0.2.65 plan if ever
needed. `sg_compute_specs/firefox/` and `sg_compute_specs/vnc/` (the separate
EC2 specs) are untouched.

**P1 — image spike (no service changes)**
Dockerfile.vnc + supervisord conf. Manual verify: `docker run`, open `:6080`,
see openbox; `docker exec` a headed `playwright open` on `:99`, drive it in
noVNC. Verify noVNC under a path-stripping reverse proxy (the sub-path
question) with a throwaway Caddy. Exit criteria: image < ~2.5 GB, cold start
< 10 s, noVNC usable behind a sub-path.

**P2 — service awareness**
`SG_PLAYWRIGHT__DISPLAY_MODE`, `DISPLAY` export, `headless` default flip in vnc
mode, `POST /desktop/browser` + autostart. Tests: in-memory for the config
plumbing; gated real-Chromium test that a headed launch succeeds under Xvfb
(CI runner has no display → the Xvfb in the image provides it).

**P3 — content_proxy switch**
Compose/user-data/edge/CLI/diagnose changes from §3. Delete `_firefox_block`.
EC2 validation: `sg content-proxy create --browsers 2 --with-aws-dns --wait`.

**P4 — customization endpoints** (bookmarks/theme/profile — separate brief once
P3 is live and the exact asks are prioritised).

## 6. Files & folders (per phase)

New files follow the repo rules: one class per file, empty `__init__.py`,
`Enum__`/`Schema__` naming, routes delegate only.

### P1 — image spike (new folder `sg_compute_specs/playwright/vnc/`)

```
sg_compute_specs/playwright/vnc/
├── Dockerfile                    # FROM ${BASE_IMAGE:-diniscruz/sg-playwright:latest} + X stack
├── supervisord.conf              # xvfb, openbox, x11vnc, novnc(websockify), fastapi — one process tree
├── README.md                     # build/run/verify commands (the P1 exit criteria as a script)
└── spike/
    ├── docker-compose.yml        # sg-playwright-vnc + throwaway caddy for the sub-path test
    └── Caddyfile                 # handle_path /browser/1/* → :6080 (the jlesage failure case, proven here)
```

The Dockerfile takes `ARG BASE_IMAGE` so CI can pin it to the exact by-digest
sg-playwright image built in the same run (see §7), while local builds default
to `:latest`.

### P2 — service awareness

```
edits:
  sg_compute_specs/playwright/core/consts/env_vars.py            # + ENV_VAR__DISPLAY_MODE, ENV_VAR__AUTOSTART_BROWSER
  sg_compute_specs/playwright/core/service/Browser__Launcher.py  # headed default-args seam (no --single-process when headed)
  …/core/fast_api/Fast_API__Playwright__Service.py               # wire Routes__Desktop (vnc mode only)
new:
  …/core/schemas/enums/Enum__Display__Mode.py                    # HEADLESS | VNC
  …/core/schemas/desktop/Schema__Desktop__Browser__Request.py    # engine + start_url (pure data)
  …/core/schemas/desktop/Schema__Desktop__Browser__Response.py
  …/core/service/Desktop__Browser__Manager.py                    # long-lived headed session via Browser__Launcher (register/stop reuse)
  …/core/fast_api/routes/Routes__Desktop.py                      # POST /desktop/browser — pure delegation
tests (repo-root tests/, mirrors existing layout):
  tests/unit/schemas/enums/…                                     # extend enum drift test
  tests/unit/service/test_Desktop__Browser__Manager.py           # in-memory, no mocks
  tests/unit/fast_api/routes/test_Routes__Desktop.py
  tests/integration/test_Desktop__Headed__Launch.py              # gated on SG_PLAYWRIGHT__CHROMIUM_EXECUTABLE + a DISPLAY/Xvfb
```

### P3 — content_proxy switch (edits only, plus deletions)

```
edits:
  sg_compute_specs/content_proxy/service/Content_Proxy__Compose__Template.py   # browser_block → sg-playwright-vnc (cp-browser-{i}, :6080)
  sg_compute_specs/content_proxy/service/Content_Proxy__Edge__Template.py      # /browser/{i} routes (engine-agnostic path)
  sg_compute_specs/content_proxy/service/Content_Proxy__User_Data__Builder.py  # DELETE _firefox_block / FIREFOX_ONE / FIREFOX_USER_JS / nss-tools
  sg_compute_specs/content_proxy/service/Content_Proxy__Service.py             # expected_containers cp-browser-{i}; drop the firefox-prep boot-ok tolerance
  sg_compute_specs/content_proxy/schemas/Schema__Content_Proxy__Create__Request.py  # + browser_count, browser_engine (firefox_count kept as alias, then removed)
  sg_compute_specs/content_proxy/cli/Cli__Content_Proxy.py                     # --browsers N --browser-engine chromium|firefox
  sg_compute_specs/content_proxy/cli/Renderers.py                              # info/create URL blocks
  sg_compute_specs/content_proxy/service/Content_Proxy__Stack__Mapper.py       # cp:browser-count / cp:browser-engine tags
  + the matching test files for each of the above
librarian (after code lands):
  team/roles/librarian/reality/content-proxy/index.md                          # /browser section rewrite
  library/catalogue/…                                                          # sg-playwright-vnc image entry
```

P4 files are deferred to their own brief (§5).

## 7. CI pipeline changes (`.github/workflows/ci-pipeline.yml`)

Today's shape (shared `workflow_call`, driven by `__dev/__main/__prod` wrappers):
`run-unit-tests → increment-tag → build-amd64 + build-arm64 (by-digest artifacts)
→ integration-test-image (pre-tag, runs the container) → push-playwright-manifest
(diniscruz/sg-playwright:<version> + :latest)`.

The VNC image extends the same run — same trigger, same version tag, so both
images always move in lockstep with one `version` file:

```
env:
  REGISTRY_IMAGE_VNC: 'diniscruz/sg-playwright-vnc'          # alongside REGISTRY_IMAGE

new jobs:
  build-vnc-amd64:
    needs: [build-amd64]                                     # base must exist first
    # docker/build-push-action@v6, file: sg_compute_specs/playwright/vnc/Dockerfile
    # build-arg BASE_IMAGE=diniscruz/sg-playwright@sha256:<digest-from-build-amd64>
    #   → the VNC layer is stacked on the EXACT image this run built, not :latest
    # pushes by digest, uploads digest-vnc-amd64 artifact (same pattern as base)

  build-vnc-arm64:
    needs: [build-arm64]                                     # same, arm64 digest

  integration-test-vnc-image:
    needs: [build-vnc-amd64]
    # pre-tag gate, mirrors integration-test-image and adds the VNC assertions:
    #   docker run -e SG_PLAYWRIGHT__DISPLAY_MODE=vnc -p 8000:8000 -p 6080:6080
    #   1. existing API health tier still passes on :8000   (regression: VNC layer must not break the service)
    #   2. GET :6080/vnc.html → 200                          (noVNC serving)
    #   3. supervisorctl status → all 5 programs RUNNING     (xvfb/openbox/x11vnc/novnc/fastapi)
    #   4. POST /desktop/browser {engine: chromium} → 200    (headed launch works under Xvfb — P2+)

  push-vnc-manifest:
    needs: [build-vnc-amd64, build-vnc-arm64, integration-test-vnc-image]
    # buildx imagetools create → diniscruz/sg-playwright-vnc:<version> + :latest
```

Notes:
- **Failure isolation**: VNC jobs hang off the base builds; a VNC-only failure
  blocks `push-vnc-manifest` but NOT `push-playwright-manifest` — the service
  image ships even if the desktop layer regresses.
- **Cost**: ~3–5 min added wall-clock (VNC layer is apt + config on a cached
  base; the arm64 leg is the slow one). Acceptable on dev; if it bites, gate
  `build-vnc-*` behind a `workflow_dispatch` input `build_vnc` defaulting true.
- **Secrets**: none new — same `DOCKERHUB_TOKEN`, new repo `sg-playwright-vnc`
  must exist on Docker Hub before first push (one-time manual step).
- **`bake-ami.yml` / content_proxy**: no change until P3 — EC2 boxes pull from
  Docker Hub at boot, so P3 only changes which image name the compose template
  writes.

## 8. Where and how we test — `sg content-proxy *` is the acceptance surface

Each phase has its own test surface; **the E2E acceptance for the whole effort
is the `sg content-proxy` CLI on a real EC2 stack** — the same commands and the
same diagnose table used throughout the jlesage debugging, so results are
directly comparable.

| Phase | Surface | What proves it |
|---|---|---|
| P0 | pytest (content_proxy suite) | drift guards: committed compose/Caddyfile byte-identical to pre-fleet render; `--firefox` gone from `create --help` |
| P1 | local docker + the `spike/` compose | headed browser visible in noVNC at `:6080`; **noVNC works behind the path-stripping Caddy sub-path** (the jlesage killer, proven before any service code is written); WebSocket survives the proxy |
| P2 | pytest (unit, in-memory) + CI `integration-test-vnc-image` | display-mode plumbing; `POST /desktop/browser` contract; headed Chromium launches under the image's own Xvfb in CI — no laptop needed |
| P3 | **`sg content-proxy` local + EC2** | see below |

### P3 local (fast loop, before any EC2 spend)

`sg content-proxy local up --browsers 1` (wiring `local up` for the fleet is a
P3 deliverable — the jlesage fleet never had it, which is why every iteration
cost an EC2 create). Then: open `https://localhost/browser/1/`, see the headed
browser, load a page, confirm the mitm `XXX` transform markers render.

### P3 EC2 (the real acceptance run)

```
sg content-proxy create --browsers 2 --with-aws-dns --wait   # all checks ✓, no boot-ok tolerance needed
sg content-proxy check  <stack>                               # cp-browser-1/2 in containers-up
sg content-proxy smoke  <stack>                               # transform chain unchanged
sg content-proxy logs   <stack> --source browser-1            # new LOG_SOURCES entries (supervisord tail)
open https://<stack>.sg-compute.sgraph.ai/browser/1/          # noVNC desktop, trusted cert
open https://<stack>.sg-compute.sgraph.ai/pw/                 # /pw unaffected
```

Pass criteria: browser desktop loads through the edge (no SSM tunnel needed),
pages render with transform markers, `--edge-auth` 401s an unauthenticated
`/browser/1/`, `delete` reaps everything, and the box self-terminates at
`--max-hours`.

### Diagnose additions (P3)

- `containers-up` expects `cp-browser-{i}` (count from the `cp:browser-count` tag).
- New per-browser check `browser-http` — on-box `curl :6080/vnc.html` per
  container, so `check`/`wait` prove the desktop is actually serving, not just
  that the container is Up (the jlesage fleet only ever checked Up).
- New `LOG_SOURCES`: `browser-{i}` → `docker logs cp-browser-{i}`.

## 9. Risks / open questions

- **Image size**: base is already ~2 GB; X stack adds ~150–300 MB. Acceptable —
  same EC2 pulls it once.
- **Memory**: a headed Chromium ≈ 300–500 MB/user. `t3.large` (8 GB) fits ~4–6
  interactive browsers + stack. Document per-count instance-type guidance.
- **`--single-process` / `--disable-gpu` defaults** were tuned for Lambda;
  headed mode needs its own default arg set (drop `--single-process`, likely
  keep `--no-sandbox` as root-in-container, add `--start-maximized`).
- **noVNC auth**: :6080 is unauthenticated inside the docker network; the edge
  gate (`--edge-auth`, already built) is the auth story — must be default-ON
  for browser routes when this ships.
- **WebSocket through the edge**: Caddy proxies WebSockets natively; needs an
  explicit P1 test through the sub-path.
- **Keep vs kill the jlesage path**: RESOLVED — killed up-front in P0 (targeted
  removal; fleet is ephemeral, nothing persists, recipe stays in git history).
- **TLS-by-IP at the edge** is orthogonal and still open (browsers send no SNI
  for IPs). Tracked separately: cert-init letsencrypt-ip certs mounted into
  Caddy, or auto-`sslip.io` hostname. Not this plan's problem.
