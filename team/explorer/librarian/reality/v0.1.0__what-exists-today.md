# Reality — v0.1.0 — 2026-04-16

**Source of truth for what exists today.** Agents must update this file whenever code changes.

---

## Summary

**Phase 0 in progress.** Repo skeleton, Dockerfile, and CI workflow scaffolding have been created. No service code, routes, schemas, or tests have been implemented yet.

---

## What Exists

### Repo structure
- `sgraph_ai_service_playwright/` package with `version` file (`v0.1.0`) and `__init__.py` exposing `path`.
- Directory skeleton for `schemas/{primitives,enums,core,steps,results,session,sequence,collections}`, `fast_api/routes`, `service`, `dispatcher`, `client`, `docker/images/sgraph_ai_service_playwright`, `consts`.
- `tests/{unit,integration,docker,deploy}` directory skeleton.
- `team/explorer/librarian/reality/` created with this file.

### Packaging
- `pyproject.toml` (Poetry, Python ^3.12) with fastapi, uvicorn, playwright, osbot-utils, osbot-aws, osbot-fast-api, osbot-fast-api-serverless.
- `requirements.txt` at repo root (mirrors runtime deps).

### Docker
- `sgraph_ai_service_playwright/docker/images/sgraph_ai_service_playwright/dockerfile` — Playwright 1.58.2-noble + LWA 1.0.0.
- `sgraph_ai_service_playwright/docker/images/sgraph_ai_service_playwright/requirements.txt` — image-level deps.
- `sgraph_ai_service_playwright/fast_api/lambda_handler.py` — placeholder `run()` that will boot the FastAPI app once the service class lands.

### CI
- `.github/workflows/ci-pipeline.yml` — base workflow (workflow_call); Job 1 (unit tests) defined; later jobs stubbed or gated on the code that will land in Phase 1+.
- `.github/workflows/ci-pipeline__dev.yml` — dev wrapper.
- `.github/workflows/ci-pipeline__main.yml` — main wrapper.
- `.github/workflows/ci-pipeline__prod.yml` — prod (manual dispatch).

### Agent guidance
- `.claude/CLAUDE.md` and `.claude/explorer/CLAUDE.md` adapted from dev-pack templates.

---

## What Does NOT Exist (PROPOSED — does not exist yet)

- All Safe_* custom primitives
- All Enum__* types (17 planned)
- All Schema__* classes
- Step schema registries
- 12 service classes (`Playwright__Service`, `Step__Executor`, `Capability__Detector`, `Session__Manager`, `Artefact__Writer`, `Credentials__Loader`, `Request__Validator`, `JS__Expression__Allowlist`, `Action__Runner`, `Sequence__Runner`, `Sequence__Dispatcher`, `Browser__Launcher`)
- 4 route classes (`Routes__Health`, `Routes__Session`, `Routes__Browser`, `Routes__Sequence`)
- `Fast_API__Playwright__Service`
- Docker infrastructure classes (Base + Build/ECR/Lambda/Local)
- Deploy-via-pytest tests
- Client package + registration helpers
- Any tests (unit/integration/docker/deploy)

---

## Next

Phase 0 close-out:
1. Verify Dockerfile builds locally (blocked on Docker availability in this session).
2. Implement Docker infrastructure classes.
3. Add `tests/docker/test_Build__Docker__SGraph-AI__Service__Playwright.py`.

Phase 1 kick-off:
1. Custom Safe_* primitives with tests.
2. All Enum__* types.
3. Core schemas.
4. Step schemas + registries.
