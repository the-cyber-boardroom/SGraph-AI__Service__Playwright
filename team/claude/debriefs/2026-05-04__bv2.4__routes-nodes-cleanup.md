# BV2.4 — Refactor `Routes__Compute__Nodes` (no logic, no raw dicts, no mocks)

**Date:** 2026-05-04
**Phase:** BV2.4
**Commit:** TBD
**Branch:** `claude/bv2-4-routes-nodes-cleanup-MXKIP`

---

## What shipped

### New files
- `sg_compute/platforms/exceptions/__init__.py` — empty package init
- `sg_compute/platforms/exceptions/Exception__AWS__No_Credentials.py` — platform-level credentials exception (not EC2-specific)

### Modified files
- `sg_compute/core/node/schemas/Schema__Node__List.py` — added `total: int` and `region: str` fields
- `sg_compute/platforms/ec2/EC2__Platform.py` — `list_nodes` wraps boto3 call; catches credential errors and re-raises as `Exception__AWS__No_Credentials`; sets `total` and `region` on returned `Schema__Node__List`
- `sg_compute/control_plane/routes/Routes__Compute__Nodes.py` — rewritten: constructor injection `platform: Platform`; pure delegation; all three endpoints return `<schema>.json()`; no business logic; no `_platform()` helper
- `sg_compute/control_plane/Fast_API__Compute.py` — added `platform: Platform` attribute; `_register_exception_handlers()` maps `Exception__AWS__No_Credentials` → HTTP 503; `_live_platform()` swaps abstract base for `EC2__Platform` when not injected; `_mount_control_routes` passes platform to routes
- `sg_compute__tests/control_plane/test_Routes__Compute__Nodes.py` — rewritten: three fake `Platform` subclasses (`Fake__Platform__Empty`, `Fake__Platform__With_Node`, `Fake__Platform__No_Creds`); zero `mock.patch` uses; 8 tests all passing; credentials test uses `Fast_API__Compute` with injected fake to exercise the registered exception handler

## Test results

- `sg_compute__tests/control_plane/`: **34 passed**
- `sg_compute__tests/` full: **231 passed, 25 skipped, 0 failed**

## Acceptance criteria — checked

- [x] `Routes__Compute__Nodes` returns `<schema>.json()` for all three endpoints
- [x] No business logic in routes (credential check moved to `EC2__Platform`)
- [x] Constructor injection `platform: Platform` — no module-level helpers
- [x] `grep -c 'mock.patch' test_Routes__Compute__Nodes.py` → `0`
- [x] `Exception__AWS__No_Credentials` handler registered on `Fast_API__Compute`
- [x] All tests green (231 passed)

## Good failures

None — the refactor was straightforward given the clear pattern established by the phase brief.

## Pattern set

This establishes the constructor-injection + fake-subclass pattern for all subsequent control-plane route refactors (BV2.3, BV2.5).
