# Debrief — BV2.11: Lambda packaging cutover; delete legacy sgraph_ai_service_playwright/

**Date:** 2026-05-05
**Branch:** `claude/bv2-11-lambda-cutover-MXKIP`
**Commit:** `59d060a`
**Status:** ✅ Complete

---

## What Was Done

Executed the Lambda packaging cutover from `sgraph_ai_service_playwright/` to `sg_compute_specs/playwright/core/`. Deleted 175 legacy files across the package tree.

### Key changes

1. **`lambda_entry.py`** — imports `Agentic_Boot_Shim` from `sg_compute_specs.playwright.core.agentic_fastapi_aws`; `PLAYWRIGHT_FAST_API_CLASS_PATH` updated to `sg_compute_specs.playwright.core.fast_api.Fast_API__Playwright__Service.Fast_API__Playwright__Service`.

2. **`sg_compute_specs/playwright/core/docker/Build__Docker__SGraph_AI__Service__Playwright.build_request()`** — now stages `sg_compute_specs/` tree (with `tests/` and `ui/` excluded via `extra_ignore_names`) instead of `sgraph_ai_service_playwright/`. `repo_root` computed 3 levels up from `core/`.

3. **`sg_compute_specs/playwright/core/agentic_fastapi/Agentic_FastAPI.py`** — `_REPO_ROOT_GUESS` now goes 3 levels up from `_APP_PACKAGE_DIR` (`core/ → playwright/ → sg_compute_specs/ → repo root`).

4. **`sgraph_ai_service_playwright/` deleted** — `git rm -r`, 175 files, 6348 lines removed.

5. **`pyproject.toml`** — `packages` updated to `[sg_compute, sg_compute_specs, sgraph_ai_service_playwright__cli, scripts]`; removed `sgraph_ai_service_playwright/version` from `include`.

6. **CI workflows** — `ci-pipeline.yml`, `ci-pipeline__dev.yml`, `ci-pipeline__main.yml`: path filters updated from `sgraph_ai_service_playwright/**` to `sg_compute_specs/playwright/**`; version file path updated.

7. **55 test/script files** — bulk sed from `from sgraph_ai_service_playwright.` → `from sg_compute_specs.playwright.core.` Includes `scripts/`, `sgraph_ai_service_playwright__cli/`, `tests/unit/`, `tests/integration/`, `tests/local/`, `tests/deploy/`, `tests/docker/`.

8. **SP CLI Docker/deploy** — `Docker__SP__CLI.build_request()` drops the `sgraph_ai_service_playwright` stage item (now covered by `sg_compute_specs`). `deploy_code.py` PACKAGE_NAMES updated. `runtime_version.py` version file path updated.

9. **`tests/ci/test_no_legacy_imports.py`** — regex narrowed from `sgraph_ai_service_playwright` to `sgraph_ai_service_playwright[^_]` to allow `sgraph_ai_service_playwright__cli` imports.

---

## Failures Encountered

### Good failures (caught early, corrected)

1. **`Agentic_FastAPI._REPO_ROOT_GUESS` wrong depth** — After the package moved from root-sibling to `sg_compute_specs/playwright/core/`, `os.path.dirname(os.path.dirname(_THIS_DIR))` reached `sg_compute_specs/playwright/` instead of the repo root. Fixed by going 3 levels up.

2. **`test_Agentic_Boot_Shim.py` `fake_path` mismatch** — Tests that inject `_BoomFinder` interceptors used `sgraph_ai_service_playwright.fast_api.Fast_API__Playwright__Service` as the module name. After changing `PLAYWRIGHT_CLASS_PATH`, the interceptors never triggered. Fixed by updating all `fake_path` constants.

3. **`test_package_code.py` zip shape assertions** — Tests asserted all zip entries start with `sgraph_ai_service_playwright/` and end in `.py`. `sg_compute_specs/` includes spec UI files (`.js`, `.html`, `.css`). Updated assertions.

4. **SP CLI `Docker__SP__CLI` still staged deleted package** — `build_request()` referenced `sgraph_ai_service_playwright/` tree. Removed that stage item; `sg_compute_specs/` (already staged) now supplies all needed imports.

5. **`runtime_version.py` fallback path** — `IMAGE_VERSION_FILE_RELATIVE` still pointed to `sgraph_ai_service_playwright/version`. Updated to `sg_compute_specs/playwright/core/version`.

---

## Test Results

- **sg_compute__tests**: 356 passed, 2 pre-existing CORS failures
- **tests/unit/** (excl. docker, api_site): 2151 passed, 1 pre-existing `agent_mitmproxy` cryptography failure
- **tests/ci/test_no_legacy_imports**: passes

---

## Acceptance Criteria Status

- ✅ `lambda_entry.py` imports from `sg_compute_specs.playwright.core`
- ✅ `sgraph_ai_service_playwright/` directory does not exist
- ✅ `pyproject.toml` does not list the legacy package
- ✅ Tests pass (modulo pre-existing failures)
- ⚠️ Production smoke test (Lambda deploy + invoke) — not performed; requires DevOps pair
- ⚠️ Reality doc update — pending
