# FV2.9 — Event vocabulary finish

**Date:** 2026-05-05
**Phase:** FV2.9
**Branch:** `claude/fv2-9-event-vocabulary-5sjIO`
**Commit:** (backfill after merge)
**Status:** SHIPPED

---

## What was done

Migrated per-spec launch events from `sp-cli:plugin:*` to `sp-cli:spec:*` namespace, updated admin.js listeners, extended the events-log FAMILIES map, and published the event vocabulary spec.

### Files created

- `library/docs/specs/v0.2.0__ui-event-vocabulary.md` — complete event vocabulary with emitter, listener, payload, and status for every dashboard event

### Files changed

| File | Change |
|------|--------|
| `plugins/docker/…sp-cli-docker-card.js` | Add `spec_id: 'docker'` to STATIC; dual-dispatch `sp-cli:spec:docker.launch-requested` + `sp-cli:plugin:docker.launch-requested` (DEPRECATED) |
| `plugins/podman/…sp-cli-podman-card.js` | Same pattern for `podman` |
| `plugins/elastic/…sp-cli-elastic-card.js` | Same for `elastic` |
| `plugins/vnc/…sp-cli-vnc-card.js` | Same for `vnc` |
| `plugins/prometheus/…sp-cli-prometheus-card.js` | Same for `prometheus` |
| `plugins/opensearch/…sp-cli-opensearch-card.js` | Same for `opensearch` |
| `plugins/neko/…sp-cli-neko-card.js` | Same for `neko` |
| `plugins/firefox/…sp-cli-firefox-card.js` | Same for `firefox` |
| `admin/admin.js` | `sp-cli:catalogue.loaded` listener now wires both `sp-cli:spec:{id}.launch-requested` (primary) and `sp-cli:plugin:{id}.launch-requested` (DEPRECATED) |
| `sp-cli-events-log.js` | FAMILIES `launch` extended with `sp-cli:spec:*.launch-requested` + `sp-cli:catalogue.loaded`; FAMILIES `nav` extended with `sp-cli:spec.toggled` |

---

## Acceptance criteria verification

- All per-spec card emitters dispatch `sp-cli:spec:<name>.launch-requested` ✅
- Deprecated `sp-cli:plugin:<name>.launch-requested` still dispatched alongside ✅
- `admin.js` wires both forms from catalogue ✅
- FAMILIES map enumerates both forms ✅
- `library/docs/specs/v0.2.0__ui-event-vocabulary.md` exists with complete table ✅
- `spec_id` field present in all 8 card STATIC objects ✅

---

## Failures

None.

## Scope notes

- `sp-cli:spec.toggled` is RESERVED in the vocabulary spec but not yet wired — settings-bus still dispatches `sp-cli:plugin.toggled`. Full migration deferred to when settings-bus is refactored (v0.3.0).
- Removal of deprecated forms is targeted for v0.3.0 per brief recommendation.

---

## Next phase

FV2.10 — A11y pass (ARIA tablist on nodes-view, icon button labels, focus styles). Unblocked.
