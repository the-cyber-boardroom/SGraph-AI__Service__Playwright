# Debrief — T3.1–T3.5 FE integration cleanup

**Date:** 2026-05-05
**Status:** PARTIAL (T3.3 rename blocked — see below)

---

## T3.1 — user/user.js API migration

`/catalog/types` → `/api/specs`, `/catalog/stacks` → `/api/nodes`. Response extraction updated: `catalogResp?.entries` → `catalogResp?.specs`, `stacksResp?.stacks` → `nodesResp?.nodes`. Dead code removed: `MODAL_TAG`, `_openModal()`, `sp-cli:user-launch` listener, `sg-compute-launch-modal` DOM element and three missing-component script tags from `user/index.html`.

Zero `/catalog/` hits remaining in `sgraph_ai_service_playwright__api_site/user/`.

---

## T3.2 — Collapse dual launch flows

Fixed `sg-compute-launch-panel._launch()` to POST to `/api/nodes` with:
- `spec_id: this._entry.spec_id`
- `node_name: values.node_name`

(was: `entry.create_endpoint_path` with `stack_name`). Updated `sg-compute-launch-form.getValues()` to return `node_name` (was `stack_name`). Updated `populate()` to read `entry.spec_id` (was `entry.type_id`).

The compute-view inline form was already canonical (`/api/nodes`, `spec_id`, `node_name`) — no change needed there. Both launch surfaces now use the same API shape. Full unification of the two surfaces (compute-view inline vs launch-panel tab) is a UX decision for a later pass.

---

## T3.3 — Cosmetic rename leftovers (PARTIAL)

Done:
- Three dead `<script>` tags removed from `user/index.html` (`sg-compute-vault-activity`, `sg-compute-launch-modal`, `sg-compute-stack-detail` — all non-existent on disk).
- `sg-compute-launch-modal` DOM element removed from `user/index.html`.
- `library/docs/specs/v0.2.0__ui-event-vocabulary.md` updated: `sp-cli-stacks-pane.js` reference removed; emitter names updated to current (`sg-compute-*`).

**STOP AND SURFACE — directory rename `components/sp-cli/` → `components/sg-compute/`:**

8 files in `sg_compute_specs/*/ui/detail/` use absolute imports `/ui/components/sp-cli/_shared/...`. These are outside the `api_site/` tree and will break if the directory is renamed without a coordinated update. The rename requires touching both trees simultaneously. Surfaced to Architect — see decision below.

---

## T3.4 — Centralise instance types / cost table

Created `shared/launch-defaults.js` with frozen exports: `REGIONS`, `INSTANCE_TYPES`, `MAX_HOURS`, `COST_TABLE`. Both consumers updated:
- `sg-compute-compute-view.js`: imports all four; local COST_TABLE deleted.
- `sg-compute-launch-form.js`: imports REGIONS, INSTANCE_TYPES, MAX_HOURS; local copies deleted.

---

## T3.5 — Pin field-name shape (drop `||` fallbacks)

Dropped in `admin.js`:
- `response?.node_id || response?.stack_info?.stack_name || response?.stack_name` → `response?.node_id`
- `stack.node_id || stack.stack_name` → `stack.node_id`
- `stack.spec_id || stack.type_id` → `stack.spec_id`
- `entry?.spec_id || entry?.type_id` → `entry?.spec_id`
- `_hostApiKeys[s.node_id || s.stack_name]` → `_hostApiKeys[s.node_id]`

Dropped in `sg-compute-nodes-view.js`:
- `c.pod_name || c.name` → `c.pod_name`
- `c.state || c.status` → `c.state`

---

## T3.3 stop-and-surface — decision required

**Question for Architect:** The `components/sp-cli/` → `components/sg-compute/` rename requires updating 8 absolute import paths in `sg_compute_specs/*/ui/detail/*.js`. Should this rename:

a) Proceed now as a coordinated two-tree change in this PR?
b) Be deferred to a dedicated rename PR that sweeps both trees?
c) Be done differently (e.g., add a path alias at the web server level)?

Current recommendation: option (b) — keep this PR focused on API cleanup; do the rename as a dedicated PR that can be reviewed and tested in isolation.

---

## Failures

None — no mocks, no patches.
