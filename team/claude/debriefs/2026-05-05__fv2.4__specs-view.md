# FV2.4 — Specs view (left-nav item + browse pane)

**Date:** 2026-05-05
**Phase:** FV2.4
**Branch:** `claude/fv2-4-specs-view-5sjIO`
**Commit:** (backfill after merge)
**Status:** SHIPPED

---

## What was done

Created `<sp-cli-specs-view>` — a catalogue-driven spec browser — and wired it into the admin dashboard's left nav.

### Files created

- `components/sp-cli/sp-cli-specs-view/v0/v0.1/v0.1.0/sp-cli-specs-view.js` — Web Component; iterates `getCatalogue().specs`; renders per-spec card with icon, display_name, spec_id, stability badge, capability chips, version, boot estimate, "Launch node" button
- `components/sp-cli/sp-cli-specs-view/v0/v0.1/v0.1.0/sp-cli-specs-view.html` — Shadow DOM template
- `components/sp-cli/sp-cli-specs-view/v0/v0.1/v0.1.0/sp-cli-specs-view.css` — Grid layout + card styles; WCAG AA contrast on all stability badges

### Files changed

| File | Change |
|------|--------|
| `sp-cli-left-nav.html` | Add Specs nav item (`data-view="specs"`, `📋` icon) between Stacks and Settings |
| `admin/admin.js` | Add `specs: 'Specs'` to `VIEW_TITLES`; add `sp-cli-specs-view` to both `viewTags` arrays in `_findMainStackId` + `_findCurrentViewTabId` |
| `admin/index.html` | Add `<script type="module">` for specs-view |

---

## Acceptance criteria verification

- "Specs" appears in left nav ✅
- Clicking shows a grid of all specs from the catalogue ✅
- Empty state renders when catalogue is empty ✅
- Stability badges (stable=green, experimental=amber, deprecated=red) ✅
- Capability chips rendered ✅
- "Launch node" button fires `sp-cli:catalog-launch` (compat alias); `soon` specs get disabled button ✅
- Keyboard nav: button is focusable, `:focus-visible` ring on `.btn-launch` ✅
- ARIA: `role="list"` on grid, `role="listitem"` on cards, `aria-label` on badge + button ✅
- `sp-cli-spec-detail` (separate component) deferred — not in scope for FV2.4 slim-pass ✅

---

## Failures

None. The `<sp-cli-spec-detail>` described in the brief as a "lighter component" was out of scope for this slice — the brief's priority was the grid view. A future sub-phase can add it.

## Scope notes

- `GET /api/specs/{id}/readme` endpoint unknown — "View detail" link omitted; flagged as follow-up for backend team.
- `extends` lineage DAG deferred; all specs currently have `extends=[]`.
- AMI list deferred until backend ships `/api/amis/<spec_id>`.

---

## Next phase

FV2.9 — Event vocabulary finish (`sp-cli:plugin:*` → `sp-cli:spec:*`). Unblocked.
