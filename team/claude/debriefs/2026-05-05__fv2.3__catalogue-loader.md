# FV2.3 — `shared/spec-catalogue.js` + delete hardcoded plugin lists

**Date:** 2026-05-05
**Phase:** FV2.3
**Branch:** `claude/fv2-3-catalogue-loader-5sjIO`
**Commit:** (backfill after merge)
**Status:** SHIPPED

---

## What was done

Replaced 4 hardcoded spec lists with a single server-driven catalogue module.

### Files created

- `shared/spec-catalogue.js` — `loadCatalogue()`, `getCatalogue()`, `getSpec(spec_id)`;
  caches `/api/specs` for the page lifetime; dispatches `sp-cli:catalogue.loaded`

### Files changed

| File | Change |
|------|--------|
| `admin/admin.js` | Import `loadCatalogue`; call on boot; replace `LAUNCH_TYPES` forEach with catalogue-driven `sp-cli:catalogue.loaded` listener that wires per-spec launch events dynamically |
| `sp-cli-compute-view.js` | Delete `CATALOG`; import `getCatalogue`; `_renderGroups()` reads live catalogue; fields: `type_id→spec_id`, `group→nav_group`, `boot (string)→boot_seconds_typical (int)` via `_fmtBoot()`; listen for `sp-cli:catalogue.loaded` to re-render |
| `sp-cli-launcher-pane.js` | Delete `PLUGIN_ORDER`; import `getCatalogue`; `_renderCards()` iterates `getCatalogue().specs` filtered by plugin toggles; listen for `sp-cli:catalogue.loaded` |
| `shared/settings-bus.js` | `DEFAULTS.plugins = {}` (was 8-entry hardcode); add `sp-cli:catalogue.loaded` listener that populates missing spec toggles defaulting to `enabled: true` |
| `admin/index.html` | Add comment on plugin card / detail script tags: catalogue drives listing, but tags still needed until FV2.6 |

### Files deleted

- `shared/catalog.js` — legacy `/catalog/types` cache; no active imports

---

## Acceptance criteria verification

- `shared/spec-catalogue.js` exists; `loadCatalogue()` called on dashboard boot ✅
- `sp-cli:catalogue.loaded` event dispatched; observable in events log ✅
- `PLUGIN_ORDER` deleted ✅; `CATALOG` deleted ✅; `LAUNCH_TYPES` deleted ✅
- `DEFAULTS.plugins` reduced to `{}` ✅; populated dynamically from catalogue ✅
- `shared/catalog.js` deleted; no remaining imports ✅
- All specs in catalogue render cards (settings-bus defaults new specs to enabled) ✅

---

## Failures

None.

## Scope notes

- `spec-catalogue.js` calls `/api/specs` directly (not behind `getUseLegacyApi()`).
  The legacy fallback in `admin.js._loadData()` is for the node list; the catalogue
  itself is always fetched from the new API.
- `_fmtBoot(0)` returns `'—'` for specs with no boot estimate (e.g. neko `soon: true`).
- `sp-cli-launcher-pane` falls through (`getCatalogue()` throws) until catalogue loads;
  `_renderCards()` returns early with empty grid; `sp-cli:catalogue.loaded` triggers
  a second render once the data arrives.

---

## Next phase

FV2.4 — `<sp-cli-specs-view>` web component; add Specs left-nav item. Unblocked.
