# Debriefs — Index

Chronological list of implementation debriefs. Most recent first.

| Date       | Commit(s)                  | Slice                                           | File |
|------------|----------------------------|-------------------------------------------------|------|
| 2026-05-17 | `a8c272c` `da82018` `1e78cf1` `6195332` `432ba5d` | **vault-publish spec v0.2.23 — full cold path (P1b→P2d).** Five commits: slug registry + Vault_Publish__Service + CLI (P1b); CloudFront primitive `sg aws cf` with 36 tests (P2a); Lambda deploy + URL CRUD `sg aws lambda` with 16 tests (P2b); Waker Lambda — Slug__From_Host, Endpoint__Resolver, Warming__Page, Endpoint__Proxy, Waker__Handler, Fast_API__Waker, lambda_entry.py with 31 tests (P2c); bootstrap command + Schema__Vault_Publish__Bootstrap_{Request,Response} + 3 factory seams on Vault_Publish__Service with 24 tests (P2d). Total: 149 vault-publish tests + 52 CF/Lambda tests — all passing. Three GOOD failures caught by tests (Type_Safe `list` annotation rejection ×2, fake ID collision). No BAD failures. **Branch `claude/review-vault-publish-spec-FT9hq` — NOT YET MERGED to dev.** | `2026-05-17__vault-publish-spec-v0.2.23.md` |
| 2026-05-16 | `e001bc05` `2d5480a9` `835918a1` `3265b684` `8fb7e41b` `ebcd73ef` | **`sg aws billing` — AWS Cost Explorer CLI sub-package.** Six commands (`last-48h`, `week`, `mtd`, `window`, `summary`, `chart`) surfacing AWS Cost Explorer spend data with Rich terminal output, emoji service labels, and eighth-cell-precision ASCII bar charts. Follows the exact pattern of `sg aws dns`. **Four GOOD failures, zero BAD failures — all caught by live testing against a real AWS account (745506449035).** (1) **IAM error conflation** — the first live run returned the "Cost Explorer not enabled" message when CE was clearly enabled. Root cause: `AccessDeniedException` (IAM missing `ce:GetCostAndUsage`) and `DataUnavailableException` (CE truly disabled) were in the same error handler. Split into two distinct blocks with tailored remediation text. (2) **Negative amounts from credits** — `ValueError: Safe_Decimal__Currency__USD must be >= 0.0` on a real account with credits. Fix: `min_value=None` in `Safe_Decimal__Currency__USD` + `RECORD_TYPE=Usage` filter as default (matching CE console), `--all-charges` to opt in. (3) **Service name mangling** — CE service names showed as `Amazon_Elastic_Compute_Cloud___Compute` (underscores). Root cause: `Safe_Str` default regex strips non-alphanumerics to `_`. Fixed with `Safe_Str__Aws_Service_Code` carrying an explicit permissive regex `r'[^A-Za-z0-9 \-().,/&_]'`. (4) **Emoji column misalignment** — two rounds: first split emoji+name into separate Rich columns (min/max width=2); second, replaced `λ` (U+03BB, 1-cell Greek letter) and `🖧` (U+1F5A7, lacks Emoji_Presentation, 1-cell) with `🟧` and `🧰` (both Emoji_Presentation, reliably 2-cell). Rule established: only Emoji_Presentation glyphs in width-constrained columns. **Key architectural choices**: `Cost_Explorer__AWS__Client` is the sole boto3 boundary (`client()` is the test seam); `NextPageToken` pagination implemented manually (CE does not support standard boto3 paginators); `all_charges: bool = False` on `Billing__Report__Builder.build()` controls `record_types` filter. 136 unit tests passing on branch `claude/plan-billing-view-u0NFG`. | `2026-05-16__aws-billing-cli.md` |
| 2026-05-15 | `813bf93` `ee3add2` *(reverted)* | **Sandbox-egress HTTPS block — root cause confirmed, workaround shipped + reverted, end-to-end `sgit push` from Claude still ❌ blocked.** The 🔴 red callout from the `--with-aws-dns` debrief, picked up. **Two-vantage diff** nailed the failure mode in minutes: from the open internet (Perplexity audit) `https://fast-hopper.sg-compute.sgraph.ai/` serves a real LE cert (R12 / ISRG Root X1) and returns 401 from uvicorn; from inside a Claude sandbox the same URL returns Envoy's `upstream connect error or disconnect/reset before headers` 503 with a **forged** cert `O=Anthropic, CN=sandbox-egress-production TLS Inspection CA`. The egress proxy intercepts TLS, generates a leaf inline, then fails to dial upstream. **Root-cause model: pre-warmed FQDN pool.** Wildcard `*.sgraph.ai` passes the allowlist string check (else we'd see `403 host_not_allowed`), but the egress maintains a separate pool of FQDNs with pre-resolved DNS + pre-negotiated upstream TLS state, and `*.sg-compute.sgraph.ai` is not in it. Confirmed by: same EC2 IP serves three sgraph.ai subdomains, all fail HTTPS-from-Claude identically; `dev.send.sgraph.ai` (long-standing default sgit server, almost certainly pre-warmed) is the one HTTPS host that works; HTTP on `:80` works for everything (no interception path). **HTTPS isn't the only Claude-reachable path** — confirmed `curl http://fast-hopper.sg-compute.sgraph.ai/health/info` returns the sg-playwright capabilities JSON in ~200ms from inside the sandbox; the prior slice's port-80 Playwright path already works, this debrief is about whether the *vault* should expose a plain-HTTP path too. **Workaround shipped (`813bf93`, ~50 LOC + 1 test):** `--no-with-tls-check --with-aws-dns` made vault bind plain HTTP on `:8080`, opened `:8080` to `0.0.0.0/0` (was caller-/32 only), broadened `StackTlsHostname` tag's semantics from "cert SAN" to "stack FQDN", mapper produces `http://<fqdn>:8080` URL. 118 passed (was 117). **Reverted (`ee3add2`)** after the operator (correctly) decided the trade-off was wrong for the *default* surface: vault is for operators (laptops + CI), where HTTPS works fine; burning a default-flag combination on "reachable from the one environment that MITM's HTTPS" makes the surface noisier for the 99% case; workarounds calcify and discourage the real fix (egress-side FQDN pool). Verified the revert is byte-for-byte against pre-change state — `git diff 813bf93^ ee3add2 -- <three files>` returned empty; 117 tests passing on dev. **Three 🟢 GOOD failures:** (a) capturing the cert the failing client *sees* (not the cert the origin claims to serve) made the MITM diagnosis unambiguous in one cycle — a technique to keep; (b) 503-vs-403 distinction falsified the wildcard-allowlist hypothesis without needing Anthropic's config — Envoy's response taxonomy did the diagnosis; (c) cheap-to-reverse: 50 LOC + 1 test meant try-and-back-off beat debate-then-not-ship. **Two 🟡 YELLOW failures:** (a) flag *implication* over explicit flag — re-using `--no-with-tls-check + --with-aws-dns` as "plain HTTP world-open" overloaded names that don't telegraph that intent; if the workaround comes back, ship as explicit `--vault-plain-http`; (b) tag-semantic broadening (StackTlsHostname → stack FQDN) got smuggled into the same commit as the SG change, so the revert had to take both — should have been split. **One 🔴 RED failure:** the user-facing `sgit push` from Claude to `fast-hopper` vault **still doesn't work**. The fix lives on Anthropic's side (add `*.sg-compute.sgraph.ai` to the pre-warmed FQDN pool). Until that lands, "Claude builds a vault" demos die at the first push. **Next thread (priority order):** (1) file cross-team brief to Anthropic egress citing both vantage captures + the 503-vs-403 evidence; (2) if (1) takes weeks, re-ship the workaround as an explicit `--vault-plain-http` flag; (3) verify sgit's `--base-url` accepts `http://` (it does — no sgit changes needed); (4) follow up on Perplexity's all-65535-TCP-ports-open finding (SG audit of the EC2 hopper-listener accept-and-hang); (5) operator-docs sandbox-egress matrix (open-internet vs Claude vs corporate WAF × `:80`/`:443`/SSM-forward). **Source captures filed** under `library/docs/research/v0.2.21__sandbox-egress-https-block__{perplexity-network-audit,claude-agent-debrief}.md`. Builds on `2026-05-15__vault-app-fqdn-https-end-to-end.md` (the slice that exposed the gap) and `2026-05-15__vault-app-tls-letsencrypt-ip.md` (LE-for-IP substrate). | `2026-05-15__sandbox-egress-https-block-investigation.md` |
| 2026-05-16 | `51eeb4d` `ae58264` `c5c475b` `10cdc62` `bb6a724` `7c636f6` `22b621b` `90ccde2` | **`sg repl` — generic interactive REPL shell for any Typer CLI** — 80-line wrapper that turns any Typer app into a navigable shell. Adds `sg repl` to the `sg` CLI; also: commands sorted alphabetically, help text shortened, `sg nodes` sub-group with `delete-all`. Key pattern: `run_repl(sg_app)` discovers sections from the live Click tree (`typer.main.get_command(app).commands`), dispatches everything as `sg_app(path + args)`, and adds zero parallel logic. Features: filesystem-style `path` list (`sg/aws/billing>`), prefix matching at every level (`pl` → `playwright`), multi-word execute without navigating (`billing chart` runs the command, stays at `sg/aws>`), arbitrary depth via `_resolve()`. `help`/`?`/`h` delegate to the real `sg [section] --help` output. Fully reusable — pass any Typer app to `run_repl()`. Branch `claude/fix-sg-compute-cli-nZaW6` **unmerged**. No new tests. Reality doc not yet updated. | `2026-05-16__sg-repl-cli-pattern.md` |
| 2026-05-15 | `ae725c2` `5caaed6` `1a08c24` `2562c35` `5d34168` | **`sp vault-app create --with-aws-dns`: one command, browser-trusted FQDN — the "interesting capability → sellable service" threshold.** Five commits on `claude/architect-qa-service-ijc61` collapsed the deploy story from a multi-step manual dance (launch, find IP, update DNS, wait for propagation, accept cert warning) into a single command that produces a stack with a real FQDN, a browser-trusted LE cert for that FQDN, the vault on standard `:443`, Playwright on standard `:80`, and a single `X-API-Key` for both. (1) **Port 80 for Playwright** (`ae725c2`) — moved from `:11024` (blocked by sandbox-egress proxies) to `:80`, with `depends_on: cert-init: required: false` to share the port cleanly with the ACME http-01 challenge. (2) **`--tls-hostname` plumbing** (`5caaed6`) — `Cert__ACME__Client.build_csr` switches SAN type by keyword (`ipaddrs=` vs `domains=`), `config(for_hostname=True)` clears the IP-only `shortlived` profile, `cert_init.py` gains a `letsencrypt-hostname` mode + `SG__CERT_INIT__TLS_HOSTNAME` env, mapper's new `StackTlsHostname` tag drives `vault_url` + `playwright_url` to use the FQDN. Enum rename `LETSENCRYPT_DNS` → `LETSENCRYPT_HOSTNAME` to disambiguate from dns-01 (challenge stays http-01). (3) **cert-init DNS-wait backstop** (`1a08c24`) — `wait_for_dns_to_match(hostname, my_ip, ...)` polls `socket.gethostbyname` until convergence, catches `gaierror`/`OSError`/`UnicodeError` so a single bad lookup never crashes the loop, configurable via `SG__CERT_INIT__DNS_WAIT_TIMEOUT_SEC` (default 900s). Plumbed through the compose template. (4) **`Vault_App__Auto_DNS` helper** (`2562c35`) — pure synchronous wrapper around the dev-branch Route 53 surface (`Route53__AWS__Client`, `Route53__Zone__Resolver`, `Route53__Authoritative__Checker`). **Never raises** — exceptions captured into `Schema__Vault_App__Auto_DNS__Result.error` so the calling thread surfaces them after `--wait` returns. Factory seams (`_aws_client_factory`, etc.) for in-memory test composition; **no new boto3 in this repo**, every AWS call goes through dev-branch service classes. (5) **`--with-aws-dns` flag with parallel-thread launch** (`5d34168`) — service derives `tls_hostname = <stack_name>.<SG_AWS__DNS__DEFAULT_ZONE>` (fallback `sg-compute.sgraph.ai`) when blank, auto-bumps `tls_mode` to `letsencrypt-hostname`. New generic `Schema__Spec__CLI__Spec.post_launch_fn` seam called by `Spec__CLI__Builder.create_impl` between `create_stack` and `_wait_healthy`; vault-app's hook spawns a daemon thread that polls for IP (10s typical, 60s ceiling) then runs `Vault_App__Auto_DNS.run`. Builder joins after `_wait_healthy` (3-min ceiling). No `SG_AWS__DNS__ALLOW_MUTATIONS=1` gate — the flag IS the consent. **Verified live** against AWS + Let's Encrypt prod: `fast-hopper.sg-compute.sgraph.ai`, R12/Let's Encrypt cert valid 15 May → 13 Aug 2026 (default 90-day profile), Chrome clean padlock. **123 tests passing** (was 84 pre-session). **Critical pivot mid-session**: the catch-22 ("need IP for DNS, need DNS for cert") was resolved not by building DNS-wait timers but by **pulling latest dev and discovering the `sg aws dns` surface had landed** — wrapping what existed instead of writing it again. **Three GOOD failures**: (a) CORS error in the Workbench led to the right diagnosis (the fetch was hitting vault, not Playwright; vault's CORS belongs to the vault team) rather than a band-aid here; (b) factory seams over monkeypatching — 6 Auto_DNS tests landed clean, refactors don't ripple through mock.patch calls; (c) `Vault_App__Auto_DNS` never raises — background threads that raise out are debugging horror, capturing into `result.error` keeps the surface flat. **Four BAD failures** (all yellow except one red): (a) **🔴 Claude egress 503 to the deployed stack** — browser reaches `fast-hopper.sg-compute.sgraph.ai` fine, Anthropic's egress proxy returns `upstream connect error or disconnect/reset before headers`; this is independent of the work but means the "Claude-reachable" promise isn't quite delivered end-to-end yet, **next thread to pull**; (b) recreate doesn't preserve `tls_hostname` / `with_aws_dns` (small follow-up); (c) post-launch hook stdout interleaves with `_wait_healthy` rendering (cosmetic); (d) daemon thread on Ctrl-C could leave an in-flight Route 53 change (recovery via `sg aws dns records check`). **Why this matters — product, not engineering:** the FQDN + browser-trusted cert + standard ports turn SG/Compute stacks from a developer convenience (IP literals + cert warnings) into a deployable service with the UX of Vercel / Render preview deploys. The use cases that follow (QA, scraping, agent workbenches, traffic simulation) become products in their own right; this slice made the substrate ready to *host* them. Cross-references: builds on `2026-05-15__vault-app-tls-letsencrypt-ip.md` (LE-for-IP substrate) and the dev-branch `sg aws dns` work (`Cli__Dns`, `Route53__AWS__Client`, `wait_for_change`, `Route53__Authoritative__Checker`). | `2026-05-15__vault-app-fqdn-https-end-to-end.md` |
| 2026-05-15 | `e4de7848`+`652bbabe`+`685427d6`+`65a85fba`+`820fb96b`+`de9cf2e7` | **`sg aws dns` post-P1.5 ergonomics + zone health + purge** — Smart `records add` positional dispatch (auto-resolve IP from latest / named instance); default zone moved to `sg-compute.sgraph.ai` (env-overridable); env-gate dropped from `add` (confirm-only); inline INSYNC progress + per-phase timings; `r`/`z`/`i` sub-typer aliases; `@spec_cli_errors` wired across all 11 commands; CLI restructure to `zone` (singular) vs `zones` (plural); new `zone check` (orphaned / stale / unmatched / ignored classification) and `zone purge` (batch DELETE in one ChangeBatch); alias-record support for CloudFront/ELB/etc.; ACM cross-reference for validation CNAMEs (cert deleted but record left behind → purge candidate); pattern-detect DKIM / SES / DMARC / SPF / autodiscover / Let's Encrypt challenges. 136/136 passing. | `2026-05-15__sg-aws-dns-ergonomics-and-zone-health.md` |
| 2026-05-15 | `9bc58d83` | **`sg aws dns records check` P1.5 (opt-in cache-polluting modes)** — `--public-resolvers` (8-resolver fan-out across Cloudflare/Google/Quad9/OpenDNS/AdGuard-EU), `--local` (`dig` against host default resolver), `--all`. Three verbatim WARNING banners (signed off in brief rev 4) printed before `[y/N]` confirms. `--yes` skips. `--min-resolvers N` quorum (default 5/8). Exit-code priority: 0 / 1 auth-fail / 2 public-quorum-fail / 3 local-mismatch. New `Route53__Local__Checker`; orchestrator + public-resolver checker refactored to support full / subset modes. `Dig__Runner` handles empty `nameserver=''` for the local case. 112/112 passing. | `2026-05-15__sg-aws-dns-p1.5-cache-polluting-modes.md` |
| 2026-05-15 | `8ec54817`+`b25b5b89`+`a52b392b`+`db1b7148` | **`sg aws dns` P1 (mutations + dig-verify + instance create-record)** — `Route53__AWS__Client` gains `create_record` / `upsert_record` / `delete_record` / `upsert_a_alias_record` + `_change_rrset` helper. CLI `records add` / `update` (diff preview) / `delete` (confirm). `Dig__Runner` (`subprocess` wrapper, `LC_ALL=C`, no `dnspython` dep). `Route53__Authoritative__Checker` queries Route 53's own NS via `dig @ns +norecurse` (zero cache pollution — the default mode). `Route53__Public_Resolver__Checker` 6-resolver EU+US for the smart-verify NEW_NAME path. `Route53__Check__Orchestrator` + `Route53__Smart_Verify` (pre-flight `get_record` → NEW_NAME / UPSERT / DELETE → verbatim info lines). `Route53__Zone__Resolver` walks FQDN labels deepest-first (two bug fixes in `b25b5b89` + `a52b392b` — child-zone delegation + apex-of-child-zone). `Route53__Instance__Linker` resolves i-id / Name tag / `--latest` via boto3 EC2. CLI `records check` (authoritative default) + `dns instance create-record` (idempotent + cert-warning). `--wait` flag polls `route53:GetChange` to INSYNC. 128/128 passing. | `2026-05-15__sg-aws-dns-p1-mutations-and-verify.md` |
| 2026-05-15 | `4f3fc81b`+`8a4ad6d9` | **`sg aws dns` + `sg aws acm` P0 (read-only)** — 39 new files under `sgraph_ai_service_playwright__cli/aws/{dns,acm,cli}/`. `Route53__AWS__Client` + `ACM__AWS__Client` (raw boto3 with documented exception headers — matches `Elastic__AWS__Client` precedent). Schemas, enums (incl. `Enum__Route53__Record_Type` with wildcard support), primitives (`Safe_Str__Hosted_Zone_Id`, multi-label-permissive `Safe_Str__Record_Name`, `Safe_Int__TTL`). CLI: `dns zones list/show`, `dns records list/get`, `acm list/show`. `--json` everywhere. Dual-region default for `acm list` (current + us-east-1) because CloudFront cert attachment requires us-east-1. Wired into `sg_compute/cli/Cli__SG.py` (caught the `provision_ec2.py` → `Cli__SG.py` entry-point move mid-flight). 52/52 passing. | `2026-05-15__sg-aws-dns-p0-read-only.md` |
| 2026-05-15 | `d33483f`+`9b699a2`+`e4c9140`+`16e4cab` | **`diniscruz/sg-playwright` runtime `ModuleNotFoundError` fixed** — Pinned `playwright==1.58.0` in `requirements.txt` to match the `mcr.microsoft.com/playwright/python:v1.58.0-noble` base image, replacing the misleading "intentionally omitted" comment. Added a build-time guard that fails the Dockerfile build if the module + version + interpreter don't all line up. Verified end-to-end: fresh `docker run` of the rebuilt image serves `/screenshot/simple` in ~1.7s with no in-container `pip install` ritual. Lockstep contract documented in both files. Three follow-up commits chased the guard's own bugs (wrong version API, missing `packaging` module). | `v0.2.11__playwright-image-pin-fix.md` |
| 2026-05-15 | *(backfill)*               | **`sg` entry point moved off `provision_ec2.py` (stage 1)** — New `sg_compute/cli/Cli__SG.py` aggregator routes all 15 sub-CLIs (playwright via the new spec; others unchanged). `pyproject.toml` console scripts (`sg`/`sgc`/`sg-compute`/`sp`) repointed. Tracking doc captures every retired inline `pw_app` command + the downstream coupling that blocks stage-2 deletion. `provision_ec2.py` is still on disk because `Ec2__Service` lazy-imports `provision()` + 5 tag constants. | `v0.2.11__provision_ec2-legacy-snapshot.md` (in `library/reference/`) |
| 2026-05-15 | *(backfill)*               | **`sg playwright` rebuilt as a proper sg_compute spec** — Rebuilt the playwright spec under `sg_compute_specs/playwright/` matching the `vault_app`/`local_claude` convention (`Spec__Service__Base` + `Spec__CLI__Builder`, shared `sg_compute` EC2 helpers). 3-container compose: host-plane + sg-playwright + agent-mitmproxy (`--with-mitmproxy`). Retired the `sgraph_ai_service_playwright__cli/playwright/` cli-plugin (44 files deleted, dropped from `PLUGIN_FOLDERS`). 30 new spec tests + 1601 CLI tests green. `provision_ec2.py` wiring left as an explicit decision (lossy 1400-line swap). | `v0.2.11__sp-playwright-spec-proper.md` |
| 2026-05-14 | *(backfill)*               | **`sp playwright` EC2+compose model rework** — Corrected the POD-backend implementation (7b32a17) to the correct EC2+compose model. Cloned the vnc spec pattern: 11 new service helpers (AWS client, SG/AMI/Instance/Launch/Tags, Mapper, IP detector, HTTP probe, compose template, user-data builder), 6 schemas rewritten with EC2 fields, routes updated to `region` query param, Renderers updated. 1670 unit tests pass. | `v0.2.11__sp-playwright-ec2-model.md` |
| 2026-05-15 | `27d5647` `288294c` `1f5ce79` `a0944a5` `8aad135` `a37b1cc` `8d2d8a8` *(+ this commit)* | **vault-app TLS, end to end — real Let's Encrypt IP cert by default.** Six rounds: architect spec → P0 PoC (TLS library + §8.2 launch contract + slim `Fast_API__TLS` app + `cert` CLI) → cross-team brief → Vault Dev team ships `diniscruz/sg-send-vault:latest` honouring the contract natively → EC2 integration + `Cert__ACME__Client` (http-01, IP cert via `acme` lib's `new_order(csr, profile='shortlived')` + `make_csr(ipaddrs=[...])`) + cert/sidecar progress in `wait`/`health` → bad-failure round-trip on the host image deps → staging-then-prod LE live → defaults flipped + access-token tag + HTTPS `vault_url`. **Outcome:** `sp vault-app create --wait` produces a browser-trusted Let's Encrypt cert on a fresh `t3.medium`, vault UI fully decrypting in-browser at `https://<ip>/`, **clean padlock, zero warning, `window.isSecureContext === true`, Web Crypto API live**. The bleeding-edge bit — ACME-for-IP via the `shortlived` profile (GA Jan 2026) — is verified end-to-end on production Let's Encrypt. **Three callouts.** (1) **GOOD failure:** `tls-alpn-01` rejected in favour of `http-01` despite the architect doc preferring the former — `tls-alpn-01` is fundamentally un-verifiable without live LE; `http-01` is a ~20-line file server. Deliberate documented deviation. (2) **GOOD failure (paid off immediately):** `wait`/`health` cert-init container probe surfaced `cert-init=Exited (1) 2 seconds ago` the moment the first prod attempt failed — diagnosis took zero detective work. (3) **BAD failure:** host image's separate `docker/host-control/requirements.txt` — root-`requirements.txt`-only `cryptography` add missed the actual image's dep tree; `cert-init` died at `import cryptography` in production. Twice. Caught by the progress visibility (above), fixed in `a37b1cc`. **Lesson baked in:** audit every requirements file when adding image-side deps. Also: `vault_url` hard-coded `http://...:8080` after TLS wiring (yellow) — fixed in the final-mile commit. **No proxy anywhere in the fleet** is now load-bearing: every FastAPI app reads the §8.2 `FAST_API__TLS__*` env contract, `Fast_API__TLS__Launcher` is single-file ready to lift into OSBot__Fast_API. **Two cross-team briefs filed:** `v0.2.6__sg-send-vault-tls-contract.md` (delivered), `v0.2.6__sgit-cli-two-auth-headers.md` (the new HTTPS URL + two auth headers, same value: `X-API-Key` + `x-sgraph-access-token`). Defaults flipped: `with_tls_check=True`, `tls_mode='letsencrypt-ip'`, `acme_prod=True`. Tags added: `StackTLS`, `AccessToken`; `sp vault-app info` shows the token + the right scheme. `sp vault-app logs -s {cert-init, vault} [-f]` for live log tailing. **Branch `claude/architect-qa-service-ijc61`; merged onto `dev` as `ded6dc2`.** | `2026-05-15__vault-app-tls-letsencrypt-ip.md` |
| 2026-05-12 | `1e692ce` `41a4bd1` `34a6a49` `0af2990` `fd7ace0` | **local-claude Mac container — laptop twin of `sg lc`** — New `docker/local-claude/` folder ships a two-service compose stack (`litellm` + `local-claude`) that runs Claude Code CLI inside Docker on macOS, pointed at host Ollama for Metal/GPU inference. Mirrors the EC2 firstboot launcher pattern (`Section__Claude_Code__Firstboot.py:74-97`). **Three challenges hit, all documented in the debrief.** (1) **UID 1000 collision** in `node:22-bookworm-slim` (`node` user already at 1000) — fixed with `userdel -r node` before `useradd claude`. BAD failure — should have been caught at first build. (2) **Ollama bound to loopback** — `127.0.0.1:11434` is unreachable from containers via `host.docker.internal`; fix is `launchctl setenv OLLAMA_HOST 0.0.0.0:11434` + Ollama.app restart. GOOD failure — matches the EC2 precedent in `Section__Ollama.py:9-16`. (3) **Anthropic vs OpenAI wire-format mismatch** — Claude Code POSTs `/v1/messages` (Anthropic) but Ollama's `/v1` is OpenAI-compat (`/v1/chat/completions`), producing a generic "model may not exist" error. Fixed by inserting **LiteLLM as a sidecar proxy** exposing `/v1/messages` and forwarding to `ollama_chat/<model>`. GOOD failure — clean root cause, standard industry pattern. **Default model:** `qwen2.5-coder:7b` (~5 GB, strong tool use on M5 24 GB), with 6 other host models registered for `/model` switching. **Out of scope (next slice):** writing a thin agent that talks to Ollama directly to avoid Claude Code overhead — user expressed interest, scoping sketch drafted in transcript. **Zero changes to `sg_compute*` / EC2 path.** Branch `claude/ollama-mac-container-14CDT` — pushed, not merged. | `2026-05-12__local-claude-mac-container.md` |
| 2026-05-11 | `950f182` `d4d20d7` `d83705a` `6f932c7` | **SG/Compute `sg lc extend` + sudoers fix + AMI cold-start brief + slim-AMI SPEC review** — New CLI verb `sg lc extend` cancels the existing `run-*.timer` via SSM, arms a fresh `systemd-run --on-active=Xs` shutdown timer, and updates the `TerminateAt` EC2 tag so `sg lc list` time-left stays accurate (`950f1821`). New `EC2__Instance__Helper.update_tags()` wraps boto3 `create_tags`. **Sudoers fix (`d4d20d71`):** `sg lc connect` started prompting for password because the SSM Agent writes `/etc/sudoers.d/ssm-agent-users` ONLY on the first interactive Session Manager session — and the 2026-05-10 fix that pre-creates `ssm-user` in `Section__Base` makes the agent skip it. Fix: `Section__Base` now writes the sudoers file explicitly; idempotent and heals existing AMIs on next boot (no re-bake required). **AMI cold-start diagnosis:** launches from the new fat AMI hang for minutes on `Loading safetensors checkpoint shards: 0%`. Root cause is **EBS lazy-load from snapshot** — new EBS volumes from a snapshot are empty until first read; first-touch fetches each block from S3 at ~50–100x EBS steady-state latency. Bug brief at `team/comms/briefs/v0.2.8__local-claude-ami-cold-start-perf.md`. **User-authored SPEC** at `team/humans/dinis_cruz/briefs/05/11/SPEC-slim-ami-s3-nvme.md` (501 lines) proposes a four-layer architecture: S3 artifact store + slim AMI (<8 GiB) + NVMe instance-store runtime cache + private-subnet network posture. Targets cold-start <2 min, bake <5 min. Reviewed and flagged 5 tightening points (fstab re-mkfs on stop/start, `default-runtime: nvidia` side-effects, schema field source-of-truth, SSM interface endpoints for private subnet, branch/PR boundary plan). **Open: benchmark step.** User asked for `ec2-boot-bench` numbers before zooming further into the plan. Pre-flight + YAML config + sweep matrix drafted in transcript. Load-bearing comparison: fat AMI (`ami-0b540a6e1c7b3297e`) vs AL2023 — determines whether lazy-load also delays the path-to-SSM-ready or is purely a model-load problem. **Branch merged to `dev` via `6f932c77`**; next session starts from `dev`. Two fat AMIs still alive; do not deregister until bench data is captured. | `2026-05-11__sg-compute-extend-and-ami-perf.md` |
| 2026-05-10 | `7ac4bca` `8b4333f` `a786a22` `545b061` `0730a5d` `1141a87` `abf14ea` | **🚀 local-claude end-to-end boot + first live Claude conversation on EC2** — Continues the prior session. Six more bugs squashed; the slice now reaches the live happy path. **Bug D (review):** `exit_code 0` coerced to `-1` via `value or default` falsy-zero — fixed with explicit `is None` check. **Bug E (masking → good):** every `sg lc logs` call returned empty because `TimeoutSeconds < 30` violates AWS SSM's hard minimum; boto3 raised `ParamValidationError` client-side and the outer try/except in `run_command` swallowed it to `('', -1)`. All SSM timeouts bumped to ≥30; `run_command` now prints `send_command` failures to stderr instead of vanishing. **Bug F (bad regression):** the prior `until id ssm-user` deadlock reappeared in `Section__SGit_Venv` and `Section__Claude_Code__Firstboot` — only the docker section had been fixed. Now `Section__Base` **pre-creates ssm-user** with `useradd -m -d /home/ssm-user`; blocking waits removed from both other sections. **Bug G (good):** `pip install sgit` → `pip install sgit-ai` (PyPI package renamed; binary still `sgit`). **Bug H (bad):** `claude-code-firstboot.service` had `systemctl enable` without `--now` — the unit was scheduled for next-boot but multi-user.target was already active, so it never ran. Fixed with `enable --now`. **Bug I (bad):** the unit also did `sudo touch /var/lib/claude-code-installed` while running as `User=ssm-user` — ssm-user has no sudo (SSM Agent's sudoers file is only created on first interactive Session). Unit redesigned to run as root with `su - ssm-user -c "<install>"`; marker written by root. **Bug J (bad):** `local-llm-claude.sh` had a bare `exec claude "$@"` — in non-login SSM sessions `~/.local/bin` is not in `$PATH`, so claude was "not found" even when installed. Launcher now prepends `$HOME/.local/bin:$HOME/.npm-global/bin:$HOME/bin`. Also: ssm-user **removed** from docker group (effective root via `docker run -v /:/host` not worth the convenience); `docker-access` diag check removed (8 checks now, not 9); `--dangerously-skip-permissions` baked into the launcher for headless private-instance UX. **Milestone:** Dinis ran `~/local-llm-claude.sh "write a Python function that reverses a string"` on `clever-bohr` and got a real Claude Code conversation — file created, executed, self-corrected from `python` to `python3`, and produced `!dlroW ,olleH`. Only manual step was the `curl … \| bash` claude install (Bug H was in flight when the instance was launched). **Theme:** every bug in this session is a silent missing side-effect — `or` swallowing zero, `try/except` swallowing param-validation errors, `enable` without `--now`, `sudo` without sudoers, `exec` without `$PATH`. The next-session gate is "ssh in and use the binary"; unit tests cannot catch this class. **Next:** bake AMI from a fresh stack carrying `1141a870`+`abf14ea9` and confirm zero manual steps. | `2026-05-10__local-claude-end-to-end-boot.md` |
| 2026-05-10 | `4b857c2` `0670674` `0a8cf01` `dcd642f` `d358315` `9cf2529` `f2ecfad` `5ba2e82` `2ddb783` `94ff823` | **SG/Compute CLI UX hardening + three SSM/boot bugs** — Create-preview banner now shows `(auto-detect)` / `(auto-resolve)` labels; full equivalent command with `no_wrap=True`; advanced-option flag (5-tuple format) hides 5 vLLM tunables from `--help`. `sg lc diag` adds live `checking…` indicator per check, "Suggested next steps" with copy-paste log commands, latest `[sg-compute]` boot marker, downgrade mid-boot vllm-container/docker-access from fail→warn. `sg lc logs` gains `--follow`/`-f`, default tail bumped 100→300, now shows `via SSM: <cmd>` so the user sees exactly what's read. `sg lc exec` accepts unquoted multi-word commands via `List[str]` args. `--mh` alias for `--max-hours`. `TerminateAt` EC2 tag at create; `time-left` column in `render_list`; `terminate-at`/`time-left` rows in `render_info`. **Bug A (GOOD):** `Section__Docker.py` `until id ssm-user` loop deadlocked the boot — `ssm-user` is created by SSM Session Manager, not SendCommand. Backgrounded with 5-min timeout. **Bug B (MASKING):** `EC2__Instance__Helper.run_command` used a fixed `time.sleep(3)` then a single `get_command_invocation` — silently returned `''` on a busy booting instance. Now polls until terminal state with caller-supplied `timeout_sec`. **Bug C (GOOD):** the Bug B fix introduced a regression where `InvocationDoesNotExist` during the SSM propagation window was swallowed by the outer try/except, silently returning `''` even though the command itself would complete. Fix: inner try/except inside the polling loop; removed artificial pre-poll `sleep(1)`; tightened poll interval `2s → 200ms`. **Design principle established:** no artificial sleeps in SSM retrieval; `InvocationDoesNotExist` is transient not fatal; show the user the exact shell command being executed. **Open Issue 2:** `Schema__CLI__Exec__Result.exit_code` never populated; always shows 0. **Open Issue 3:** advanced-option pattern applied only to local-claude; other specs still expose every option. **Branch `claude/sg-compute-continuation-Ko0RY` pushed, not yet merged.** | `2026-05-10__sg-compute-cli-ux-and-bug-fixes.md` |
| 2026-05-10 | `2005614` `c0e9387` `0afd0f5` `fabd828` `2ebc4ac` `f028b8b` `9f0da84` `cfd99c1` | **SG/Compute v0.2.7→v0.2.8 — Spec__CLI__Builder bug-fix sweep + AMI feature** — Eight Ollama-wedge bugs surfaced on real AWS post-v0.2.7 merge, all fixed (Safe_Str `__init__` no-op; HTTP vs HTTPS health probe; public-IP polling; SSM IAM profile missing; `--disk-size` ignored; bool-default-true dual-flag; `ssm-user` docker group; empty `__init__.py` import). Plus: `pricing` column on `render_list`; v0.2.8 ships `ami list/bake/wait/delete` sub-typer on `Spec__CLI__Builder` with shared tag scheme (`sg-compute-spec`/`sg-source-stack`/`sg-source-instance`); `Section__Shutdown` moved to position 2 in user-data (1-hour spot ran 3 hours because `set -euo pipefail` aborted before timer registered). Sonnet-executable plan written for Phase 4 Docker migration with all 8 lessons baked in. **Branch `claude/add-spec-documentation-XB9ug` pushed but not merged.** Live-AWS smoke is now mandatory for any future Builder migration. | `2026-05-10__sg-compute-spec-cli-builder-and-ami.md` |
| 2026-05-05 | *(pending)*                | **Bug Batch 1 — Sidecar auth and terminal failures** — Root-cause analysis of 5 production bugs (boot-log 401, pods 500, rbash error, CLI-launched API key gap, quick-commands auth failure). All trace to CLI nodes bypassing dashboard key capture. Three backend briefs filed: `BV__host-api-key-via-ec2-tag.md` (EC2 tag interim fix, approved as acceptable risk), `BV__pods-500-error-handling.md` (structured 503), `BV__sidecar-rbash-fix.md`. No frontend code changes needed — frontend already handles `host_api_key` field correctly. | `2026-05-05__bug-batch-1__sidecar-auth-terminal.md` |
| 2026-05-05 | `e4d83e5` | **T2.2b — Firefox credentials + mitm-script routes** — `Schema__Firefox__Credentials__Response` + `Schema__Firefox__Mitm__Script__Response`; `Firefox__SSM__Helper` (SSM send_command + 30s poll); `Firefox__Service.set_credentials` (sed docker-compose.yml + docker compose up) + `upload_mitm_script` (heredoc write + mitmproxy restart); `Routes__Firefox__Stack` PUT /{node_id}/credentials + PUT /{node_id}/mitm-script; `Cli__Firefox` `NotImplementedError` removed; file-read in CLI `upload-mitm-script`; 7 tests. Closes T2_2b brief. | *(no separate debrief — clean implementation)* |
| 2026-05-05 | `04c3f74` | **BV__caller-ip-endpoint — `GET /catalog/caller-ip` + frontend consumer** — `Schema__Caller__IP` (`ip: Safe_Str__IP__Address`); `Routes__Compute__Catalog` reads `X-Forwarded-For` (first in chain); `/catalog/caller-ip` added to `_AUTH_FREE_PATHS`; 4 tests. Frontend: `_seedCallerIp()` calls backend on remote hosts; "Find my public IP ↗" link removed; dead `.caller-ip-lookup` CSS deleted. Closes `BV__caller-ip-endpoint`; T2.5 debrief flipped PARTIAL → COMPLETE. | *(no separate debrief — clean implementation)* |
| 2026-05-05 | `99efcc6` | **BV__spec-readme-endpoint — `GET /api/specs/{spec_id}/readme`** — `Spec__Readme__Resolver` (mirrors `Spec__UI__Resolver`); `Routes__Compute__Specs` adds readme endpoint returning `text/markdown` (404 if spec unknown or no README); `Fast_API__Compute.readme_root_override` threaded; `sg_compute_specs/firefox/README.md` seeded; `*/README.md` in package-data; 4 tests (200+content, 404-no-readme, 404-unknown, root-override). Closes `BV__spec-readme-endpoint` brief. | *(no separate debrief — clean implementation, no failures)* |
| 2026-05-05 | `0d2f3bc`, `fc13272` | **T2.7b — Docstring sweep COMPLETE** — Convert all `"""` template string constants to `'''` across `sg_compute/platforms/ec2/user_data/` (Section__*) and all `sg_compute_specs/*/service/` builders/templates (28 files total). Delete method docstrings from `Cli__Firefox`, `Browser__Launcher`, `Playwright__Service`. `grep -rln '^\s*"""' sg_compute/ sg_compute_specs/` → zero hits. BAD FAILURE classification for T2.7 original (grep gate silently skipped). | `2026-05-05__t2-7b__docstring-sweep.md` |
| 2026-05-05 | *(pending — branch claude/fix-t2-4-production-U2yIZ)* | **T2.6c — Safe_* primitives: pod schema fields + spec-side service sweep** — 5 new primitives (`Safe_Str__Docker__Image`, `Safe_Str__Log__Content`, `Safe_Int__Log__Lines`, `Safe_Int__Pids`, `Safe_Int__Max__Hours`). All 5 pod schemas fully typed (zero raw `: str`/`: int`). `Pod__Manager` schema construction sites explicitly wrap sidecar API values in Safe_Str types. `Docker__Service`, `Podman__Service`, `Vnc__Service` public methods typed. All 3 `*__User_Data__Builder.render()` methods typed. `EC2__Platform.create_node` wraps SSM path. | `2026-05-05__t2-6c__safe-str-schemas-and-spec-side.md` |
| 2026-05-05 | *(pending — branch claude/fix-t2-4-production-U2yIZ)* | **T2.6b (PARTIAL) — Safe_Str public method signatures** — BAD FAILURE classification for T2.6 (silent scope cut, no PARTIAL flag, Pod__Manager left untouched). T2.6b ships: Pod__Manager 6 public methods typed; Platform + EC2__Platform public methods typed; Routes__Compute__Pods + Routes__Compute__Nodes wrap Safe_Str; test calls updated. Deferred to T2.6c: pod schema field primitives (new primitives needed), spec-side service sweep. | `2026-05-05__t2-6b__safe-str-pod-manager-and-platform.md` |
| 2026-05-05 | *(pending)*                | **T3.3b — components/sp-cli/ → components/sg-compute/ directory rename** — `git mv` of parent dir; `sed` sweep of 28 api_site/ references (admin/index.html, user/index.html, launch-form.js) + 45 sg_compute_specs/*/ui/detail/ absolute imports; snapshot test COMPONENT_DIR paths updated; 32/33 CI tests pass (1 pre-existing env gap). Zero `components/sp-cli/` references remain. | `2026-05-05__t3.3b__sp-cli-dir-rename.md` |
| 2026-05-05 | *(pending)*                | **T2.1b — AMI picker live fetch wiring** — `GET /api/amis` landed in dev; `sg-compute-ami-picker.setSpecId()` stub replaced with real `apiClient.get('/api/amis?spec_id=...')` call; `_populateAmis()` builds `<option>` elements, handles empty list + error states; `_showLoading()` / `_showError()` / `_hidePlaceholder()` helpers; `apiClient` import added; stub comment removed. 17-assertion snapshot test `test_sg_compute_ami_picker__snapshot.py` — all pass. T2.1 debrief flipped PARTIAL → COMPLETE. Reality doc: frontend component table added; `Routes__Compute__AMIs` + `AMI__Lister` + schemas documented. | `2026-05-05__t2.1b__ami-picker-live-fetch.md` |
| 2026-05-05 | *(pending)*                | **T2-FE-patch — ami_name + spec-detail fixes** — Task 1: `ami_name` threaded through `sg-compute-launch-panel._launch()` POST body (was silently dropped). Task 2: `creation_mode` confirmed kebab-case matches backend — no change. Task 3: spec-card body click + Enter/Space keyboard access wired in `sg-compute-specs-view`; card `tabindex="0"` + `:focus-visible` ring + `:hover` border. Task 4: README broken anchor replaced with placeholder referencing `BV__spec-readme-endpoint.md`; inline `style=` attributes replaced with `.sd-field-empty`/`.sd-extends-id` CSS classes; `stability\|\|'experimental'` → `\|\|'unknown'` with neutral badge class. Task 5: 13-assertion structural test at `tests/ci/test_sg_compute_spec_detail__snapshot.py` — all pass; visual snapshot PARTIAL (future: Playwright Node live-test pattern). Task 7: T2.2 debrief flipped COMPLETE → PARTIAL with gaps table appended. | `2026-05-05__t2-fe-patch__ami-spec-detail-fixes.md` |
| 2026-05-05 | *(pending — branch claude/fix-t2-4-production-U2yIZ)* | **T2.4b — Vault production wiring fix** — BAD FAILURE classification for T2.4: `vault_attached=False` in production wiring made every PUT return 409; test bypassed `prefix='/api/vault'` and never hit the production URL. Fix: two lines — `vault_attached=True` in `Fast_API__Compute._mount_control_routes`; `prefix='/api/vault'` in `_client()` with URL strings updated. No scope cut. CI gate required before merge. | `2026-05-05__t2-4b__vault-writer-production-fix.md` |
| 2026-05-05 | *(backfill)*               | **T3.1–T3.5 + T2.3–T2.5 — FE integration cleanup** — T3.1: `user.js` migrated from `/catalog/types`+`/catalog/stacks` to `/api/specs`+`/api/nodes`; dead `sg-compute-launch-modal` / `sg-compute-stack-detail` / `sg-compute-vault-activity` script tags removed from `user/index.html`; dead `_openModal` / `MODAL_TAG` / `sp-cli:user-launch` listener removed from `user.js`. T3.2: `sg-compute-launch-panel._launch()` now POSTs to `/api/nodes` with `spec_id` + `node_name`; `sg-compute-launch-form.getValues()` returns `node_name` (was `stack_name`); `populate()` uses `entry.spec_id` (was `type_id`). T3.3 partial: dead script tags removed; event-vocabulary spec updated (removed `sp-cli-stacks-pane.js` reference, updated emitter names). T3.3 rename STOPPED — 8 `sg_compute_specs/*/ui/detail/` files have absolute imports `/ui/components/sp-cli/_shared/...`; rename must coordinate across both trees. T3.4: `shared/launch-defaults.js` created (REGIONS, INSTANCE_TYPES, MAX_HOURS, COST_TABLE); local copies deleted from `sg-compute-compute-view.js` and `sg-compute-launch-form.js`. T3.5: all `||` fallbacks dropped — `node_id\|\|stack_name`, `spec_id\|\|type_id`, `pod_name\|\|name`, `state\|\|status` — in `admin.js` and `sg-compute-nodes-view.js`. T2.3: roving tabindex keyboard nav on nodes-view tablist; `--text-3` WCAG AA fix (#5a6478→#96a6bc). T2.4: dual dispatch `sp-cli:spec.toggled`+`sp-cli:plugin.toggled` in settings-bus. T2.5: caller-IP Option B heuristic (PARTIAL — backend brief filed). | `2026-05-05__t2.3__a11y-keyboard-and-contrast.md`, `2026-05-05__t2.4__settings-bus-dual-dispatch.md`, `2026-05-05__t2.5__caller-ip-consumer.md` |
| 2026-05-05 | 02d57ea                    | **T2.1 — BE: create_node for podman + vnc; generic service dispatch** — Option B adopted: `EC2__Platform._service_for()` maps spec_id to wired service; each of docker/podman/vnc owns `create_node(base_request, api_key_ssm_path)`; schema fields `registry` + `api_key_ssm_path` added to `Schema__Podman__Create__Request` + `Schema__Vnc__Stack__Create__Request`; both services thread fields to `user_data_builder.render()`; 19 new tests; 259 pass. | `2026-05-05__t2-1-be-create-node-podman-vnc.md` |
| 2026-05-05 | 815b7c5                    | **T1 — BE Security Hotfix** — T1.1: `Fast_API__Compute` → `Serverless__Fast_API` + `_Middleware__Health_Bypass` (health probes bypass auth) + `setup_middlewares()` called in `setup()` (critical fix: middleware was never registered). T1.2: `--privileged` removed from `Section__Sidecar` TEMPLATE. T1.3: SSM SecureString per-node key (`SSM__Sidecar__Key`); `Section__Sidecar` fetches key at EC2 boot via `aws ssm get-parameter --with-decryption`; `api_key_value` → `api_key_ssm_path` across 14 spec builders. T1.4: fixed transitively. T1.5: `Pod__Manager._resolve_api_key()` reads per-node SSM key first, env-var fallback. T1.6: boot assertions in both `Fast_API__Compute` and `Fast_API__Host__Control`. 12 new auth tests; 384 total pass. | `2026-05-05__t1-be-security-hotfix.md` |
| 2026-05-05 | *(backfill)*               | **T2.2 — sg-compute-spec-detail component (FV2.4 scope cut)** — New `sg-compute-spec-detail` component: manifest panel, extends lineage (placeholder), AMI list (placeholder), README link, Launch node button; "View details" button on spec cards in `sg-compute-specs-view`; `_openSpecDetailTab()` + `sp-cli:spec.selected` listener in admin.js. Linux spec removal: `sp linux` alias removed from provision_ec2.py; scripts/linux.py deleted. | `2026-05-05__t2.2__spec-detail-view.md` |
| 2026-05-05 | *(backfill)*               | **T2.1 — Launch flow three-mode selector + AMI picker (PARTIAL)** — Add FRESH/BAKE_AMI/FROM_AMI radio selector to `sg-compute-launch-form`; CSS-only show/hide via mode class on form root; cost preview banner for BAKE_AMI; new `sg-compute-ami-picker` component (placeholder — `GET /api/amis` backend route TBD); `validate()` blocks FROM_AMI without AMI; `sg-compute-launch-panel` passes `creation_mode` + `ami_id` in POST body. Backend follow-up brief filed at `team/comms/briefs/v0.2.1__hotfix/backend/BV__ami-list-endpoint.md`. | `2026-05-05__t2.1__launch-flow-three-modes.md` |
| 2026-05-05 | *(backfill)*               | **T1.7 — Fix /ui/ absolute imports broken by BV2.10** — Add `_mount_dashboard_ui()` to `Fast_API__Compute`: mounts `api_site/` at `/ui/` directly on the control plane; `dashboard_root_override` for tests; 4 new tests. Option A (relative imports) rejected — URL branches can't be traversed. Option B is stable across `/legacy/` deprecation. | `2026-05-05__t1.7__ui-imports-runtime-fix.md` |
| 2026-05-05 | 1edc6ce                    | **BV2.12 — Delete `agent_mitmproxy/`; shim task deferred** — `scripts/provision_ec2.py` import fixed to `sg_compute_specs.mitmproxy`; `agent_mitmproxy/` git rm (35 files); `tests/unit/agent_mitmproxy/` git rm (12 tests); `ci__agent_mitmproxy.yml` deleted; CI path filters cleaned; 2114 unit tests pass. Shim task deferred: `__cli/<spec>/` implementations diverged from `sg_compute_specs/<spec>/` — thin shims would silently break callers. | `2026-05-05__bv2.12__cleanup-mitmproxy.md` |
| 2026-05-05 | 59d060a                    | **BV2.11 — Lambda packaging cutover; delete legacy sgraph_ai_service_playwright/** — `lambda_entry.py` → `sg_compute_specs.playwright.core`; `build_request()` stages `sg_compute_specs/` tree; `_REPO_ROOT_GUESS` depth fix; 55 test files bulk-updated; SP CLI docker/deploy updated; `sgraph_ai_service_playwright/` git rm (175 files); `pyproject.toml` updated; 2151 unit tests pass. | `2026-05-05__bv2.11__lambda-cutover.md` |
| 2026-05-05 | *(backfill)*               | **BV2.10 — Fold Fast_API__SP__CLI into Fast_API__Compute at /legacy/** — git mv `Routes__Ec2__Playwright` + `Routes__Observability` to `sg_compute/control_plane/legacy_routes/`; shims at old paths; `_mount_legacy_routes()` mounts `Fast_API__SP__CLI` sub-app (with auth) at `/legacy`; ASGI wrapper injects `X-Deprecated: true` on all responses; `run_sp_cli.py` → `Fast_API__Compute`; 11 new tests; 356 total. | `2026-05-05__bv2.10__fold-sp-cli-fastapi.md` |
| 2026-05-05 | *(backfill)*               | **FV2.6 — all 8 specs UI co-location** — 48 files moved to `sg_compute_specs/{spec}/ui/{card,detail}/` for docker+podman+vnc+neko+prometheus+opensearch+elastic+firefox; `api_site/plugins/` deleted; all per-spec detail dirs deleted; detail JS imports → absolute `/ui/` paths; `admin/index.html` all spec tags → `/api/specs/<id>/ui/...`. | `2026-05-05__fv2.6__docker-ui-colocation.md` |
| 2026-05-05 | *(backfill)*               | **BV2.19 — StaticFiles mount for per-spec UI assets** — `Spec__UI__Resolver` (Type_Safe, importlib discovery, `ui_root_override` for tests); `Fast_API__Compute` mounts `StaticFiles` at `/api/specs/{spec_id}/ui` when `ui/` dir exists; silent no-op when absent; `sg_compute_specs/*/ui/**/*` in pyproject.toml; 9 new tests; 322 total. Unblocks FV2.6. | `2026-05-05__bv2.19__spec-ui-static-files.md` |
| 2026-05-05 | *(backfill)*               | **BV2.9 — Migrate vault layer to sg_compute/vault/; rename plugin→spec** — 13 new files under `sg_compute/vault/`; `Vault__Spec__Writer` accepts `spec_registry` or `write_handles_by_spec` dict; `Routes__Vault__Spec` mounted at `/api/vault` on `Fast_API__Compute`; 11 legacy shims; BV2.8 `Optional[T]` fix included (10 non-circular ok; 7 circular reverted to `object=None`); 313 passed + 66 vault tests. | `2026-05-05__bv2.9__migrate-vault.md` |
| 2026-05-05 | 0991ce0                    | **FV2.12 — sp-cli-* → sg-compute-* prefix rename** — Mechanical sweep: 30 top-level + 8 _shared + 8 plugin-card components renamed (dirs + files + content); `customElements.define('sg-compute-*')`, `class SgCompute*`, `admin/index.html` script paths all updated; 146 files changed. Event names (`sp-cli:*`) and localStorage keys intentionally preserved. Un-deferred from v0.3 by Architect to unblock FV2.6. | `2026-05-05__fv2.12__prefix-rename.md` |
| 2026-05-05 | 9983f91                    | **BV2.7: Migrate tier-1 `__cli/` → `sg_compute/`** — 14 new canonical modules (primitives, enums, event_bus, image/); 46 spec files import-rewritten; CI guard `test_no_legacy_imports` added; 584 tests passing. BV2.8 Task 2 (`object=None` typed annotations) blocked by Type_Safe union limitation + circular imports — all changes reverted cleanly. | `2026-05-05__bv2.7__migrate-tier1-cli.md` |
| 2026-05-05 | 667dbdb                    | **BV2.6: Per-spec CLI + dispatcher** — `Spec__CLI__Loader` auto-discovers `cli/Cli__<Pascal>.py` from each spec; docker is the pilot (`Cli__Docker` with create/list/info/delete); `Cli__Compute__Spec` mounts discovered apps at `sg-compute spec <spec_id> <verb>`; 19 new tests. | `2026-05-05__bv2.6__per-spec-cli.md` |
| 2026-05-05 | 75853e9                    | **BV2.5 — `EC2__Platform.create_node` + `POST /api/nodes` + `lambda_handler.py`** — `create_node` dispatches on `spec_id` (docker live, others raise `NotImplementedError`); `Routes__Compute__Nodes` gains `POST /api/nodes`; `lambda_handler.py` mirrors host-plane Mangum pattern. 4 new tests; 235 total. | `2026-05-05__bv2.5__create-node-lambda.md` |
| 2026-05-05 | *(backfill)*               | **FV2.8 — Confirm zero /containers/* URL references** — Sweep confirmed zero `/containers/*` URL hits in active dashboard code; `container_count` also absent; single cosmetic fix: CSS comment `Containers tab` → `Pods tab`; reality doc updated (BV2.5 removed from PROPOSED, `POST /api/nodes` added to EXISTS). Gates BV2.17 (sidecar alias deletion). | `2026-05-05__fv2.8__containers-sweep.md` |
| 2026-05-05 | *(backfill)*               | **FV2.7 — Pods tab via control-plane proxy** — `_fetchContainers()` uses `GET /api/nodes/{id}/pods/list` via apiClient; `_fetchPodLogs()` + `_pollLiveLogs()` use `GET /api/nodes/{id}/pods/{name}/logs`; `_renderContainers()` adapts `pod_name`/`state` schema; `_portLinks()` parses Docker string port format. Host stats + pod stats still direct sidecar (no proxy). Unblocked by BV2.3. | `2026-05-05__fv2.7__pods-tab.md` |
| 2026-05-05 | *(backfill)*               | **FV2.5 — Launch flow via POST /api/nodes** — `_launch()` in compute-view now posts `{spec_id, node_name, region, instance_type, max_hours}` to `/api/nodes`; removed per-spec `create_endpoint_path` usage; removed `public_ingress` / Open access checkbox (no backend field); `caller_ip` auto-detected server-side. Unblocked by BV2.5. | `2026-05-05__fv2.5__launch-flow.md` |
| 2026-05-05 | *(backfill)*               | **FV2.11 — Delete legacy components** — Delete `sp-cli-stacks-pane` (3 files); remove its script tag from admin/index.html and dead querySelector from admin/admin.js; delete `api.ipify.org` call + `_fetchBrowserIp()` from `sp-cli-launch-form.js`; remove browser IP detection row from launch-form.html; clean up dead `.ip-row`/`.ip-label`/`.ip-value` CSS. | `2026-05-05__fv2.11__legacy-cleanup.md` |
| 2026-05-05 | *(backfill)*               | **FV2.10 — A11y pass** — `role="tablist"` + per-tab `aria-selected`/`aria-controls` + per-panel `role="tabpanel"` on nodes-view detail; node-row divs keyboard-accessible (`role="button"`, `tabindex`, Enter/Space); `aria-label` on all icon-only buttons (collapse, refresh, close-detail, refresh-ct, close-log, live-log, refresh-boot, refresh-ec2, eye, copy); spec cards in compute-view: `role="button"`, `aria-pressed`, `aria-disabled`, keyboard handler; `:focus-visible` rings on nodes-view, compute-view, left-nav. | `2026-05-05__fv2.10__a11y-pass.md` |
| 2026-05-05 | *(backfill)*               | **FV2.9 — Event vocabulary finish** — Migrated per-spec launch events from `sp-cli:plugin:*` to `sp-cli:spec:*` namespace; added `spec_id` to all 8 plugin card STATIC objects; dual-dispatch in `_launch()` (both forms); `admin.js` listener wiring from catalogue covers both forms; FAMILIES map in `sp-cli-events-log.js` extended; published `library/docs/specs/v0.2.0__ui-event-vocabulary.md` as event vocabulary spec. | `2026-05-05__fv2.9__event-vocabulary.md` |
| 2026-05-05 | *(backfill)*               | **FV2.4 — Specs view** — Create `<sp-cli-specs-view>` (3-file component); per-spec card with icon, display_name, spec_id, stability badge, capability chips, version, boot estimate, "Launch node" button; empty state; WCAG AA contrast; ARIA labels; add "Specs" to left-nav + VIEW_TITLES + viewTags. | `2026-05-05__fv2.4__specs-view.md` |
| 2026-05-05 | *(backfill)*               | **FV2.3 — `shared/spec-catalogue.js` + delete hardcoded plugin lists** — Create `spec-catalogue.js` (`loadCatalogue`, `getCatalogue`, `getSpec`); delete `CATALOG` from compute-view, `PLUGIN_ORDER` from launcher-pane, `LAUNCH_TYPES` from admin.js, `DEFAULTS.plugins` reduced to `{}`; settings-bus populates toggles from catalogue; delete `shared/catalog.js`. | `2026-05-05__fv2.3__catalogue-loader.md` |
| 2026-05-05 | *(backfill)*               | **FV2.2 — Switch dashboard to `/api/specs` + `/api/nodes`; field renames** — Fix `NODE_STATE` values to lowercase; add `use_legacy_api` toggle to settings-bus (schema v3); update `admin.js` to call `/api/specs` + `/api/nodes` with feature-flag fallback to `/catalog/*`; delete via `DELETE /api/nodes/{node_id}`; rename `stack_name→node_id`, `type_id→spec_id` across 18 files (nodes-view, stacks-pane, user-pane, stack-header, stop-button, host-shell, cost-tracker, events-log, 7 per-spec detail components). | `2026-05-05__fv2.2__api-client-migration.md` |
| 2026-05-05 | 4f81a67                    | **BV2.2: Section__Sidecar composable user-data fragment** — `Section__Sidecar` extracted; wired into 8 template-based spec builders (docker, podman, opensearch, prometheus, neko, firefox, elastic, vnc) and 2 parts-based builders (ollama, open_design); PLACEHOLDERS tuples updated in 8 test files; 17 new sidecar tests; 553 passing. | `2026-05-05__bv2.2__section-sidecar.md` |
| 2026-05-05 | 113fef8                    | **BV2.3 — Pod__Manager + Routes__Compute__Pods** — `Sidecar__Client` HTTP adapter; `Pod__Manager` bridge (node_id→public_ip→sidecar); 5 pod schemas; 2 `Type_Safe__Dict` collections; `Routes__Compute__Pods` (6 endpoints, constructor injection). Key finding: `Type_Safe__Dict` subclasses can't be Pydantic-serialised by osbot_fast_api — `Schema__Pod__Start__Request` simplified to str fields only. 15 new tests; 246 total passing. | `2026-05-05__bv2.3__pod-manager.md` |
| 2026-05-04 | 7ca8b96                    | **BV2.4 — Refactor `Routes__Compute__Nodes`** — constructor injection `platform: Platform`; all endpoints return `<schema>.json()`; credential logic moved to `EC2__Platform`; `Exception__AWS__No_Credentials` handler on `Fast_API__Compute`; `test_Routes__Compute__Nodes` rewritten with zero mocks (3 fake Platform subclasses). 231 passed. | `2026-05-04__bv2.4__routes-nodes-cleanup.md` |
| 2026-05-04 | *(backfill)*               | **FV2.1 — Centralise node-state vocabulary** — Create `shared/node-state.js` (`NODE_STATE`, `isRunning`, `stateClass`, `stateLabel`, `nodePillClass`, `podPillClass`); replace 6 node-state `=== 'running'` hardcodes in `sp-cli-nodes-view.js`; sweep 5 detail components + cost-tracker + stacks-pane + user-pane + status-chip; add `state-ready/booting/terminating/terminated/failed` CSS classes. Zero `=== 'running'` hits post-patch. | `2026-05-04__fv2.1__state-vocabulary-fix.md` |
| 2026-05-04 | 0517528                    | **BV2.1 — Delete orphan `sgraph_ai_service_playwright__host/`** — 40+ files git-rm'd; pyproject.toml cleaned; ci__host_control.yml run-unit-tests job removed; 3 legacy test files deleted; host-control/index.md updated (authority → sg_compute/host_plane/, port `:9000` → `:19009`). sg_compute__tests: 231 passed. | `2026-05-04__bv2.1__delete-orphan-host.md` |
| 2026-05-04 | *(pending)*                | **Sidecar UI endpoints (v0.1.154 brief)** — `GET /host/logs/boot` (cloud-init tail), `GET /pods/{name}/logs` (schema updated: `name→container`, `logs→content`, add `lines`+`truncated`, add `timestamps` param), `GET /pods/{name}/stats` (`docker stats --no-stream` with MB parsing helpers). P3 WebSocket streaming explicitly skipped — polling `/pods/{name}/logs?tail=50` is sufficient; WS auth complexity (query-param API key) deferred. 34 new tests pass. | `2026-05-04__sidecar-ui-endpoints.md` |
| 2026-05-04 | c3fc219                    | **B6: host plane → sg_compute/host_plane/, containers → pods** — `sgraph_ai_service_playwright__host/` replicated under `sg_compute/host_plane/`; `containers/` → `pods/`; all `Container__*` classes renamed to `Pod__*`; `Schema__Host__Status.container_count` → `pod_count`; `get_container_runtime()` → `get_pod_runtime()`; Dockerfile COPY + CMD updated; tests moved to `sg_compute__tests/host_plane/`. 43 files. | `2026-05-03__spot-instances-by-default.md` |
| 2026-05-03 | 16afcde, e8a79e8           | **Spot instances by default across all EC2 stacks** — `InstanceMarketOptions: {MarketType: spot}` injected in all 7 Launch Helpers; `use_spot: bool = True` added to all 7 create request schemas; `--no-spot` flag on all 9 CLI create commands (incl. firefox create-from-ami); `spot: bool` in `Schema__Docker__Info` (from `InstanceLifecycle`), `use_spot` in create response; pricing column in `sp docker list`, row in info and create. 5 fake stubs updated. Key issue: `AWSServiceRoleForEC2Spot` must exist in the account first — one-time `aws iam create-service-linked-role --aws-service-name spot.amazonaws.com`. 35 files, 1636 tests. | `2026-05-03__spot-instances-by-default.md` |
| 2026-05-03 | b5870b1…eefc8c2            | **Host control plane — sp docker end-to-end** — `sgraph_ai_service_playwright_host` ECR image wired into `sp docker create`: Dockerfile fixed (source copy + PYTHONPATH, `docker-cli` apk, ECR token deleted post-pull), dedicated CI pipeline (`ci__host_control.yml`), port 9000 opened in Docker SG, `Docker__User_Data__Builder` extended (ECR login + docker run + api key injection), `--wait` now checks host control container status, `--open` opens SG to 0.0.0.0/0, create output shows DevTools cookie snippet. 11 bugs fixed: PyPI phantom package, missing CI, pytest not in workflow, JMESPath null crash, cross-package import, health timeout=0 never probing, SG missing port 19009, user-data missing host control section, ECR token on disk, docker-cli missing in Alpine, --open misinterpreted. | `2026-05-03__host-control-plane-docker-stack.md` |
| 2026-05-02 | ddd3ff8                    | **B5 — sg-compute CLI** — `sg_compute/cli/` with `Cli__Compute` (root typer app), `Cli__Compute__Spec` (`spec list` + `spec info <id>`), `Cli__Compute__Node/Pod/Stack` (list placeholders). `Renderers.py` with Rich table/panel output for all 5 types. Entry point `scripts/sg_compute_cli.py` registered as `sg-compute` console script in `pyproject.toml`. Script renamed from `sg_compute.py` → `sg_compute_cli.py` to avoid shadowing the `sg_compute/` package on sys.path. 21 new tests, 190 total. | `2026-05-02__b5__sg-compute-cli.md` |
| 2026-05-02 | 77505a0                    | **B4 — Fast_API__Compute control plane** — `sg_compute/control_plane/` with `Fast_API__Compute` (auto-discovers and mounts per-spec route classes via `Spec__Routes__Loader` convention), `Routes__Compute__Health` (`/api/health`, `/api/health/ready`), `Routes__Compute__Specs` (`/api/specs`, `/api/specs/{spec_id}`), `Routes__Compute__Nodes` + `Routes__Compute__Stacks` (placeholders). Mounts `Routes__Docker__Stack` + `Routes__Podman__Stack` under `/api/specs/{spec_id}/*` automatically. 27 new tests, 169 total. Good failures: package vs class-file import in loader, `Safe_Str__Fast_API__Route__Prefix` path type in set membership, `raise_server_exceptions=False` for AWS-credential path. | `2026-05-02__b4__fast-api-compute-control-plane.md` |
| 2026-05-02 | d654425, b3e94d0, 54524ab, 3dd231b | **B3.4–B3.7 — sg_compute_specs migration: Prometheus, OpenSearch, Elastic, Firefox** — Four spec packages migrated from `sgraph_ai_service_playwright__cli/` into `sg_compute_specs/`. Each follows: enums + primitives + schemas + collections + service helpers (tags builder, stack mapper, HTTP probe, interceptor resolver) + FastAPI routes + manifest + tests. Key decisions: Elastic uses monolithic AWS client (CLI fidelity); Firefox routes use `body: dict + from_json()` for nested interceptor choice; `Enum__Firefox__Stack__State` upgraded to `(str, Enum)` for JSON serialization; Firefox `nav_group=BROWSERS` (COMPUTE not in `Enum__Spec__Nav_Group`). 3 good failures surfaced early (COMPUTE enum, 1-char stack name, shell `${{}}` escaping). Total: 139 new tests, all green. | `2026-05-02__b3.4-b3.7__sg-compute-specs-migration.md` |
| 2026-05-02 | *(pending)*                | **Vault paused; settings-bus migrated to localStorage** — `settings-bus.js` now loads/persists settings via `localStorage` key `sp-cli:settings:v2`. Removed vault dependency from `admin.js` (startVaultBus + vault:connected listener) and `sp-cli-settings-view` (isWritable + ro-banner). `vault-bus.js` left intact for future sync-on-connect re-integration. `sp-cli:settings.loaded` now fires synchronously at boot with no network round-trip. | `2026-05-02__vault-pause-localStorage-settings.md` |
| 2026-04-29 | *(pending)*                | **v0.1.97 (3/3) — `sp doctor` typer command — v0.1.97 done** — New top-level `sp doctor` with `passrole` + `preflight` subcommands (and a `@callback(invoke_without_command=True)` so bare `sp doctor` runs both). Reuses `ensure_caller_passrole` / `aws_account_id` / `aws_region` / `ecr_registry_host` from `Ec2__AWS__Client`. **Hard-cut: `sp pw ensure-passrole` deleted entirely** (folded into `sp doctor passrole` per Q3 default). Inline iam:PassRole warning updated to point at `sp doctor passrole`. 4 new tests + extended pw subgroup test. 1011 / 1 skipped passing. **v0.1.97 done — clean top-level shape: 2 commands (catalog, doctor) + 8 subgroups, symmetric across all stack sections.** | `2026-04-29__v0.1.97__03__sp-doctor.md` |
| 2026-04-29 | eb8ff78                    | **v0.1.97 (2/3) — `sp catalog` typer command** — Cross-section enumeration. Mirrors `Routes__Stack__Catalog` HTTP surface but reads directly from `Stack__Catalog__Service` (no FastAPI server). Two commands: `sp catalog types` (lists known stack types incl. linux/docker/elastic/opensearch/vnc) and `sp catalog stacks [--type X]` (live stacks across all sections). `--type` validates against `Enum__Stack__Type`; unknown raises `typer.BadParameter`. New `cli/catalog/cli/Renderers.py` (`render_types` + `render_stacks`) and `scripts/catalog.py`. Note: `list_all_stacks` only iterates linux/docker/elastic today (catalog `Stack__Catalog__Service` doesn't compose OS/Prom/VNC services yet — separate gap, see vnc-handover dev-pack 05). 7 new tests; 1007 passing. | `2026-04-29__v0.1.97__02__sp-catalog.md` |
| 2026-04-29 | a0c1f23                    | **v0.1.97 (1/3) — `sp pw` subgroup; Playwright joins the sister-section convention** — Per the v0.1.97 brief (clean top-level commands). 21 Playwright-EC2-specific commands (`create`, `list`, `info`, `delete`, `connect`, `shell`, `env`, `run`, `exec`, `exec-c`, `logs`, `diagnose`, `forward`, `wait`, `clean`, `create-from-ami`, `open`, `screenshot`, `smoke`, `health`, `ensure-passrole`) moved under `sp pw` (long form `sp playwright`, short alias hidden). The `sp vault` and `sp ami` subgroups also nested under `sp pw`. Top-level `app` help reworded — no longer "the Playwright provisioner". Pure decorator move; function bodies unchanged. Test renames + 1 new `sp pw` subgroup test. 1000 / 1 skipped passing. | `2026-04-29__v0.1.97__01__sp-pw-subgroup.md` |
| 2026-04-29 | 68a3078                    | **Playwright stack split — Phase D (2/2): regroup `vault-*` under `sp vault` + `*-ami` under `sp ami` — v0.1.96 done** — Per plan doc 7 C1 (hard cut). Seven flat `sp vault-{clone,list,run,commit,push,pull,status}` commands moved under `sp vault` subgroup (with `sp v` short alias). Four flat `sp {bake,wait,tag}-ami` + `sp list-amis` commands moved under `sp ami` subgroup (verbs match `sp el ami`: `create / wait / tag / list`). `sp create-from-ami` stays top-level (different action — launches an instance from a baked AMI). Pure decorator move; function bodies + signatures unchanged. 2 new subgroup-help tests; existing `test__app_has_expected_commands` updated. 999 / 1 skipped passing. **All 4 Phase D items shipped — v0.1.96 stack-split refactor is complete (Phases A → D).** | `2026-04-29__playwright-stack-split__phase-D__vault-and-ami-regroup.md` |
| 2026-04-29 | d5b5c26                    | **Playwright stack split — Phase D (1/2): drop forward-* / metrics + add `sp prom metrics <url>`** — Per plan doc 7 (C1 hard cut + C2 metrics move). Deleted `sp forward-prometheus` / `forward-browser` / `forward-dockge` typer commands (their targets moved out of the Playwright EC2 in Phase C). Deleted top-level `sp metrics` (one-off SSM curl; operators can `sp exec` it). Added `sp prom metrics <url> [--key <api-key>]` — URL-based, plain HTTP, works for any `/metrics` endpoint. Dropped orphaned constants: `EC2__BROWSER_INTERNAL_PORT` (in `Ec2__AWS__Client`), `EC2__PROMETHEUS_PORT` / `EC2__BROWSER_IMAGE` / `EC2__DOCKGE_PORT` / `EC2__DOCKGE_IMAGE` (in `provision_ec2.py`). Cleaned `sp open` URL hints (dropped 3 lines), `sp diagnose` port grep (8000/8001 only), `provision()` return dict (no more `browser_url`). 997 tests still passing. | `2026-04-29__playwright-stack-split__phase-D__forward-and-metrics.md` |
| 2026-04-29 | 9af6498                    | **Playwright stack split — Phase C: strip the Playwright EC2 (9 containers → 2)** — Removed 7 compose-fragment constants (`COMPOSE_SVC_BROWSER`/`BROWSER_PROXY`/`CADVISOR`/`NODE_EXPORTER`/`PROMETHEUS`/`FLUENT_BIT`/`DOCKGE`), 7 config-template constants (PROMETHEUS_YML/REMOTE_WRITE/FLUENT_BIT_*/NGINX_BROWSER), and 2 render functions (`render_observability_configs_section`/`render_browser_proxy_section`). `render_compose_yaml` shrank from 11 → 9 params (dropped `amp_remote_write_url`/`opensearch_endpoint` + the 3 conditional branches + DOCKGE service line + `prometheus_data` volume). `render_user_data` shrank from 9 → 6 params. `Ec2__AWS__Client.SG_INGRESS_PORTS` shrank from 3 → 2 ports (dropped `EC2__BROWSER_INTERNAL_PORT`); `SG__DESCRIPTION` updated. `~−270 LoC net` in `provision_ec2.py`. `forward-prometheus` / `forward-browser` / `forward-dockge` typer commands stay broken transitionally — Phase D deletes them. New defensive test `test__no_browser_or_observability_services`; entire `test_render_observability_configs` class deleted. **2-container compose now canonical.** | `2026-04-29__playwright-stack-split__phase-C__strip-playwright-ec2.md` |
| 2026-04-29 | e07c68f                    | **v0.1.101 — MVP Provisioning UI (Admin + User)** — Three backend PRs + static UI. PR-1: linux + docker routes mounted on `Fast_API__SP__CLI` (routes existed, weren't wired); `List__Port` → `List[int]` fix for FastAPI startup. PR-2: new `catalog/` sub-package — `Enum__Stack__Type`, 4 schemas, 2 collections, `Stack__Catalog__Service` (composes Linux/Docker/Elastic), `Routes__Stack__Catalog` (`GET /catalog/types` + `GET /catalog/stacks?type=`), 13 tests. PR-3: `Routes__Elastic__Stack` (5 lifecycle endpoints), 10 tests. Static UI: 4 shared utilities (tokens, api-client, catalog cache, poll loop), 7 Web Components (`sg-api-client`, `sg-auth-panel`, `sg-header`, `sg-toast-host`, `sg-stack-card`, `sg-stack-grid` 4-mode, `sg-create-modal` 3-state), admin dashboard, user provisioning page, landing page, `scripts/ui__serve-locally.sh`. Post-ship: fixed 500 on `/linux/stacks` — `Linux__Service.setup()` must be called explicitly; `aws_client` is lazy-init. Full suite: 1176 passing. | `2026-04-29__v0.1.101__mvp-admin-user-ui.md` |
| 2026-04-29 | 41120fc                    | **Playwright stack split — Phase B · Step 7h: `sp vnc` typer commands (Tier-2A) — sp vnc complete** — **7 commands** (`create` / `list` / `info` / `delete` / `health` / `flows` / `interceptors`) wired into the main `sp` app via `add_typer` in `scripts/provision_ec2.py` (single `sp vnc` name; no short alias per N1). Two extras vs prom: `sp vnc flows <name>` (peek mitmweb flows; per N4 no auto-export) and `sp vnc interceptors` (lists baked example names; no AWS call — reads `list_examples()` directly). N5 mutually-exclusive flags on `create`: `--interceptor <name>` (baked example) or `--interceptor-script <path>` (read local `.py` file → INLINE source); both omitted → NONE default-off; both provided → `typer.BadParameter`. Renderers handle `interceptor_kind=NONE → 'none'` label. 15 new tests via `typer.testing.CliRunner` + Rich Console-capture. **Phase B step 7 (`sp vnc`) is now functionally complete end-to-end.** | `2026-04-29__playwright-stack-split__phase-B__step-7h__sp-vnc-typer.md` |
| 2026-04-29 | 2eb7bb7                    | **Playwright stack split — Phase B · Step 7g: `sp vnc` FastAPI routes (Tier-2B)** — Two route files. `Routes__Vnc__Stack` ships 5 routes mirroring `Routes__Prometheus__Stack` (POST/GET/DELETE under `/vnc`); `create` takes `body: dict` + `from_json` because the request carries a nested `Schema__Vnc__Interceptor__Choice` (same workaround as prom). `Routes__Vnc__Flows` ships one route — `GET /vnc/stack/{name}/flows` returns `{"flows": [...summaries]}` (per N4 no auto-export — just a peek for human debug; envelope keeps it future-proof for total/dropped counts). 13 new tests via real `_Fake_Service(Vnc__Service)` subclass + FastAPI TestClient (no mocks). | `2026-04-29__playwright-stack-split__phase-B__step-7g__sp-vnc-routes.md` |
| 2026-04-29 | fc5f5fc                    | **Playwright stack split — Phase B · Step 7f: `sp vnc` user-data + compose + interceptor resolver + launch + wire `create_stack`** — Compressed slice mirroring sp prom 6f. Ships `Vnc__Compose__Template` (3 services on `sg-net`; chromium uses `HTTPS_PROXY=http://mitmproxy:8080` + `--browser=chromium` per N2; **port 443 only**; **no secrets in YAML** — locked by test), `Vnc__Interceptor__Resolver` (N5: 3 baked example sources inline; `resolve(choice)` → `(source, label)` for NONE/NAME/INLINE; defensive on unknown name + empty inline source), `Vnc__User_Data__Builder` (installs `docker`+`httpd-tools`+`openssl`; writes nginx config + bcrypt htpasswd + self-signed TLS cert + mitmproxy proxyauth file + interceptor source + compose YAML; **operator_password interpolated exactly once** as a shell var, then referenced from there for both htpasswd and proxyauth), `Vnc__Launch__Helper` (`DEFAULT_INSTANCE_TYPE='t3.large'` for chromium RAM headroom). `Vnc__AWS__Client.setup()` adds `launch` slot; `Vnc__Service.setup()` chain grows from 5 to 8 helpers; `create_stack` resolves all defaults + threads N5 source/label through user-data + tags + response. 47 new tests; 161 / 161 green in `cli/vnc/`. **`sp vnc` is now functionally complete in pure logic.** | `2026-04-29__playwright-stack-split__phase-B__step-7f__sp-vnc-create-stack.md` |
| 2026-04-29 | 877791e                    | **Playwright stack split — Phase B · Step 7e: `sp vnc` Service orchestrator (read paths) + flows()** — 4 small focused files mirroring sp prom 6e. `Caller__IP__Detector` (section-local), `Random__Stack__Name__Generator` (vocabulary parity locked), `Vnc__Stack__Mapper` (pure boto3-detail → schema; builds `viewer_url`/`mitmweb_url`; **decodes `sg:interceptor` tag back into N5 (kind, name)**), `Vnc__Service` (Tier-1 orchestrator with `setup()` wiring 5 helpers). Service exposes `list_stacks` / `get_stack_info` / `delete_stack` / `health` (READY only when both `nginx_ok` and `mitmweb_ok`) + new `flows()` op (per N4 — peek mitmweb flows; no auto-export; consumed by `Routes__Vnc__Flows` in 7g). `_flow_summary_from_mitmweb` pure mapper handles missing `pretty_url` / `response` / `timestamp_created` defensively. 25 new tests via real `_Fake_*` subclasses (no mocks). | `2026-04-29__playwright-stack-split__phase-B__step-7e__sp-vnc-service-read-paths.md` |
| 2026-04-29 | 4d5a035                    | **Playwright stack split — Phase B · Step 7d: `sp vnc` HTTP base + probe** — Two small focused HTTP files. `Vnc__HTTP__Base` wraps `requests` with `verify=False` default (nginx self-signed) + Basic auth seam (operator creds front both nginx UI and mitmweb). `Vnc__HTTP__Probe` ships 3 probes: `nginx_ready` (200 on `/`), `mitmweb_ready` (200 on `/api/flows`), `flows_listing` (parsed JSON list from `/api/flows`; `[]` on any failure incl. defensive non-array body check). 15 new tests via real `_Fake_HTTP` + `_Fake_Response` subclasses (no mocks). | `2026-04-29__playwright-stack-split__phase-B__step-7d__sp-vnc-http-base-and-probe.md` |
| 2026-04-29 | 7ae5ad2                    | **Playwright stack split — Phase B · Step 7c: `sp vnc` AWS helpers (small-file discipline)** — 4 small focused helpers + composition wired into `Vnc__AWS__Client.setup()`. `Vnc__SG__Helper` (port **443** — nginx TLS; KasmVNC 3000 stays SSM-only, mitmproxy 8080 is loopback-only), `Vnc__AMI__Helper` (filtered by `sg:purpose=vnc`), `Vnc__Instance__Helper` (list/find/terminate), `Vnc__Tags__Builder` (**7 tags** — adds `sg:interceptor` with N5 values: `'none'` / `'name:{ex}'` / `'inline'`; source itself never tagged). 26 new tests via real `_Fake_Boto_EC2` subclasses (no mocks). | `2026-04-29__playwright-stack-split__phase-B__step-7c__sp-vnc-aws-helpers.md` |
| 2026-04-29 | 3e6803b                    | **Playwright stack split — Phase B · Step 7b: `sp vnc` schemas + collections** — 7 schemas (`Schema__Vnc__Interceptor__Choice`, `Stack__Create__Request`/`Response`, `Info`, `List`, `Delete__Response`, `Health`, `Mitm__Flow__Summary`) + 2 collections + new primitive `Safe_Str__Vnc__Interceptor__Source` (32 KB, preserves indentation — `osbot-utils.Safe_Str__Code__Snippet` capped at 1024 too small for real interceptors) + new enum `Enum__Vnc__Interceptor__Kind` (NONE / NAME / INLINE). N5 three-shape Interceptor__Choice exercised by tests (defaults to NONE; NAME carries example name; INLINE preserves raw Python source incl. indentation). Defensive "no password" check on `Stack__Info`. 24 new tests. | `2026-04-29__playwright-stack-split__phase-B__step-7b__sp-vnc-schemas.md` |
| 2026-04-29 | 4c7b1b7                    | **Playwright stack split — Phase B · Step 7a: `sp vnc` foundation** — First slice of the new browser-viewer sister section (chromium + nginx + mitmproxy). Folder `cli/vnc/`; typer command will be `sp vnc` only (per N1 — `sp nvm` was the working title, dropped). Lands `Safe_Str__Vnc__Stack__Name`, `Safe_Str__IP__Address` (local copy), `Safe_Str__Vnc__Password` (URL-safe base64, used for **both** nginx Basic auth and `MITM_PROXYAUTH`), `Enum__Vnc__Stack__State`, and `Vnc__AWS__Client` skeleton with `VNC_NAMING = Stack__Naming(section_prefix='vnc')` + 7 tag constants (incl. `sg:interceptor` for N5 default-off interceptor selection). Regex + lifecycle parity with elastic + os + prom locked by test. 24 new tests. Helper slots wired in step 7c. | `2026-04-29__playwright-stack-split__phase-B__step-7a__sp-vnc-foundation.md` |
| 2026-04-28 | c81a1b1                    | **Playwright stack split — Phase B · Step 6h: `sp prom` / `sp prometheus` typer commands (Tier-2A) — sp prom complete** — 5 commands (`create` / `list` / `info` / `delete` / `health`) wired into the main `sp` app via `add_typer` in `scripts/provision_ec2.py` (`sp prometheus` long, `sp prom` short alias hidden — same convention as `sp os` / `sp opensearch`). Two small focused files: `cli/prometheus/cli/Renderers.py` (~95 lines, pure Tier-2A Rich renderers — **no password rendered anywhere** per P1) and `scripts/prometheus.py` (~80 lines, thin typer wrappers). 9 new tests via `typer.testing.CliRunner` + Rich Console-capture. **Phase B step 6 (`sp prom`) is now functionally complete end-to-end.** | `2026-04-28__playwright-stack-split__phase-B__step-6h__sp-prom-typer.md` |
| 2026-04-28 | e4723ea                    | **Playwright stack split — Phase B · Step 6g: `sp prom` FastAPI routes (Tier-2B)** — 5 routes mirroring `Routes__OpenSearch__Stack` under `/prometheus`. Handlers carry zero logic. The `create` handler takes `body: dict` and round-trips via `Schema__Prom__Stack__Create__Request.from_json(body)` — FastAPI/pydantic can't auto-generate a schema for the nested `List__Schema__Prom__Scrape__Target` Type_Safe collection (good failure caught at route-registration time; locked-in escape hatch for future sister sections with nested collections). 9 new tests via real `_Fake_Service(Prometheus__Service)` subclass + FastAPI TestClient (no mocks). 161 / 161 green in `cli/prometheus/`. | `2026-04-28__playwright-stack-split__phase-B__step-6g__sp-prom-routes.md` |
| 2026-04-28 | c0911f7                    | **Playwright stack split — Phase B · Step 6f: `sp prom` user-data + compose + config generator + launch + wire `create_stack`** — Compressed slice; the `sp os` 5f.1 → 5f.4b sequence (5 commits) lands here as one because the `sp prom` surface is simpler (no admin password, no `vm.max_map_count`). Ships `Prometheus__Compose__Template` (3 services on `sg-net`; `--storage.tsdb.retention.time=24h` per P2; **no Dashboards** per P1), `Prometheus__Config__Generator` (renders `prometheus.yml` — baseline cadvisor + node-exporter + appended `Schema__Prom__Scrape__Target` blocks per P3), `Prometheus__User_Data__Builder` (installs Docker via `dnf`, writes both YAMLs in heredocs, runs compose up — **no `vm.max_map_count` bump**), `Prometheus__Launch__Helper` (base64 UserData, `MinCount=MaxCount=1`, `DEFAULT_INSTANCE_TYPE='t3.medium'`), and wires `Prometheus__Service.create_stack(request, creator='')` end-to-end. `Prometheus__AWS__Client.setup()` adds the `launch` slot; `Prometheus__Service.setup()` chain grows from 5 to 8 helpers. 47 new tests; 152 / 152 green in `cli/prometheus/`. **`sp prom` is now functionally complete in pure logic.** | `2026-04-28__playwright-stack-split__phase-B__step-6f__sp-prom-create-stack.md` |
| 2026-04-28 | 2b5b93b                    | **Playwright stack split — Phase B · Step 6e: `sp prom` Service orchestrator (read paths)** — 4 small focused files. `Caller__IP__Detector` (section-local), `Random__Stack__Name__Generator` (vocabulary parity with elastic + opensearch locked by test), `Prometheus__Stack__Mapper` (pure boto3-detail → schema mapper; `prometheus_url = http://<ip>:9090/`; state-enum mapping table), `Prometheus__Service` (Tier-1 orchestrator with `setup()` wiring 5 helpers). Service exposes `list_stacks` / `get_stack_info` / `delete_stack` / `health`. `health` derives `(targets_total, targets_up)` from `data.activeTargets` via a small module-level `_count_targets` helper; falls through to `(-1, -1)` sentinels when the probe returns `{}`. `create_stack` deferred to 6f.4b when user-data is ready. 24 new tests across 4 focused files using real `_Fake_*` subclasses (no mocks). 105 / 105 green in `cli/prometheus/`. | `2026-04-28__playwright-stack-split__phase-B__step-6e__sp-prom-service-read-paths.md` |
| 2026-04-28 | 23f82f8                    | **Playwright stack split — Phase B · Step 6d: `sp prom` HTTP base + probe** — Two small focused HTTP files. `Prometheus__HTTP__Base` (~45 lines) wraps `requests` with `verify=False` default + scoped urllib3 warning suppression + Basic auth seam + new `params` kwarg for `/api/v1/query`. `Prometheus__HTTP__Probe` (~50 lines) ships 3 probes: `prometheus_ready` (True on 2xx of `/-/healthy`), `targets_status` (parsed `/api/v1/targets`; `{}` on failure), `query` (forwards PromQL via `/api/v1/query?query=…` for the future `sp prom query` command). All probes return `False` / `{}` on any failure; caller maps to `-1` sentinels in `Schema__Prom__Health`. 16 new tests via real `_Fake_HTTP` + `_Fake_Response` subclasses (no mocks). | `2026-04-28__playwright-stack-split__phase-B__step-6d__sp-prom-http-base-and-probe.md` |
| 2026-04-28 | 6141cf3                    | **Playwright stack split — Phase B · Step 6c: `sp prom` AWS helpers (small-file discipline)** — 4 small focused helpers + composition wired into `Prometheus__AWS__Client.setup()`. `Prometheus__SG__Helper` (port **9090** — Prometheus' own UI; no nginx because P1 says no UI in this stack), `Prometheus__AMI__Helper` (latest_al2023 + latest_healthy filtered by `sg:purpose=prometheus`), `Prometheus__Instance__Helper` (list/find/terminate filtered by section), `Prometheus__Tags__Builder` (Name uses `PROM_NAMING.aws_name_for_stack` so `prometheus-` prefix never doubles). Each helper ~50 lines, each test ~80 lines, all using real `_Fake_Boto_EC2` subclasses (no mocks). 23 new tests. `Prometheus__AWS__Client` test extended in-place to cover `setup()` wiring. | `2026-04-28__playwright-stack-split__phase-B__step-6c__sp-prom-aws-helpers.md` |
| 2026-04-28 | a1c814c                    | **Playwright stack split — Phase B · Step 6b: `sp prom` schemas + collections** — 7 schemas (`Schema__Prom__Scrape__Target`, `Stack__Create__Request`/`Response`, `Info`, `List`, `Delete__Response`, `Health`) + 3 collections (`List__Schema__Prom__Stack__Info`, `List__Schema__Prom__Scrape__Target`, `List__Str` local copy). Drops `admin_password` (P1: no built-in auth) and `dashboards_url` (P1: no UI); adds `Schema__Prom__Scrape__Target` for the baked-target list (P3). Defensive "no password field" tests on Request/Response/Info. Caught `Safe_Str__Text` stripping `/` from `metrics_path` — switched to `Safe_Str__Url__Path`. 23 new unit tests. | `2026-04-28__playwright-stack-split__phase-B__step-6b__sp-prom-schemas.md` |
| 2026-04-26 | *(pending)*                | **v0.1.96 stack-split — session handover dev-pack** — 9-file dev-pack at `team/claude/debriefs/v0.1.96-handover/` (00 README → 08 resumption prompt) for clean fresh-session continuation. Covers original task, what shipped, patterns + conventions, lessons learned, key commits, the proven sister-section template, remaining phases (B6b–6h, B7, C, D), and the copy-pasteable resumption prompt. Each file 41-97 lines. | `v0.1.96-handover/00__README.md` (entry point) |
| 2026-04-26 | 1a19d3f                    | **Playwright stack split — Phase B · Step 6a: `sp prom` foundation** — First slice of the Prometheus sister section. Folder `cli/prometheus/`; typer aliases `sp prom` + `sp prometheus`. Lands `Safe_Str__Prom__Stack__Name`, `Safe_Str__IP__Address` (local copy), `Enum__Prom__Stack__State`, and `Prometheus__AWS__Client` skeleton with `PROM_NAMING = Stack__Naming(section_prefix='prometheus')` + 6 tag constants. Regex + lifecycle parity with elastic + opensearch locked by test. 19 new tests. Helper slots wired in step 6c. | `2026-04-26__playwright-stack-split__phase-B__step-6a__sp-prom-foundation.md` |
| 2026-04-26 | 6abf20b                    | **Playwright stack split — Phase B · Step 5i: `sp os` / `sp opensearch` typer commands** — 5 commands (create / list / info / delete / health) wired into the main `sp` app via `add_typer`. Two small focused files: `cli/opensearch/cli/Renderers.py` (~85 lines, pure Tier-2A Rich renderers) and `scripts/opensearch.py` (~75 lines, thin typer wrappers). 9 new tests via Rich Console-capture + typer.testing.CliRunner. **Phase B `sp os` is now functionally complete end-to-end.** | `2026-04-26__playwright-stack-split__phase-B__step-5i__sp-os-typer.md` |
| 2026-04-26 | aef4018                    | **Playwright stack split — Phase B · Step 5h: `Routes__OpenSearch__Stack` (FastAPI surface)** — 5 routes mirroring the Routes__Ec2__Playwright pattern. Each handler is a thin `service` call + `.json()` — zero logic. POST /opensearch/stack, GET /opensearch/stacks, GET/DELETE /opensearch/stack/{name} (404 on miss), GET /opensearch/stack/{name}/health. 9 new tests via FastAPI TestClient with a real `_Fake_Service` subclass (no mocks). | `2026-04-26__playwright-stack-split__phase-B__step-5h__opensearch-routes.md` |
| 2026-04-26 | 2b21126                    | **Playwright stack split — Phase B · Step 5f.4b: `create_stack` wired into `OpenSearch__Service`** — End-to-end create flow working in pure logic. Service composes name_gen + ip_detector + ami helper + sg helper + tags builder + compose template + user_data builder + launch helper. Default resolution: random `os-{adj}-{sci}` name, caller IP via detector, `secrets.token_urlsafe(24)` password (32-char URL-safe base64), latest AL2023 AMI, default region/instance type. Secret only flows via compose; user-data builder does not take admin_password. 6 new tests with real `_Fake_*` subclasses. setup() chains 7 helpers; AWS client wires 5. | `2026-04-26__playwright-stack-split__phase-B__step-5f4b__create-stack-wired.md` |
| 2026-04-26 | 0a09731                    | **Playwright stack split — Phase B · Step 5f.4a: `OpenSearch__Launch__Helper`** — Single-purpose `run_instance(region, ami_id, sg_id, user_data, tags, instance_type, instance_profile_name?)`. Base64-encodes UserData (AWS rejects raw); `MinCount=MaxCount=1` (single-node OS stack); IamInstanceProfile attached only when set. DEFAULT_INSTANCE_TYPE=`t3.large`. 11 new tests with a real `_Fake_Boto_EC2` subclass — happy path / SG / tags / profile attached / pinned counts / empty response raises / boto failure propagates. | `2026-04-26__playwright-stack-split__phase-B__step-5f4a__launch-helper.md` |
| 2026-04-26 | 06bf140                    | **Playwright stack split — Phase B · Step 5f.3: expand user-data with Docker install + compose-up** — `USER_DATA_TEMPLATE` grew into a working AL2023 boot script: dnf install Docker, install docker compose plugin, write compose YAML to `/opt/sg-opensearch/docker-compose.yml` via heredoc, bump `vm.max_map_count` to 262144 (runtime + persistent — required for OS 2.x), `docker compose up -d`. `render()` signature now takes `compose_yaml` (admin_password lives only inside it — secrets in one place). PLACEHOLDERS expanded; defensive test asserts no `admin_password` leak in user-data. 13 tests (was 6). | `2026-04-26__playwright-stack-split__phase-B__step-5f3__user-data-install-steps.md` |
| 2026-04-26 | 8658520                    | **Playwright stack split — Phase B · Step 5f.2: `OpenSearch__Compose__Template`** — Single-purpose `docker-compose.yml` renderer for single-node OpenSearch + Dashboards. `OS_IMAGE`/`DASHBOARDS_IMAGE` use moving `:latest` per plan doc 4 OS1; tests pin a known version. Invariants locked by test: `bootstrap.memory_lock=true` + memlock ulimits (OS 2.x requirement), `sg-net` bridge, container names `sg-opensearch` + `sg-opensearch-dashboards`, dashboards `depends_on: [opensearch]`, `opensearch-data` named volume. 9 tests; PLACEHOLDERS = (os_image, dashboards_image, admin_password, heap_size). | `2026-04-26__playwright-stack-split__phase-B__step-5f2__compose-template.md` |
| 2026-04-26 | 363341c                    | **Playwright stack split — Phase B · Step 5f.1: `OpenSearch__User_Data__Builder` skeleton** — Smallest atomic step toward the user-data flow. Ships ONLY the rendering contract — a tight bash scaffold with placeholders for stack_name / admin_password / region — so subsequent install-step slices grow the template in isolation. PLACEHOLDERS tuple locked by test against every `{key}` in template. 6 tests (placeholders substituted, no leftover, shebang, `set -euo pipefail`, canonical log path, PLACEHOLDERS contract). | `2026-04-26__playwright-stack-split__phase-B__step-5f1__user-data-skeleton.md` |
| 2026-04-26 | 82afd0e                    | **Playwright stack split — Phase B · Step 5e: `sp os` Service orchestrator (read paths)** — 5 small focused files (~25-70 lines each): `Caller__IP__Detector` (section-local), `Random__Stack__Name__Generator` (vocabulary parity with elastic), `OpenSearch__Stack__Mapper` (pure boto3-detail → schema mapper with state-enum mapping table), `OpenSearch__Service` (Tier-1 orchestrator with `setup()` wiring 5 helpers). Service exposes `list_stacks`/`get_stack_info`/`delete_stack`/`health`. `create_stack` deliberately deferred to 5f when user-data is ready. 33 new tests across 4 focused files using real `_Fake_*` subclasses (no mocks). 176 tests pass across all my work areas. | `2026-04-26__playwright-stack-split__phase-B__step-5e__sp-os-service-read-paths.md` |
| 2026-04-26 | 05c0bb7                    | **Playwright stack split — Phase B · Step 5d: `sp os` HTTP base + probe** — Two small focused HTTP files (one class each). `OpenSearch__HTTP__Base` (~40 lines) wraps `requests` with `verify=False` + Basic auth + scoped urllib3 warning suppression. `OpenSearch__HTTP__Probe` (~35 lines) does `cluster_health()` (returns `{}` on any failure; populates `-1` sentinels) and `dashboards_ready()` (True on 2xx). 14 new tests covering happy / non-200 / network / non-JSON / 2xx range / Basic-auth forwarding paths via real `_Fake_HTTP` + `_Fake_Response` subclasses (no mocks). Index/bulk/saved-object ops deferred to their own helpers when needed. | `2026-04-26__playwright-stack-split__phase-B__step-5d__sp-os-http-base-and-probe.md` |
| 2026-04-26 | f5dcde7                    | **Playwright stack split — Phase B · Step 5c: `sp os` AWS helpers (small-file discipline)** — Per operator request to keep new files small. The Elastic equivalent is one ~470-line file with ~20 methods; this slice splits the OpenSearch AWS surface into 4 focused helpers, each with its own ~80-line test file: `OpenSearch__SG__Helper`, `OpenSearch__AMI__Helper`, `OpenSearch__Instance__Helper`, `OpenSearch__Tags__Builder`. `OpenSearch__AWS__Client` becomes a composition shell wired by `setup()`. Each helper has its own `_Fake_Boto_EC2` subclass (real classes, no mocks). 22 new tests covering ensure-SG (create/reuse/duplicate-ingress swallowed), AMI lookups (latest_al2023 / latest_healthy with ordering + empty-when-none), instance list-by-purpose-and-state / find-by-stack-name / terminate, and tag-spec construction (Name prefix never doubled, creator-empty fallback). | `2026-04-26__playwright-stack-split__phase-B__step-5c__sp-os-aws-helpers.md` |
| 2026-04-26 | 9a1e04e                    | **Playwright stack split — Phase B · Step 5b: `sp os` schemas + collections** — 6 schemas (`Schema__OS__Stack__Create__Request`/`Response`, `Info`, `List`, `Delete__Response`, `Health`), the `List__Schema__OS__Stack__Info` collection, and two primitives (`Safe_Str__OS__Password`, plus a local `Safe_Str__IP__Address` copy so sister sections stay self-contained). Defensive test locks the `Info` schema against ever leaking `admin_password`. 19 new unit tests, all green. | `2026-04-26__playwright-stack-split__phase-B__step-5b__sp-os-schemas.md` |
| 2026-04-26 | be6d4dd                    | **Playwright stack split — Phase B · Step 5a: `sp os` foundation** — First slice of the OpenSearch sister section. New `cli/opensearch/` package (folder named `opensearch` not `os` because `os` shadows the Python stdlib — caught immediately by 175 unit-test failures, recorded in plan doc 4). Lands `Safe_Str__OS__Stack__Name`, `Enum__OS__Stack__State`, and an `OpenSearch__AWS__Client` skeleton declaring `OS_NAMING = Stack__Naming(section_prefix='opensearch')` + 6 tag constants. 23 new unit tests including parity checks against the elastic equivalents. AWS-touching methods land in step 5c. | `2026-04-26__playwright-stack-split__phase-B__step-5a__sp-os-foundation.md` |
| 2026-04-26 | 14cdc51                    | **Playwright stack split — Phase A · Step 4: `DELETE /ec2/playwright/delete-all` route + Phase A complete** — Adds the matching HTTP route for `sp delete --all` (the only CLI op with a service method that lacked a route). `Ec2__Service__In_Memory` test fixture extended with `delete_all_instances()`. Other typer commands (connect/shell/exec/forward/screenshot/wait/bake-ami/etc.) are interactive or out of scope and were not added. **Phase A done.** Foundation work for Phase B (sister sections) is now in place: `Stack__Naming`, `Image__Build__Service`, `Ec2__AWS__Client` (full AWS boundary), reduced typer wrappers for `list`/`info`/`delete`, and `delete-all` available via both CLI and HTTP. | `2026-04-26__playwright-stack-split__phase-A__step-4__delete-all-route.md` |
| 2026-04-26 | cde60c5                    | **Playwright stack split — Phase A · Step 3f: typer `cmd_list`/`cmd_info`/`cmd_delete` reduced to thin wrappers** — `Schema__Ec2__Instance__Info` gained `instance_type` so the cmd_list table builds off the schema. `Ec2__Service` gained `delete_all_instances()`. `cmd_info` body shrinks from ~60 lines to ~10 with rendering moved to `_render_info()` (Tier 2A). `cmd_delete` and `cmd_list` now call the service for instance data. New shared `_resolve_typer_target()` helper for the "auto-pick when only one instance" UX. 2 new unit tests with a real `Ec2__AWS__Client` subclass (no mocks). 0 behavioural change. Closes Phase A step 3 (3a/3b/3c/3d/3f). | `2026-04-26__playwright-stack-split__phase-A__step-3f__typer-list-info-delete-wrappers.md` |
| 2026-04-26 | 166541e                    | **Playwright stack split — Phase A · Step 3d: SG + AMI helpers** — Adds the 9 SG/AMI constants and 6 methods (`ensure_security_group`, `latest_al2023_ami_id`, `create_ami`, `wait_ami_available`, `tag_ami`, `latest_healthy_ami`) to `Ec2__AWS__Client`. Caught a real bug — `SG__DESCRIPTION` had an em-dash that AWS rejects as non-ASCII; replaced with a hyphen (matches Elastic precedent). 14 new unit tests with an in-memory `_Fake_Boto3_Client` covering the AMI lifecycle paths. 0 behavioural change to the success path. | `2026-04-26__playwright-stack-split__phase-A__step-3d__sg-and-ami-helpers.md` |
| 2026-04-26 | 5fb8e45                    | **Playwright stack split — Phase A · Step 3c: IAM helpers + constants** — Adds the 8 IAM constants + 3 functions (`decode_aws_auth_error`, `ensure_caller_passrole`, `ensure_instance_profile`) to `cli/ec2/service/Ec2__AWS__Client.py`. `_print_auth_error` (Tier 2A Console formatter) stays in `provision_ec2.py`; `ensure_caller_passrole` no longer prints on auth failure (raises only — typer command formats). 7 new unit tests covering constant shape + the decode short-circuit / swallow-on-failure paths. 0 behavioural change to the success path. | `2026-04-26__playwright-stack-split__phase-A__step-3c__iam-helpers.md` |
| 2026-04-26 | 6ab825d                    | **Playwright stack split — Phase A · Step 3b: AWS context accessors** — Adds `aws_account_id`, `aws_region`, `ecr_registry_host`, `default_playwright_image_uri`, `default_sidecar_image_uri` (plus the two IMAGE_NAME re-exports) to `cli/ec2/service/Ec2__AWS__Client.py`. Removes the 5 definitions from `provision_ec2.py`; alias-imports keep typer commands working. `Ec2__Service.create()` import block shrinks from 7 → 2. 4 new unit tests with module-level monkey-patching (no mocks). 0 behavioural change. | `2026-04-26__playwright-stack-split__phase-A__step-3b__aws-context-accessors.md` |
| 2026-04-26 | 68a6c85                    | **Playwright stack split — Phase A · Step 3a: `Ec2__AWS__Client` (naming + lookup helpers)** — First sub-slice of step 3 (broken into 3a/3b/3c/3d/3f to keep each commit reviewable). Mirrors the `Elastic__AWS__Client` pattern: a new module at `cli/ec2/service/Ec2__AWS__Client.py` hosts module-level pure helpers (`random_deploy_name`, `get_creator`, `uptime_str`, `instance_tag`, `instance_deploy_name`) plus a Type_Safe class with the AWS-touching methods (`find_instances`, `find_instance_ids`, `resolve_instance_id`, `terminate_instances`). `provision_ec2.py` keeps thin wrapper functions matching the old signatures (the `ec2: EC2 = None` parameter is now ignored — wrappers go away in 3f when typer commands become thin wrappers). `Ec2__Service` no longer imports lookup helpers from `scripts.provision_ec2`; uses the new client via an `aws_client()` seam. 24 new unit tests with an in-memory `_Fake_EC2` (real subclass, no mocks). 0 behavioural change. | `2026-04-26__playwright-stack-split__phase-A__step-3a__ec2-aws-client-helpers.md` |
| 2026-04-26 | 0162e93                    | **Playwright stack split — Phase A · Step 2: `Image__Build__Service`** — Shared Docker image build pipeline. New `cli/image/` module with 5 Type_Safe schemas + 2 collections + 1 service. `Schema__Image__Build__Request` carries `image_folder` + `image_tag` + an ordered list of `Schema__Image__Stage__Item` (file or tree, with per-item `extra_ignore_names`); `Schema__Image__Build__Result` returns `image_id` / `image_tags` / `duration_ms`. The service has two seams: `stage_build_context()` (pure I/O, fully unit-testable) and `build()` (invokes the docker SDK directly, bypassing osbot-docker's `@catch` wrapper so real build failures propagate). Both existing builders (`Build__Docker__SGraph_AI__Service__Playwright` for the Playwright EC2 image and `Docker__SP__CLI` for the SP CLI Lambda image) reduce to thin composers that build a request and delegate. Removed: ~70% duplication (tempdir staging, ignore-noise filter, direct docker SDK call, tempdir cleanup) plus the now-redundant `_ignore_build_noise` / `ignore_build_noise` helpers. 15 new unit tests, 1 in-memory fake docker client (real class, no mocks). 0 behavioural change. | `2026-04-26__playwright-stack-split__phase-A__step-2__image-build-service.md` |
| 2026-04-26 | 4ee540c                    | **Playwright stack split — Phase A · Step 1: `Stack__Naming`** — First code change of the v0.1.96 stack-split plan. Promotes the elastic-only `aws_name_for_stack()` and `sg_name_for_stack()` helpers to a shared `Type_Safe` class at `sgraph_ai_service_playwright__cli/aws/Stack__Naming.py`. Each section binds a `section_prefix` (`elastic` / `opensearch` / `prometheus` / `vnc`) once and the methods enforce CLAUDE.md rules #14 (SG GroupName must not start with `sg-`) and #15 (Name tag must not be double-prefixed). 9 new unit tests, 5 elastic call sites switched to `ELASTIC_NAMING.*`. 0 behavioural change. | `2026-04-26__playwright-stack-split__phase-A__step-1__stack-naming.md` |
| 2026-04-26 | 2e1cf69 → 38f61f0 *(7 commits)* | **`sp el lets cf events` (LETS slice 2)** — Content-reading sibling of slice 1. CLI: `load` / `wipe` / `list` / `health` under `sp el lets cf events`. Fetches each `.gz` via `s3:GetObject`, gunzips, parses TSV into 38-field `Schema__CF__Event__Record` (incl. URL-decoded UA, status class derivation, bot classification via 28 named-bot regex + 5 generic word-bounded indicators, cache_hit flag), groups records by event timestamp date for daily-rolling `sg-cf-events-{YYYY-MM-DD}` indices, bulk-posts with `_id = "{etag}__{line_index}"` for per-event idempotency. After each successful file, flips slice 1's inventory `content_processed=true` via `_update_by_query` (Painless). `--from-inventory` mode reads the manifest as the work queue → daily incremental refresh. Auto-imports 6-panel "CloudFront Logs - Events Overview" dashboard (Vis Editor). Matched-pair wipe also resets the manifest. 201 unit tests, zero mocks. Side-effect surface: zero existing files modified (additive to `scripts/elastic_lets.py` only). Real-world validated: 565 events from 50 .gz files in 12s; wpbot/Googlebot correctly classified BOT_KNOWN; workspace.sgraph.ai users correctly classified HUMAN. | 3-part: `2026-04-26__lets-cf-events__01-why.md` / `__02-what.md` / `__03-how-to-use.md` |
| 2026-04-26 | 88b213f → f34ed56 *(14 commits)* | **`sp el lets cf inventory` (LETS slice 1)** — First LETS-shaped vertical pass on top of the Ephemeral Kibana. CLI: `load` / `wipe` / `list` / `health` under `sp el lets cf inventory`. Lists CloudFront real-time S3 metadata (no `.gz` content reads), parses Firehose-embedded timestamps from filenames, indexes into daily `sg-cf-inventory-{YYYY-MM-DD}` indices keyed on `delivery_at` with `_id = etag` for idempotent re-loads. Auto-imports a 5-panel "CloudFront Logs - Inventory Overview" dashboard (Vis Editor, NOT Lens — migration-footgun hardened). Matched-pair wipe is idempotent. 150 unit tests, zero mocks. End-to-end LETS round trip proven: stack-A export → stack-B import → both auto-imported and hand-built Lens dashboards re-bind to the same data view by title-match. Side-effect surface: +2 lines in `scripts/elastic.py` for the Typer mount. | 3-part: `2026-04-26__lets-cf-inventory__01-why.md` / `__02-what.md` / `__03-how-to-use.md` |
| 2026-04-25 | 501f12b → d9346b6 *(23 commits)* | **`sp el` (Ephemeral Kibana)** — Single-command lifecycle for an ephemeral Elasticsearch + Kibana + nginx-TLS stack on EC2. Cold path 158s, AMI fast-launch 80s. CLI: lifecycle (create/wait/info/list/delete/connect/exec/health/harden), data (seed/wipe), saved-objects (dashboard, data-view), AMI (list/create/delete/wait), one-shot create-from-ami. Auto-creates Kibana data view + 4-panel "Synthetic Logs Overview" dashboard from synthetic seed data. Boot-time harden script hides Observability/Security/Fleet/ML side-nav groups via Spaces API (baked into AMI). 165 unit tests, no mocks. | 3-part: `2026-04-25__sp-elastic-kibana__01-why.md` / `__02-what.md` / `__03-how-to-use.md` |
| 2026-04-24 | 90904f9, e6b4305, 5c2e6ec, f5170bd | **v0.1.72 OpenSearch index pattern** — `sp ob create` now creates `sg-playwright-logs` index pattern automatically. Root causes: (1) `POST /api/data_views/data_view` silently no-ops on AOS; (2) verify checked status not body; (3) IAM/SigV4 cannot write saved objects on FGAC domains; (4) `fields` key in body causes AOS 400; (5) `&security_tenant=` in query string AND header simultaneously causes 400. Fix: single path — `POST /api/saved_objects/index-pattern/{id}?overwrite=true` with basic auth + header-only tenant + no fields key. Deleted `_sys_idx_write`, `_sys_idx_write_sigv4`, `_DV_TRIES`, `_fetch_index_fields`. | `v0.1.72__opensearch-index-pattern.md` |
| 2026-04-21 | 2e44d54, 9f4cba7, f2eeb02, 112ffab | **v0.1.49 Performance Fix** — Eliminated 28-30s per-request latency. Two compounding bugs: (1) `browser.close()` / `playwright.stop()` blocked on Chromium's keep-alive drain (mitmproxy holds connections open) — fixed by SIGKILL Node.js subprocess + Chromium children via `/proc` PID scan; (2) Playwright sync API raised inside Lambda's asyncio event loop — fixed by `_run_sequence()` wrapper that dispatches to a clean `ThreadPoolExecutor` thread when a loop is detected. Also: CI `ec2-image` filter added (EC2 pulls ECR directly, needs rebuild on any Python source change); AWS Public ECR auth added to CI (avoids pull rate-limit). Result: avg cold=1065ms, avg warm=1069ms across 4 URLs. | `2026-04-21__v0.1.49-perf-fix-browser-close-asyncio.md` |
| 2026-04-20 | *(pending)* | **v0.1.46 Observability P1+P2** — P1: `GET /metrics` (Prometheus text) on both Playwright + agent_mitmproxy services; 6 `sg_playwright_*` + 4 `sg_mitmproxy_*` metrics with isolated `CollectorRegistry`. P2: `provision_ec2.py` extended with 8-service observability compose stack (prometheus, grafana, cadvisor, node-exporter, loki, promtail, cloudwatch-agent, xray-daemon), config file injection via user-data, `IAM__OBSERVABILITY_POLICY_ARNS`, 3 new CLI commands. 4 Grafana dashboard JSONs. AWS setup runbook. | `2026-04-20__v0.1.46-observability-p1-p2.md` |
| 2026-04-20 | 721335f (P1), 005127b (P2) | **Proxy gateway P1+P2** — P1: agent_mitmproxy upstream mode (3 new env vars, wrapper-script pattern to avoid supervisord interpolation crash, `proxy_mode` on `/health/info`). P2: Playwright proxy API cleanup — delete `Proxy__Auth__Binder` (CDP Fetch workaround), `Schema__Proxy__Config`, `Schema__Proxy__Auth__Basic`; proxy is now boot-time env (`SG_PLAYWRIGHT__DEFAULT_PROXY_URL`), not per-request API param. 188 unit tests green. | `2026-04-20__proxy-gateway-p1-p2.md` |
| 2026-04-19 | 0bdecab, 298f9e1, 450a8d5, dfee003, 69f0d4e, e094adc | **v0.1.29 — First-pass agentic refactor.** Env-var rename `SG_PLAYWRIGHT__* → AGENTIC_*`; new L1/L2 subpackages `agentic_fastapi/` + `agentic_fastapi_aws/`; one-command `scripts/deploy_code.py` (package → S3 → Lambda env → smoke); 8 read-only `/admin/*` endpoints (Agentic_Admin_API); SKILL files + `capabilities.json` stub; single-track CI with new `deploy-code` job. Public HTTP surface (10) + service classes (10) unchanged. Hotfix `e094adc` — swap raw boto3 for AWS_Config after first CI deploy-code run tripped `s3.region_name()` AttributeError. 390 unit tests green. | `2026-04-19__v0.1.29-first-pass-agentic-refactor.md` |
| 2026-04-17 | *(pending)*                | **v0.1.24 — Remove stateful session API, repurpose `/browser/*` as stateless one-shots.** Deletes `/session/*`, `/quick/*`, `Session__Manager`, `Action__Runner`; reimplements `/browser/*` as 6 stateless one-shot endpoints each accepting optional `browser_config` (incl. proxy); drops `session_id` + `close_session_after` from `Schema__Sequence__Request`. Endpoint count 25 → 10. Lambda-safe + security-safe by construction. | `2026-04-17__v0.1.24-remove-stateful-session-api.md` |
| 2026-04-17 | deb5c79                    | **QA session** — authenticated-proxy testing against dev Lambda; produced `qa-findings__proxy-testing.md` with P1 Bug #1 (CDP Fetch needed for proxy auth) + P1 Bug #4 (sync/async Playwright poisoning) + P3 Bug #3 (session leak on CF 504) + schema redesign proposal (`Schema__Proxy__Auth__Basic`). Drove the v0.1.14 reliability slice (deadline + watchdog). | `2026-04-17__qa-proxy-testing-findings.md` |
| 2026-04-17 | 906ba05 (preceded by 8922484, cdba60c, 22dd98a, 6869632) | **Milestone** — 100%-clean-state per call (per-call sync_playwright + Chromium, register/try-finally teardown) + per-phase timing breakdown surfaced (`Schema__Sequence__Timings`; JSON on `/quick/html` + `/sequence/execute`, `X-*-Ms` headers on `/quick/screenshot`); production-verified at ~800–1200 ms total | `2026-04-17__milestone-clean-state-per-call.md` |
| 2026-04-17 | 0f24354                    | Phase 2.9 — Step__Executor (NAVIGATE / CLICK / FILL / SCREENSHOT / GET_CONTENT / GET_URL; 10 deferred stubs) + Artefact__Writer.capture_* + CI env-var hotfix for integration job | `2026-04-17__phase-2.9-step-executor.md` |
| 2026-04-17 | 5184011                    | Phase 2.8 — Docker infra (Base + Build + ECR + Lambda + Local) + deploy-via-pytest (ECR push, local container, deploy to dev, smoke dev) | `2026-04-17__phase-2.8-docker-infra.md` |
| 2026-04-17 | 29be9d9                    | Phase 2.7 — FastAPI wiring (Playwright__Service orchestrator + Routes__Health + Fast_API__Playwright__Service; chassis-before-engine reorder) | `2026-04-17__phase-2.7-fastapi-wiring.md` |
| 2026-04-17 | 4ff12b2                    | Phase 2.6 — Browser__Launcher (real Chromium + env-var escape hatch; 5 integration tests live) | `2026-04-17__phase-2.6-browser-launcher.md` |
| 2026-04-17 | d4d8a53                    | Phase 2.5 — Credentials__Loader (vault ↔ browser-context glue; full spec source) | `2026-04-17__phase-2.5-credentials-loader.md` |
| 2026-04-17 | 4794f02                    | Phase 2.4 — Artefact__Writer (sink routing + vault JSON seams; capture_* deferred) | `2026-04-17__phase-2.4-artefact-writer.md` |
| 2026-04-16 | f6a4cfb                    | Phase 2.3 — Sequence__Dispatcher (parse surface; execute_step deferred) | `2026-04-16__phase-2.3-sequence-dispatcher.md` |
| 2026-04-16 | b9bbce3                    | Phase 2.2 — Session__Manager (v2 §4 callsites; v1 not in pack) | `2026-04-16__phase-2.2-session-manager.md` |
| 2026-04-16 | 571fd77                    | Phase 2.1 — Capability__Detector + ENV_VAR constants (spec §4.6, §7) | `2026-04-16__phase-2.1-capability-detector.md` |
| 2026-04-16 | 64dc249                    | Phase 1.4 — Request__Validator + JS__Expression__Allowlist (spec §9) | `2026-04-16__phase-1.4-request-validator.md` |
| 2026-04-16 | 4c63685                    | Phase 1.3 complete — §5.5–§5.10 + §6 + §8 + tests | `2026-04-16__phase-1.3-complete.md` |
| 2026-04-16 | 1f2edd6                    | Refactor — one-class-per-file (primitives, enums, schemas) | `2026-04-16__refactor-one-class-per-file.md` |
| 2026-04-16 | 8cb083c                    | CI fix — pytest exit-code-5 on placeholder jobs | `2026-04-16__ci-fix-exit-code-5.md` |
| 2026-04-16 | f219b01                    | CI fix — pin Playwright base image tag         | `2026-04-16__ci-fix-playwright-tag.md` |
| 2026-04-16 | 5ab4dc2, 68114e0           | CI fix — docker/deploy test placeholders + pytest install | `2026-04-16__ci-fix-docker-deploy-jobs.md` |
| 2026-04-16 | 0fa0f7f                    | Phase 1.3 (WIP) — core schemas §5.1–§5.4 + Safe_Str__Host | `2026-04-16__phase-1.3-wip-core-schemas.md` |
| 2026-04-16 | 3585724                    | Phase 1.1 + 1.2 — Safe_* primitives + Enum__* types | `2026-04-16__phase-1.1-1.2-primitives-enums.md` |
| 2026-04-16 | 08641a9                    | Phase 0 — bootstrap skeleton                    | `2026-04-16__phase-0-bootstrap.md` |
