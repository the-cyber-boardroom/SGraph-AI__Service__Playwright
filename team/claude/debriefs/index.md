# Debriefs — Index

Chronological list of implementation debriefs. Most recent first.

| Date       | Commit(s)                  | Slice                                           | File |
|------------|----------------------------|-------------------------------------------------|------|
| 2026-05-02 | 77505a0                    | **B4 — Fast_API__Compute control plane** — `sg_compute/control_plane/` with `Fast_API__Compute` (auto-discovers and mounts per-spec route classes via `Spec__Routes__Loader` convention), `Routes__Compute__Health` (`/api/health`, `/api/health/ready`), `Routes__Compute__Specs` (`/api/specs`, `/api/specs/{spec_id}`), `Routes__Compute__Nodes` + `Routes__Compute__Stacks` (placeholders). Mounts `Routes__Docker__Stack` + `Routes__Podman__Stack` under `/api/specs/{spec_id}/*` automatically. 27 new tests, 169 total. Good failures: package vs class-file import in loader, `Safe_Str__Fast_API__Route__Prefix` path type in set membership, `raise_server_exceptions=False` for AWS-credential path. | `2026-05-02__b4__fast-api-compute-control-plane.md` |
| 2026-05-02 | d654425, b3e94d0, 54524ab, 3dd231b | **B3.4–B3.7 — sg_compute_specs migration: Prometheus, OpenSearch, Elastic, Firefox** — Four spec packages migrated from `sgraph_ai_service_playwright__cli/` into `sg_compute_specs/`. Each follows: enums + primitives + schemas + collections + service helpers (tags builder, stack mapper, HTTP probe, interceptor resolver) + FastAPI routes + manifest + tests. Key decisions: Elastic uses monolithic AWS client (CLI fidelity); Firefox routes use `body: dict + from_json()` for nested interceptor choice; `Enum__Firefox__Stack__State` upgraded to `(str, Enum)` for JSON serialization; Firefox `nav_group=BROWSERS` (COMPUTE not in `Enum__Spec__Nav_Group`). 3 good failures surfaced early (COMPUTE enum, 1-char stack name, shell `${{}}` escaping). Total: 139 new tests, all green. | `2026-05-02__b3.4-b3.7__sg-compute-specs-migration.md` |
| 2026-05-02 | *(pending)*                | **Vault paused; settings-bus migrated to localStorage** — `settings-bus.js` now loads/persists settings via `localStorage` key `sp-cli:settings:v2`. Removed vault dependency from `admin.js` (startVaultBus + vault:connected listener) and `sp-cli-settings-view` (isWritable + ro-banner). `vault-bus.js` left intact for future sync-on-connect re-integration. `sp-cli:settings.loaded` now fires synchronously at boot with no network round-trip. | `2026-05-02__vault-pause-localStorage-settings.md` |
| 2026-04-29 | *(pending)*                | **v0.1.97 (3/3) — `sp doctor` typer command — v0.1.97 done** — New top-level `sp doctor` with `passrole` + `preflight` subcommands (and a `@callback(invoke_without_command=True)` so bare `sp doctor` runs both). Reuses `ensure_caller_passrole` / `aws_account_id` / `aws_region` / `ecr_registry_host` from `Ec2__AWS__Client`. **Hard-cut: `sp pw ensure-passrole` deleted entirely** (folded into `sp doctor passrole` per Q3 default). Inline iam:PassRole warning updated to point at `sp doctor passrole`. 4 new tests + extended pw subgroup test. 1011 / 1 skipped passing. **v0.1.97 done — clean top-level shape: 2 commands (catalog, doctor) + 8 subgroups, symmetric across all stack sections.** | `2026-04-29__v0.1.97__03__sp-doctor.md` |
| 2026-04-29 | eb8ff78                    | **v0.1.97 (2/3) — `sp catalog` typer command** — Cross-section enumeration. Mirrors `Routes__Stack__Catalog` HTTP surface but reads directly from `Stack__Catalog__Service` (no FastAPI server). Two commands: `sp catalog types` (lists known stack types incl. linux/docker/elastic/opensearch/vnc) and `sp catalog stacks [--type X]` (live stacks across all sections). `--type` validates against `Enum__Stack__Type`; unknown raises `typer.BadParameter`. New `cli/catalog/cli/Renderers.py` (`render_types` + `render_stacks`) and `scripts/catalog.py`. Note: `list_all_stacks` only iterates linux/docker/elastic today (catalog `Stack__Catalog__Service` doesn't compose OS/Prom/VNC services yet — separate gap, see vnc-handover dev-pack 05). 7 new tests; 1007 passing. | `2026-04-29__v0.1.97__02__sp-catalog.md` |
| 2026-04-29 | a0c1f23                    | **v0.1.97 (1/3) — `sp pw` subgroup; Playwright joins the sister-section convention** — Per the v0.1.97 brief (clean top-level commands). 21 Playwright-EC2-specific commands (`create`, `list`, `info`, `delete`, `connect`, `shell`, `env`, `run`, `exec`, `exec-c`, `logs`, `diagnose`, `forward`, `wait`, `clean`, `create-from-ami`, `open`, `screenshot`, `smoke`, `health`, `ensure-passrole`) moved under `sp pw` (long form `sp playwright`, short alias hidden). The `sp vault` and `sp ami` subgroups also nested under `sp pw`. Top-level `app` help reworded — no longer "the Playwright provisioner". Pure decorator move; function bodies unchanged. Test renames + 1 new `sp pw` subgroup test. 1000 / 1 skipped passing. | `2026-04-29__v0.1.97__01__sp-pw-subgroup.md` |
| 2026-04-29 | 68a3078                    | **Playwright stack split — Phase D (2/2): regroup `vault-*` under `sp vault` + `*-ami` under `sp ami` — v0.1.96 done** — Per plan doc 7 C1 (hard cut). Seven flat `sp vault-{clone,list,run,commit,push,pull,status}` commands moved under `sp vault` subgroup (with `sp v` short alias). Four flat `sp {bake,wait,tag}-ami` + `sp list-amis` commands moved under `sp ami` subgroup (verbs match `sp el ami`: `create / wait / tag / list`). `sp create-from-ami` stays top-level (different action — launches an instance from a baked AMI). Pure decorator move; function bodies + signatures unchanged. 2 new subgroup-help tests; existing `test__app_has_expected_commands` updated. 999 / 1 skipped passing. **All 4 Phase D items shipped — v0.1.96 stack-split refactor is complete (Phases A → D).** | `2026-04-29__playwright-stack-split__phase-D__vault-and-ami-regroup.md` |
| 2026-04-29 | d5b5c26                    | **Playwright stack split — Phase D (1/2): drop forward-* / metrics + add `sp prom metrics <url>`** — Per plan doc 7 (C1 hard cut + C2 metrics move). Deleted `sp forward-prometheus` / `forward-browser` / `forward-dockge` typer commands (their targets moved out of the Playwright EC2 in Phase C). Deleted top-level `sp metrics` (one-off SSM curl; operators can `sp exec` it). Added `sp prom metrics <url> [--key <api-key>]` — URL-based, plain HTTP, works for any `/metrics` endpoint. Dropped orphaned constants: `EC2__BROWSER_INTERNAL_PORT` (in `Ec2__AWS__Client`), `EC2__PROMETHEUS_PORT` / `EC2__BROWSER_IMAGE` / `EC2__DOCKGE_PORT` / `EC2__DOCKGE_IMAGE` (in `provision_ec2.py`). Cleaned `sp open` URL hints (dropped 3 lines), `sp diagnose` port grep (8000/8001 only), `provision()` return dict (no more `browser_url`). 997 tests still passing. | `2026-04-29__playwright-stack-split__phase-D__forward-and-metrics.md` |
| 2026-04-29 | 9af6498                    | **Playwright stack split — Phase C: strip the Playwright EC2 (9 containers → 2)** — Removed 7 compose-fragment constants (`COMPOSE_SVC_BROWSER`/`BROWSER_PROXY`/`CADVISOR`/`NODE_EXPORTER`/`PROMETHEUS`/`FLUENT_BIT`/`DOCKGE`), 7 config-template constants (PROMETHEUS_YML/REMOTE_WRITE/FLUENT_BIT_*/NGINX_BROWSER), and 2 render functions (`render_observability_configs_section`/`render_browser_proxy_section`). `render_compose_yaml` shrank from 11 → 9 params (dropped `amp_remote_write_url`/`opensearch_endpoint` + the 3 conditional branches + DOCKGE service line + `prometheus_data` volume). `render_user_data` shrank from 9 → 6 params. `Ec2__AWS__Client.SG_INGRESS_PORTS` shrank from 3 → 2 ports (dropped `EC2__BROWSER_INTERNAL_PORT`); `SG__DESCRIPTION` updated. `~−270 LoC net` in `provision_ec2.py`. `forward-prometheus` / `forward-browser` / `forward-dockge` typer commands stay broken transitionally — Phase D deletes them. New defensive test `test__no_browser_or_observability_services`; entire `test_render_observability_configs` class deleted. **2-container compose now canonical.** | `2026-04-29__playwright-stack-split__phase-C__strip-playwright-ec2.md` |
| 2026-04-29 | e07c68f                    | **v0.1.101 — MVP Provisioning UI (Admin + User)** — Three backend PRs + static UI. PR-1: linux + docker routes mounted on `Fast_API__SP__CLI` (routes existed, weren't wired); `List__Port` → `List[int]` fix for FastAPI startup. PR-2: new `catalog/` sub-package — `Enum__Stack__Type`, 4 schemas, 2 collections, `Stack__Catalog__Service` (composes Linux/Docker/Elastic), `Routes__Stack__Catalog` (`GET /catalog/types` + `GET /catalog/stacks?type=`), 13 tests. PR-3: `Routes__Elastic__Stack` (5 lifecycle endpoints), 10 tests. Static UI: 4 shared utilities (tokens, api-client, catalog cache, poll loop), 7 Web Components (`sg-api-client`, `sg-auth-panel`, `sg-header`, `sg-toast-host`, `sg-stack-card`, `sg-stack-grid` 4-mode, `sg-create-modal` 3-state), admin dashboard, user provisioning page, landing page, `scripts/ui__serve-locally.sh`. Post-ship: fixed 500 on `/linux/stacks` — `Linux__Service.setup()` must be called explicitly; `aws_client` is lazy-init. Full suite: 1176 passing. | `2026-04-29__v0.1.101__mvp-admin-user-ui.md` |
| 2026-04-29 | 41120fc                    | **Playwright stack split — Phase B · Step 7h: `sp vnc` typer commands (Tier-2A) — sp vnc complete** — **7 commands** (`create` / `list` / `info` / `delete` / `health` / `flows` / `interceptors`) wired into the main `sp` app via `add_typer` in `scripts/provision_ec2.py` (single `sp vnc` name; no short alias per N1). Two extras vs prom: `sp vnc flows <name>` (peek mitmweb flows; per N4 no auto-export) and `sp vnc interceptors` (lists baked example names; no AWS call — reads `list_examples()` directly). N5 mutually-exclusive flags on `create`: `--interceptor <name>` (baked example) or `--interceptor-script <path>` (read local `.py` file → INLINE source); both omitted → NONE default-off; both provided → `typer.BadParameter`. Renderers handle `interceptor_kind=NONE → 'none'` label. 15 new tests via `typer.testing.CliRunner` + Rich Console-capture. **Phase B step 7 (`sp vnc`) is now functionally complete end-to-end.** | `2026-04-29__playwright-stack-split__phase-B__step-7h__sp-vnc-typer.md` |
| 2026-04-29 | 2eb7bb7                    | **Playwright stack split — Phase B · Step 7g: `sp vnc` FastAPI routes (Tier-2B)** — Two route files. `Routes__Vnc__Stack` ships 5 routes mirroring `Routes__Prometheus__Stack` (POST/GET/DELETE under `/vnc`); `create` takes `body: dict` + `from_json` because the request carries a nested `Schema__Vnc__Interceptor__Choice` (same workaround as prom). `Routes__Vnc__Flows` ships one route — `GET /vnc/stack/{name}/flows` returns `{"flows": [...summaries]}` (per N4 no auto-export — just a peek for human debug; envelope keeps it future-proof for total/dropped counts). 13 new tests via real `_Fake_Service(Vnc__Service)` subclass + FastAPI TestClient (no mocks). | `2026-04-29__playwright-stack-split__phase-B__step-7g__sp-vnc-routes.md` |
| 2026-04-29 | fc5f5fc                    | **Playwright stack split — Phase B · Step 7f: `sp vnc` user-data + compose + interceptor resolver + launch + wire `create_stack`** — Compressed slice mirroring sp prom 6f. Ships `Vnc__Compose__Template` (3 services on `sg-net`; chromium uses `HTTPS_PROXY=http://mitmproxy:8080` + `--browser=chromium` per N2; **port 443 only**; **no secrets in YAML** — locked by test), `Vnc__Interceptor__Resolver` (N5: 3 baked example sources inline; `resolve(choice)` → `(source, label)` for NONE/NAME/INLINE; defensive on unknown name + empty inline source), `Vnc__User_Data__Builder` (installs `docker`+`httpd-tools`+`openssl`; writes nginx config + bcrypt htpasswd + self-signed TLS cert + mitmproxy proxyauth file + interceptor source + compose YAML; **operator_password interpolated exactly once** as a shell var, then referenced from there for both htpasswd and proxyauth), `Vnc__Launch__Helper` (`DEFAULT_INSTANCE_TYPE='t3.large'` for chromium RAM headroom). `Vnc__AWS__Client.setup()` adds `launch` slot; `Vnc__Service.setup()` chain grows from 5 to 8 helpers; `create_stack` resolves all defaults + threads N5 source/label through user-data + tags + response. 47 new tests; 161 / 161 green in `cli/vnc/`. **`sp vnc` is now functionally complete in pure logic.** | `2026-04-29__playwright-stack-split__phase-B__step-7f__sp-vnc-create-stack.md` |
| 2026-04-29 | 877791e                    | **Playwright stack split — Phase B · Step 7e: `sp vnc` Service orchestrator (read paths) + flows()** — 4 small focused files mirroring sp prom 6e. `Caller__IP__Detector` (section-local), `Random__Stack__Name__Generator` (vocabulary parity locked), `Vnc__Stack__Mapper` (pure boto3-detail → schema; builds `viewer_url`/`mitmweb_url`; **decodes `sg:interceptor` tag back into N5 (kind, name)**), `Vnc__Service` (Tier-1 orchestrator with `setup()` wiring 5 helpers). Service exposes `list_stacks` / `get_stack_info` / `delete_stack` / `health` (READY only when both `nginx_ok` and `mitmweb_ok`) + new `flows()` op (per N4 — peek mitmweb flows; no auto-export; consumed by `Routes__Vnc__Flows` in 7g). `_flow_summary_from_mitmweb` pure mapper handles missing `pretty_url` / `response` / `timestamp_created` defensively. 25 new tests via real `_Fake_*` subclasses (no mocks). | `2026-04-29__playwright-stack-split__phase-B__step-7e__sp-vnc-service-read-paths.md` |
| 2026-04-29 | 4d5a035                    | **Playwright stack split — Phase B · Step 7d: `sp vnc` HTTP base + probe** — Two small focused HTTP files. `Vnc__HTTP__Base` wraps `requests` with `verify=False` default (nginx self-signed) + Basic auth seam (operator creds front both nginx UI and mitmweb). `Vnc__HTTP__Probe` ships 3 probes: `nginx_ready` (200 on `/`), `mitmweb_ready` (200 on `/api/flows`), `flows_listing` (parsed JSON list from `/api/flows`; `[]` on any failure incl. defensive non-array body check). 15 new tests via real `_Fake_HTTP` + `_Fake_Response` subclasses (no mocks). | `2026-04-29__playwright-stack-split__phase-B__step-7d__sp-vnc-http-base-and-probe.md` |
| 2026-04-29 | 7ae5ad2                    | **Playwright stack split — Phase B · Step 7c: `sp vnc` AWS helpers (small-file discipline)** — 4 small focused helpers + composition wired into `Vnc__AWS__Client.setup()`. `Vnc__SG__Helper` (port **443** — nginx TLS; KasmVNC 3000 stays SSM-only, mitmproxy 8080 is loopback-only), `Vnc__AMI__Helper` (filtered by `sg:purpose=vnc`), `Vnc__Instance__Helper` (list/find/terminate), `Vnc__Tags__Builder` (**7 tags** — adds `sg:interceptor` with N5 values: `'none'` / `'name:{ex}'` / `'inline'`; source itself never tagged). 26 new tests via real `_Fake_Boto_EC2` subclasses (no mocks). | `2026-04-29__playwright-stack-split__phase-B__step-7c__sp-vnc-aws-helpers.md` |
| 2026-04-29 | 3e6803b                    | **Playwright stack split — Phase B · Step 7b: `sp vnc` schemas + collections** — 7 schemas (`Schema__Vnc__Interceptor__Choice`, `Stack__Create__Request`/`Response`, `Info`, `List`, `Delete__Response`, `Health`, `Mitm__Flow__Summary`) + 2 collections + new primitive `Safe_Str__Vnc__Interceptor__Source` (32 KB, preserves indentation — `osbot-utils.Safe_Str__Code__Snippet` capped at 1024 too small for real interceptors) + new enum `Enum__Vnc__Interceptor__Kind` (NONE / NAME / INLINE). N5 three-shape Interceptor__Choice exercised by tests (defaults to NONE; NAME carries example name; INLINE preserves raw Python source incl. indentation). Defensive "no password" check on `Stack__Info`. 24 new tests. | `2026-04-29__playwright-stack-split__phase-B__step-7b__sp-vnc-schemas.md` |
| 2026-04-29 | 4c7b1b7                    | **Playwright stack split — Phase B · Step 7a: `sp vnc` foundation** — First slice of the new browser-viewer sister section (chromium + nginx + mitmproxy). Folder `cli/vnc/`; typer command will be `sp vnc` only (per N1 — `sp nvm` was the working title, dropped). Lands `Safe_Str__Vnc__Stack__Name`, `Safe_Str__IP__Address` (local copy), `Safe_Str__Vnc__Password` (URL-safe base64, used for **both** nginx Basic auth and `MITM_PROXYAUTH`), `Enum__Vnc__Stack__State`, and `Vnc__AWS__Client` skeleton with `VNC_NAMING = Stack__Naming(section_prefix='vnc')` + 7 tag constants (incl. `sg:interceptor` for N5 default-off interceptor selection). Regex + lifecycle parity with elastic + os + prom locked by test. 24 new tests. Helper slots wired in step 7c. | `2026-04-29__playwright-stack-split__phase-B__step-7a__sp-vnc-foundation.md` |
| 2026-04-28 | c81a1b1                    | **Playwright stack split — Phase B · Step 6h: `sp prom` / `sp prometheus` typer commands (Tier-2A) — sp prom complete** — 5 commands (`create` / `list` / `info` / `delete` / `health`) wired into the main `sp` app via `add_typer` in `scripts/provision_ec2.py` (`sp prometheus` long, `sp prom` short alias hidden — same convention as `sp os` / `sp opensearch`). Two small focused files: `cli/prometheus/cli/Renderers.py` (~95 lines, pure Tier-2A Rich renderers — **no password rendered anywhere** per P1) and `scripts/prometheus.py` (~80 lines, thin typer wrappers). 9 new tests via `typer.testing.CliRunner` + Rich Console-capture. **Phase B step 6 (`sp prom`) is now functionally complete end-to-end.** | `2026-04-28__playwright-stack-split__phase-B__step-6h__sp-prom-typer.md` |
| 2026-04-28 | e4723ea                    | **Playwright stack split — Phase B · Step 6g: `sp prom` FastAPI routes (Tier-2B)** — 5 routes mirroring `Routes__OpenSearch__Stack` under `/prometheus`. Handlers carry zero logic. The `create` handler takes `body: dict` and round-trips via `Schema__Prom__Stack__Create__Request.from_json(body)` — FastAPI/pydantic can't auto-generate a schema for the nested `List__Schema__Prom__Scrape__Target` Type_Safe collection (good failure caught at route-registration time; locked-in escape hatch for future sister sections with nested collections). 9 new tests via real `_Fake_Service(Prometheus__Service)` subclass + FastAPI TestClient (no mocks). 161 / 161 green in `cli/prometheus/`. | `2026-04-28__playwright-stack-split__phase-B__step-6g__sp-prom-routes.md` |
| 2026-04-28 | c0911f7                    | **Playwright stack split — Phase B · Step 6f: `sp prom` user-data + compose + config generator + launch + wire `create_stack`** — Compressed slice; the `sp os` 5f.1 → 5f.4b sequence (5 commits) lands here as one because the `sp prom` surface is simpler (no admin password, no `vm.max_map_count`). Ships `Prometheus__Compose__Template` (3 services on `sg-net`; `--storage.tsdb.retention.time=24h` per P2; **no Dashboards** per P1), `Prometheus__Config__Generator` (renders `prometheus.yml` — baseline cadvisor + node-exporter + appended `Schema__Prom__Scrape__Target` blocks per P3), `Prometheus__User_Data__Builder` (installs Docker via `dnf`, writes both YAMLs in heredocs, runs compose up — **no `vm.max_map_count` bump**), `Prometheus__Launch__Helper` (base64 UserData, `MinCount=MaxCount=1`, `DEFAULT_INSTANCE_TYPE='t3.medium'`), and wires `Prometheus__Service.create_stack(request, creator='')` end-to-end. `Prometheus__AWS__Client.setup()` adds the `launch` slot; `Prometheus__Service.setup()` chain grows from 5 to 8 helpers. 47 new tests; 152 / 152 green in `cli/prometheus/`. **`sp prom` is now functionally complete in pure logic.** | `2026-04-28__playwright-stack-split__phase-B__step-6f__sp-prom-create-stack.md` |
| 2026-04-28 | 2b5b93b                    | **Playwright stack split — Phase B · Step 6e: `sp prom` Service orchestrator (read paths)** — 4 small focused files. `Caller__IP__Detector` (section-local), `Random__Stack__Name__Generator` (vocabulary parity with elastic + opensearch locked by test), `Prometheus__Stack__Mapper` (pure boto3-detail → schema mapper; `prometheus_url = http://<ip>:9090/`; state-enum mapping table), `Prometheus__Service` (Tier-1 orchestrator with `setup()` wiring 5 helpers). Service exposes `list_stacks` / `get_stack_info` / `delete_stack` / `health`. `health` derives `(targets_total, targets_up)` from `data.activeTargets` via a small module-level `_count_targets` helper; falls through to `(-1, -1)` sentinels when the probe returns `{}`. `create_stack` deferred to 6f.4b when user-data is ready. 24 new tests across 4 focused files using real `_Fake_*` subclasses (no mocks). 105 / 105 green in `cli/prometheus/`. | `2026-04-28__playwright-stack-split__phase-B__step-6e__sp-prom-service-read-paths.md` |
| 2026-04-28 | 23f82f8                    | **Playwright stack split — Phase B · Step 6d: `sp prom` HTTP base + probe** — Two small focused HTTP files. `Prometheus__HTTP__Base` (~45 lines) wraps `requests` with `verify=False` default + scoped urllib3 warning suppression + Basic auth seam + new `params` kwarg for `/api/v1/query`. `Prometheus__HTTP__Probe` (~50 lines) ships 3 probes: `prometheus_ready` (True on 2xx of `/-/healthy`), `targets_status` (parsed `/api/v1/targets`; `{}` on failure), `query` (forwards PromQL via `/api/v1/query?query=…` for the future `sp prom query` command). All probes return `False` / `{}` on any failure; caller maps to `-1` sentinels in `Schema__Prom__Health`. 16 new tests via real `_Fake_HTTP` + `_Fake_Response` subclasses (no mocks). | `2026-04-28__playwright-stack-split__phase-B__step-6d__sp-prom-http-base-and-probe.md` |
| 2026-04-28 | 6141cf3                    | **Playwright stack split — Phase B · Step 6c: `sp prom` AWS helpers (small-file discipline)** — 4 small focused helpers + composition wired into `Prometheus__AWS__Client.setup()`. `Prometheus__SG__Helper` (port **9090** — Prometheus' own UI; no nginx because P1 says no UI in this stack), `Prometheus__AMI__Helper` (latest_al2023 + latest_healthy filtered by `sg:purpose=prometheus`), `Prometheus__Instance__Helper` (list/find/terminate filtered by section), `Prometheus__Tags__Builder` (Name uses `PROM_NAMING.aws_name_for_stack` so `prometheus-` prefix never doubles). Each helper ~50 lines, each test ~80 lines, all using real `_Fake_Boto_EC2` subclasses (no mocks). 23 new tests. `Prometheus__AWS__Client` test extended in-place to cover `setup()` wiring. | `2026-04-28__playwright-stack-split__phase-B__step-6c__sp-prom-aws-helpers.md` |
| 2026-04-28 | a1c814c                    | **Playwright stack split — Phase B · Step 6b: `sp prom` schemas + collections** — 7 schemas (`Schema__Prom__Scrape__Target`, `Stack__Create__Request`/`Response`, `Info`, `List`, `Delete__Response`, `Health`) + 3 collections (`List__Schema__Prom__Stack__Info`, `List__Schema__Prom__Scrape__Target`, `List__Str` local copy). Drops `admin_password` (P1: no built-in auth) and `dashboards_url` (P1: no UI); adds `Schema__Prom__Scrape__Target` for the baked-target list (P3). Defensive "no password field" tests on Request/Response/Info. Caught `Safe_Str__Text` stripping `/` from `metrics_path` — switched to `Safe_Str__Url__Path`. 23 new unit tests. | `2026-04-28__playwright-stack-split__phase-B__step-6b__sp-prom-schemas.md` |
| 2026-04-26 | *(pending)*                | **v0.1.96 stack-split — session handover dev-pack** — 9-file dev-pack at `team/claude/debriefs/v0.1.96-handover/` (00 README → 08 resumption prompt) for clean fresh-session continuation. Covers original task, what shipped, patterns + conventions, lessons learned, key commits, the proven sister-section template, remaining phases (B6b–6h, B7, C, D), and the copy-pasteable resumption prompt. Each file 41-97 lines. | `v0.1.96-handover/00__README.md` (entry point) |
| 2026-04-26 | 1a19d3f                    | **Playwright stack split — Phase B · Step 6a: `sp prom` foundation** — First slice of the Prometheus sister section. Folder `cli/prometheus/`; typer aliases `sp prom` + `sp prometheus`. Lands `Safe_Str__Prom__Stack__Name`, `Safe_Str__IP__Address` (local copy), `Enum__Prom__Stack__State`, and `Prometheus__AWS__Client` skeleton with `PROM_NAMING = Stack__Naming(section_prefix='prometheus')` + 6 tag constants. Regex + lifecycle parity with elastic + opensearch locked by test. 19 new tests. Helper slots wired in step 6c. | `2026-04-26__playwright-stack-split__phase-B__step-6a__sp-prom-foundation.md` |
| 2026-04-26 | 6abf20b                    | **Playwright stack split — Phase B · Step 5i: `sp os` / `sp opensearch` typer commands** — 5 commands (create / list / info / delete / health) wired into the main `sp` app via `add_typer`. Two small focused files: `cli/opensearch/cli/Renderers.py` (~85 lines, pure Tier-2A Rich renderers) and `scripts/opensearch.py` (~75 lines, thin typer wrappers). 9 new tests via Rich Console-capture + typer.testing.CliRunner. **Phase B `sp os` is now functionally complete end-to-end.** | `2026-04-26__playwright-stack-split__phase-B__step-5i__sp-os-typer.md` |
| 2026-04-26 | aef4018                    | **Playwright stack split — Phase B · Step 5h: `Routes__OpenSearch__Stack` (FastAPI surface)** — 5 routes mirroring the Routes__Ec2__Playwright pattern. Each handler is a thin `service` call + `.json()` — zero logic. POST /opensearch/stack, GET /opensearch/stacks, GET/DELETE /opensearch/stack/{name} (404 on miss), GET /opensearch/stack/{name}/health. 9 new tests via FastAPI TestClient with a real `_Fake_Service` subclass (no mocks). | `2026-04-26__playwright-stack-split__phase-B__step-5h__opensearch-routes.md` |
| 2026-04-26 | 2b21126                    | **Playwright stack split — Phase B · Step 5f.4b: `create_stack` wired into `OpenSearch__Service`** — End-to-end create flow working in pure logic. Service composes name_gen + ip_detector + ami helper + sg helper + tags builder + compose template + user_data builder + launch helper. Default resolution: random `os-{adj}-{sci}` name, caller IP via detector, `secrets.token_urlsafe(24)` password (32-char URL-safe base64), latest AL2023 AMI, default region/instance type. Secret only flows via compose; user-data builder does not take admin_password. 6 new tests with real `_Fake_*` subclasses. setup() chains 7 helpers; AWS client wires 5. | `2026-04-26__playwright-stack-split__phase-B__step-5f4b__create-stack-wired.md` |
| 2026-04-26 | 0a09731                    | **Playwright stack split — Phase B · Step 5f.4a: `OpenSearch__Launch__Helper`** — Single-purpose `run_instance(region, ami_id, sg_id, user_data, tags, instance_type, instance_profile_name?)`. Base64-encodes UserData (AWS rejects raw); `MinCount=MaxCount=1` (single-node OS stack); IamInstanceProfile attached only when set. DEFAULT_INSTANCE_TYPE=`t3.large`. 11 new tests with a real `_Fake_Boto_EC2` subclass — happy path / SG / tags / profile attached / pinned counts / empty response raises / boto failure propagates. | `2026-04-26__playwright-stack-split__phase-B__step-5f4a__launch-helper.md` |
| 2026-04-26 | 06bf140                    | **Playwright stack split — Phase B · Step 5f.3: expand user-data with Docker install + compose-up** — `USER_DATA_TEMPLATE` grew into a working AL2023 boot script: dnf install Docker, install docker compose plugin, write compose YAML to `/opt/sg-opensearch/docker-compose.yml` via heredoc, bump `vm.max_map_count` to 262144 (runtime + persistent — required for OS 2.x), `docker compose up -d`. `render()` signature now takes `compose_yaml` (admin_password lives only inside it — secrets in one place). PLACEHOLDERS expanded; defensive test asserts no `admin_password` leak in user-data. 13 tests (was 6). | `2026-04-26__playwright-stack-split__phase-B__step-5f3__user-data-install-steps.md` |
| 2026-04-26 | 8658520                    | **Playwright stack split — Phase B · Step 5f.2: `OpenSearch__Compose__Template`** — Single-purpose `docker-compose.yml` renderer for single-node OpenSearch + Dashboards. `OS_IMAGE`/`DASHBOARDS_IMAGE` use moving `:latest` per plan doc 4 OS1; tests pin a known version. Invariants locked by test: `bootstrap.memory_lock=true` + memlock ulimits (OS 2.x requirement), `sg-net` bridge, container names `sg-opensearch` + `sg-opensearch-dashboards`, dashboards `depends_on: [opensearch]`, `opensearch-data` named volume. 9 tests; PLACEHOLDERS = (os_image, dashboards_image, admin_password, heap_size). | `2026-04-26__playwright-stack-split__phase-B__step-5f2__compose-template.md` |
| 2026-04-26 | 363341c                    | **Playwright stack split — Phase B · Step 5f.1: `OpenSearch__User_Data__Builder` skeleton** — Smallest atomic step toward the user-data flow. Ships ONLY the rendering contract — a tight bash scaffold with placeholders for stack_name / admin_password / region — so subsequent install-step slices grow the template in isolation. PLACEHOLDERS tuple locked by test against every `{key}` in template. 6 tests (placeholders substituted, no leftover, shebang, `set -euo pipefail`, canonical log path, PLACEHOLDERS contract). | `2026-04-26__playwright-stack-split__phase-B__step-5f1__user-data-skeleton.md` |
| 2026-04-26 | 82afd0e                    | **Playwright stack split — Phase B · Step 5e: `sp os` Service orchestrator (read paths)** — 5 small focused files (~25-70 lines each): `Caller__IP__Detector` (section-local), `Random__Stack__Name__Generator` (vocabulary parity with elastic), `OpenSearch__Stack__Mapper` (pure boto3-detail → schema mapper with state-enum mapping table), `OpenSearch__Service` (Tier-1 orchestrator with `setup()` wiring 5 helpers). Service exposes `list_stacks`/`get_stack_info`/`delete_stack`/`health`. `create_stack` deliberately deferred to 5f when user-data is ready. 33 new tests across 4 focused files using real `_Fake_*` subclasses (no mocks). 176 tests pass across all my work areas. | `2026-04-26__playwright-stack-split__phase-B__step-5e__sp-os-service-read-paths.md` |
| 2026-04-26 | 05c0bb7                    | **Playwright stack split — Phase B · Step 5d: `sp os` HTTP base + probe** — Two small focused HTTP files (one class each). `OpenSearch__HTTP__Base` (~40 lines) wraps `requests` with `verify=False` + Basic auth + scoped urllib3 warning suppression. `OpenSearch__HTTP__Probe` (~35 lines) does `cluster_health()` (returns `{}` on any failure; populates `-1` sentinels) and `dashboards_ready()` (True on 2xx). 14 new tests covering happy / non-200 / network / non-JSON / 2xx range / Basic-auth forwarding paths via real `_Fake_HTTP` + `_Fake_Response` subclasses (no mocks). Index/bulk/saved-object ops deferred to their own helpers when needed. | `2026-04-26__playwright-stack-split__phase-B__step-5d__sp-os-http-base-and-probe.md` |
| 2026-04-26 | f5dcde7                    | **Playwright stack split — Phase B · Step 5c: `sp os` AWS helpers (small-file discipline)** — Per operator request to keep new files small. The Elastic equivalent is one ~470-line file with ~20 methods; this slice splits the OpenSearch AWS surface into 4 focused helpers, each with its own ~80-line test file: `OpenSearch__SG__Helper`, `OpenSearch__AMI__Helper`, `OpenSearch__Instance__Helper`, `OpenSearch__Tags__Builder`. `OpenSearch__AWS__Client` becomes a composition shell wired by `setup()`. Each helper has its own `_Fake_Boto_EC2` subclass (real classes, no mocks). 22 new tests covering ensure-SG (create/reuse/duplicate-ingress swallowed), AMI lookups (latest_al2023 / latest_healthy with ordering + empty-when-none), instance list-by-purpose-and-state / find-by-stack-name / terminate, and tag-spec construction (Name prefix never doubled, creator-empty fallback). | `2026-04-26__playwright-stack-split__phase-B__step-5c__sp-os-aws-helpers.md` |
| 2026-04-26 | 9a1e04e                    | **Playwright stack split — Phase B · Step 5b: `sp os` schemas + collections** — 6 schemas (`Schema__OS__Stack__Create__Request`/`Response`, `Info`, `List`, `Delete__Response`, `Health`), the `List__Schema__OS__Stack__Info` collection, and two primitives (`Safe_Str__OS__Password`, plus a local `Safe_Str__IP__Address` copy so sister sections stay self-contained). Defensive test locks the `Info` schema against ever leaking `admin_password`. 19 new unit tests, all green. | `2026-04-26__playwright-stack-split__phase-B__step-5b__sp-os-schemas.md` |
| 2026-04-26 | be6d4dd                    | **Playwright stack split — Phase B · Step 5a: `sp os` foundation** — First slice of the OpenSearch sister section. New `cli/opensearch/` package (folder named `opensearch` not `os` because `os` shadows the Python stdlib — caught immediately by 175 unit-test failures, recorded in plan doc 4). Lands `Safe_Str__OS__Stack__Name`, `Enum__OS__Stack__State`, and an `OpenSearch__AWS__Client` skeleton declaring `OS_NAMING = Stack__Naming(section_prefix='opensearch')` + 6 tag constants. 23 new unit tests including parity checks against the elastic equivalents. AWS-touching methods land in step 5c. | `2026-04-26__playwright-stack-split__phase-B__step-5a__sp-os-foundation.md` |
| 2026-04-26 | 14cdc51                    | **Playwright stack split — Phase A · Step 4: `DELETE /ec2/playwright/delete-all` route + Phase A complete** — Adds the matching HTTP route for `sp delete --all` (the only CLI op with a service method that lacked a route). `Ec2__Service__In_Memory` test fixture extended with `delete_all_instances()`. Other typer commands (connect/shell/exec/forward/screenshot/wait/bake-ami/etc.) are interactive or out of scope and were not added. **Phase A done.** Foundation work for Phase B (sister sections) is now in place: `Stack__Naming`, `Image__Build__Service`, `Ec2__AWS__Client` (full AWS boundary), reduced typer wrappers for `list`/`info`/`delete`, and `delete-all` available via both CLI and HTTP. | `2026-04-26__playwright-stack-split__phase-A__step-4__delete-all-route.md` |
| 2026-04-26 | cde60c5                    | **Playwright stack split — Phase A · Step 3f: typer `cmd_list`/`cmd_info`/`cmd_delete` reduced to thin wrappers** — `Schema__Ec2__Instance__Info` gained `instance_type` so the cmd_list table builds off the schema. `Ec2__Service` gained `delete_all_instances()`. `cmd_info` body shrinks from ~60 lines to ~10 with rendering moved to `_render_info()` (Tier 2A). `cmd_delete` and `cmd_list` now call the service for instance data. New shared `_resolve_typer_target()` helper for the "auto-pick when only one instance" UX. 2 new unit tests with a real `Ec2__AWS__Client` subclass (no mocks). 0 behavioural change. Closes Phase A step 3 (3a/3b/3c/3d/3f). | `2026-04-26__playwright-stack-split__phase-A__step-3f__typer-list-info-delete-wrappers.md` |
| 2026-04-26 | 166541e                    | **Playwright stack split — Phase A · Step 3d: SG + AMI helpers** — Adds the 9 SG/AMI constants and 6 methods (`ensure_security_group`, `latest_al2023_ami_id`, `create_ami`, `wait_ami_available`, `tag_ami`, `latest_healthy_ami`) to `Ec2__AWS__Client`. Caught a real bug — `SG__DESCRIPTION` had an em-dash that AWS rejects as non-ASCII; replaced with a hyphen (matches Elastic precedent). 14 new unit tests with an in-memory `_Fake_Boto3_Client` covering the AMI lifecycle paths. 0 behavioural change to the success path. | `2026-04-26__playwright-stack-split__phase-A__step-3d__sg-and-ami-helpers.md` |
| 2026-04-26 | 5fb8e45                    | **Playwright stack split — Phase A · Step 3c: IAM helpers + constants** — Adds the 8 IAM constants + 3 functions (`decode_aws_auth_error`, `ensure_caller_passrole`, `ensure_instance_profile`) to `cli/ec2/service/Ec2__AWS__Client.py`. `_print_auth_error` (Tier 2A Console formatter) stays in `provision_ec2.py`; `ensure_caller_passrole` no longer prints on auth failure (raises only — typer command formats). 7 new unit tests covering constant shape + the decode short-circuit / swallow-on-failure paths. 0 behavioural change to the success path. | `2026-04-26__playwright-stack-split__phase-A__step-3c__iam-helpers.md` |
| 2026-04-26 | 6ab825d                    | **Playwright stack split — Phase A · Step 3b: AWS context accessors** — Adds `aws_account_id`, `aws_region`, `ecr_registry_host`, `default_playwright_image_uri`, `default_sidecar_image_uri` (plus the two IMAGE_NAME re-exports) to `cli/ec2/service/Ec2__AWS__Client.py`. Removes the 5 definitions from `provision_ec2.py`; alias-imports keep typer commands working. `Ec2__Service.create()` import block shrinks from 7 → 2. 4 new unit tests with module-level monkey-patching (no mocks). 0 behavioural change. | `2026-04-26__playwright-stack-split__phase-A__step-3b__aws-context-accessors.md` |
| 2026-04-26 | 68a6c85                    | **Playwright stack split — Phase A · Step 3a: `Ec2__AWS__Client` (naming + lookup helpers)** — First sub-slice of step 3 (broken into 3a/3b/3c/3d/3f to keep each commit reviewable). Mirrors the `Elastic__AWS__Client` pattern: a new module at `cli/ec2/service/Ec2__AWS__Client.py` hosts module-level pure helpers (`random_deploy_name`, `get_creator`, `uptime_str`, `instance_tag`, `instance_deploy_name`) plus a Type_Safe class with the AWS-touching methods (`find_instances`, `find_instance_ids`, `resolve_instance_id`, `terminate_instances`). `provision_ec2.py` keeps thin wrapper functions matching the old signatures (the `ec2: EC2 = None` parameter is now ignored — wrappers go away in 3f when typer commands become thin wrappers). `Ec2__Service` no longer imports lookup helpers from `scripts.provision_ec2`; uses the new client via an `aws_client()` seam. 24 new unit tests with an in-memory `_Fake_EC2` (real subclass, no mocks). 0 behavioural change. | `2026-04-26__playwright-stack-split__phase-A__step-3a__ec2-aws-client-helpers.md` |
| 2026-04-26 | 0162e93                    | **Playwright stack split — Phase A · Step 2: `Image__Build__Service`** — Shared Docker image build pipeline. New `cli/image/` module with 5 Type_Safe schemas + 2 collections + 1 service. `Schema__Image__Build__Request` carries `image_folder` + `image_tag` + an ordered list of `Schema__Image__Stage__Item` (file or tree, with per-item `extra_ignore_names`); `Schema__Image__Build__Result` returns `image_id` / `image_tags` / `duration_ms`. The service has two seams: `stage_build_context()` (pure I/O, fully unit-testable) and `build()` (invokes the docker SDK directly, bypassing osbot-docker's `@catch` wrapper so real build failures propagate). Both existing builders (`Build__Docker__SGraph_AI__Service__Playwright` for the Playwright EC2 image and `Docker__SP__CLI` for the SP CLI Lambda image) reduce to thin composers that build a request and delegate. Removed: ~70% duplication (tempdir staging, ignore-noise filter, direct docker SDK call, tempdir cleanup) plus the now-redundant `_ignore_build_noise` / `ignore_build_noise` helpers. 15 new unit tests, 1 in-memory fake docker client (real class, no mocks). 0 behavioural change. | `2026-04-26__playwright-stack-split__phase-A__step-2__image-build-service.md` |
| 2026-04-26 | 4ee540c                    | **Playwright stack split — Phase A · Step 1: `Stack__Naming`** — First code change of the v0.1.96 stack-split plan. Promotes the elastic-only `aws_name_for_stack()` and `sg_name_for_stack()` helpers to a shared `Type_Safe` class at `sgraph_ai_service_playwright__cli/aws/Stack__Naming.py`. Each section binds a `section_prefix` (`elastic` / `opensearch` / `prometheus` / `vnc`) once and the methods enforce CLAUDE.md rules #14 (SG GroupName must not start with `sg-`) and #15 (Name tag must not be double-prefixed). 9 new unit tests, 5 elastic call sites switched to `ELASTIC_NAMING.*`. 0 behavioural change. | `2026-04-26__playwright-stack-split__phase-A__step-1__stack-naming.md` |
| 2026-04-26 | 2e1cf69 → 38f61f0 *(7 commits)* | **`sp el lets cf events` (LETS slice 2)** — Content-reading sibling of slice 1. CLI: `load` / `wipe` / `list` / `health` under `sp el lets cf events`. Fetches each `.gz` via `s3:GetObject`, gunzips, parses TSV into 38-field `Schema__CF__Event__Record` (incl. URL-decoded UA, status class derivation, bot classification via 28 named-bot regex + 5 generic word-bounded indicators, cache_hit flag), groups records by event timestamp date for daily-rolling `sg-cf-events-{YYYY-MM-DD}` indices, bulk-posts with `_id = "{etag}__{line_index}"` for per-event idempotency. After each successful file, flips slice 1's inventory `content_processed=true` via `_update_by_query` (Painless). `--from-inventory` mode reads the manifest as the work queue → daily incremental refresh. Auto-imports 6-panel "CloudFront Logs - Events Overview" dashboard (Vis Editor). Matched-pair wipe also resets the manifest. 201 unit tests, zero mocks. Side-effect surface: zero existing files modified (additive to `scripts/elastic_lets.py` only). Real-world validated: 565 events from 50 .gz files in 12s; wpbot/Googlebot correctly classified BOT_KNOWN; workspace.sgraph.ai users correctly classified HUMAN. | 3-part: `2026-04-26__lets-cf-events__01-why.md` / `__02-what.md` / `__03-how-to-use.md` |
| 2026-04-26 | 88b213f → f34ed56 *(14 commits)* | **`sp el lets cf inventory` (LETS slice 1)** — First LETS-shaped vertical pass on top of the Ephemeral Kibana. CLI: `load` / `wipe` / `list` / `health` under `sp el lets cf inventory`. Lists CloudFront real-time S3 metadata (no `.gz` content reads), parses Firehose-embedded timestamps from filenames, indexes into daily `sg-cf-inventory-{YYYY-MM-DD}` indices keyed on `delivery_at` with `_id = etag` for idempotent re-loads. Auto-imports a 5-panel "CloudFront Logs - Inventory Overview" dashboard (Vis Editor, NOT Lens — migration-footgun hardened). Matched-pair wipe is idempotent. 150 unit tests, zero mocks. End-to-end LETS round trip proven: stack-A export → stack-B import → both auto-imported and hand-built Lens dashboards re-bind to the same data view by title-match. Side-effect surface: +2 lines in `scripts/elastic.py` for the Typer mount. | 3-part: `2026-04-26__lets-cf-inventory__01-why.md` / `__02-what.md` / `__03-how-to-use.md` |
| 2026-04-25 | 501f12b → d9346b6 *(23 commits)* | **`sp el` (Ephemeral Kibana)** — Single-command lifecycle for an ephemeral Elasticsearch + Kibana + nginx-TLS stack on EC2. Cold path 158s, AMI fast-launch 80s. CLI: lifecycle (create/wait/info/list/delete/connect/exec/health/harden), data (seed/wipe), saved-objects (dashboard, data-view), AMI (list/create/delete/wait), one-shot create-from-ami. Auto-creates Kibana data view + 4-panel "Synthetic Logs Overview" dashboard from synthetic seed data. Boot-time harden script hides Observability/Security/Fleet/ML side-nav groups via Spaces API (baked into AMI). 165 unit tests, no mocks. | 3-part: `2026-04-25__sp-elastic-kibana__01-why.md` / `__02-what.md` / `__03-how-to-use.md` |
| 2026-04-24 | 90904f9, e6b4305, 5c2e6ec, f5170bd | **v0.1.72 OpenSearch index pattern** — `sp ob create` now creates `sg-playwright-logs` index pattern automatically. Root causes: (1) `POST /api/data_views/data_view` silently no-ops on AOS; (2) verify checked status not body; (3) IAM/SigV4 cannot write saved objects on FGAC domains; (4) `fields` key in body causes AOS 400; (5) `&security_tenant=` in query string AND header simultaneously causes 400. Fix: single path — `POST /api/saved_objects/index-pattern/{id}?overwrite=true` with basic auth + header-only tenant + no fields key. Deleted `_sys_idx_write`, `_sys_idx_write_sigv4`, `_DV_TRIES`, `_fetch_index_fields`. | `v0.1.72__opensearch-index-pattern.md` |
| 2026-04-21 | 2e44d54, 9f4cba7, f2eeb02, 112ffab | **v0.1.49 Performance Fix** — Eliminated 28-30s per-request latency. Two compounding bugs: (1) `browser.close()` / `playwright.stop()` blocked on Chromium's keep-alive drain (mitmproxy holds connections open) — fixed by SIGKILL Node.js subprocess + Chromium children via `/proc` PID scan; (2) Playwright sync API raised inside Lambda's asyncio event loop — fixed by `_run_sequence()` wrapper that dispatches to a clean `ThreadPoolExecutor` thread when a loop is detected. Also: CI `ec2-image` filter added (EC2 pulls ECR directly, needs rebuild on any Python source change); AWS Public ECR auth added to CI (avoids pull rate-limit). Result: avg cold=1065ms, avg warm=1069ms across 4 URLs. | `2026-04-21__v0.1.49-perf-fix-browser-close-asyncio.md` |
| 2026-04-20 | *(pending)* | **v0.1.46 Observability P1+P2** — P1: `GET /metrics` (Prometheus text) on both Playwright + agent_mitmproxy services; 6 `sg_playwright_*` + 4 `sg_mitmproxy_*` metrics with isolated `CollectorRegistry`. P2: `provision_ec2.py` extended with 8-service observability compose stack (prometheus, grafana, cadvisor, node-exporter, loki, promtail, cloudwatch-agent, xray-daemon), config file injection via user-data, `IAM__OBSERVABILITY_POLICY_ARNS`, 3 new CLI commands. 4 Grafana dashboard JSONs. AWS setup runbook. | `2026-04-20__v0.1.46-observability-p1-p2.md` |
| 2026-04-20 | 721335f (P1), 005127b (P2) | **Proxy gateway P1+P2** — P1: agent_mitmproxy upstream mode (3 new env vars, wrapper-script pattern to avoid supervisord interpolation crash, `proxy_mode` on `/health/info`). P2: Playwright proxy API cleanup — delete `Proxy__Auth__Binder` (CDP Fetch workaround), `Schema__Proxy__Config`, `Schema__Proxy__Auth__Basic`; proxy is now boot-time env (`SG_PLAYWRIGHT__DEFAULT_PROXY_URL`), not per-request API param. 188 unit tests green. | `2026-04-20__proxy-gateway-p1-p2.md` |
| 2026-04-19 | 0bdecab, 298f9e1, 450a8d5, dfee003, 69f0d4e, e094adc | **v0.1.29 — First-pass agentic refactor.** Env-var rename `SG_PLAYWRIGHT__* → AGENTIC_*`; new L1/L2 subpackages `agentic_fastapi/` + `agentic_fastapi_aws/`; one-command `scripts/deploy_code.py` (package → S3 → Lambda env → smoke); 8 read-only `/admin/*` endpoints (Agentic_Admin_API); SKILL files + `capabilities.json` stub; single-track CI with new `deploy-code` job. Public HTTP surface (10) + service classes (10) unchanged. Hotfix `e094adc` — swap raw boto3 for AWS_Config after first CI deploy-code run tripped `s3.region_name()` AttributeError. 390 unit tests green. | `2026-04-19__v0.1.29-first-pass-agentic-refactor.md` |
| 2026-04-17 | *(pending)*                | **v0.1.24 — Remove stateful session API, repurpose `/browser/*` as stateless one-shots.** Deletes `/session/*`, `/quick/*`, `Session__Manager`, `Action__Runner`; reimplements `/browser/*` as 6 stateless one-shot endpoints each accepting optional `browser_config` (incl. proxy); drops `session_id` + `close_session_after` from `Schema__Sequence__Request`. Endpoint count 25 → 10. Lambda-safe + security-safe by construction. | `2026-04-17__v0.1.24-remove-stateful-session-api.md` |
| 2026-04-17 | deb5c79                    | **QA session** — authenticated-proxy testing against dev Lambda; produced `qa-findings__proxy-testing.md` with P1 Bug #1 (CDP Fetch needed for proxy auth) + P1 Bug #4 (sync/async Playwright poisoning) + P3 Bug #3 (session leak on CF 504) + schema redesign proposal (`Schema__Proxy__Auth__Basic`). Drove the v0.1.14 reliability slice (deadline + watchdog). | `2026-04-17__qa-proxy-testing-findings.md` |
| 2026-04-17 | 906ba05 (preceded by 8922484, cdba60c, 22dd98a, 6869632) | **Milestone** — 100%-clean-state per call (per-call sync_playwright + Chromium, register/try-finally teardown) + per-phase timing breakdown surfaced (`Schema__Sequence__Timings`; JSON on `/quick/html` + `/sequence/execute`, `X-*-Ms` headers on `/quick/screenshot`); production-verified at ~800–1200 ms total | `2026-04-17__milestone-clean-state-per-call.md` |
| 2026-04-17 | 0f24354                    | Phase 2.9 — Step__Executor (NAVIGATE / CLICK / FILL / SCREENSHOT / GET_CONTENT / GET_URL; 10 deferred stubs) + Artefact__Writer.capture_* + CI env-var hotfix for integration job | `2026-04-17__phase-2.9-step-executor.md` |
| 2026-04-17 | 5184011                    | Phase 2.8 — Docker infra (Base + Build + ECR + Lambda + Local) + deploy-via-pytest (ECR push, local container, deploy to dev, smoke dev) | `2026-04-17__phase-2.8-docker-infra.md` |
| 2026-04-17 | 29be9d9                    | Phase 2.7 — FastAPI wiring (Playwright__Service orchestrator + Routes__Health + Fast_API__Playwright__Service; chassis-before-engine reorder) | `2026-04-17__phase-2.7-fastapi-wiring.md` |
| 2026-04-17 | 4ff12b2                    | Phase 2.6 — Browser__Launcher (real Chromium + env-var escape hatch; 5 integration tests live) | `2026-04-17__phase-2.6-browser-launcher.md` |
| 2026-04-17 | d4d8a53                    | Phase 2.5 — Credentials__Loader (vault ↔ browser-context glue; full spec source) | `2026-04-17__phase-2.5-credentials-loader.md` |
| 2026-04-17 | 4794f02                    | Phase 2.4 — Artefact__Writer (sink routing + vault JSON seams; capture_* deferred) | `2026-04-17__phase-2.4-artefact-writer.md` |
| 2026-04-16 | f6a4cfb                    | Phase 2.3 — Sequence__Dispatcher (parse surface; execute_step deferred) | `2026-04-16__phase-2.3-sequence-dispatcher.md` |
| 2026-04-16 | b9bbce3                    | Phase 2.2 — Session__Manager (v2 §4 callsites; v1 not in pack) | `2026-04-16__phase-2.2-session-manager.md` |
| 2026-04-16 | 571fd77                    | Phase 2.1 — Capability__Detector + ENV_VAR constants (spec §4.6, §7) | `2026-04-16__phase-2.1-capability-detector.md` |
| 2026-04-16 | 64dc249                    | Phase 1.4 — Request__Validator + JS__Expression__Allowlist (spec §9) | `2026-04-16__phase-1.4-request-validator.md` |
| 2026-04-16 | 4c63685                    | Phase 1.3 complete — §5.5–§5.10 + §6 + §8 + tests | `2026-04-16__phase-1.3-complete.md` |
| 2026-04-16 | 1f2edd6                    | Refactor — one-class-per-file (primitives, enums, schemas) | `2026-04-16__refactor-one-class-per-file.md` |
| 2026-04-16 | 8cb083c                    | CI fix — pytest exit-code-5 on placeholder jobs | `2026-04-16__ci-fix-exit-code-5.md` |
| 2026-04-16 | f219b01                    | CI fix — pin Playwright base image tag         | `2026-04-16__ci-fix-playwright-tag.md` |
| 2026-04-16 | 5ab4dc2, 68114e0           | CI fix — docker/deploy test placeholders + pytest install | `2026-04-16__ci-fix-docker-deploy-jobs.md` |
| 2026-04-16 | 0fa0f7f                    | Phase 1.3 (WIP) — core schemas §5.1–§5.4 + Safe_Str__Host | `2026-04-16__phase-1.3-wip-core-schemas.md` |
| 2026-04-16 | 3585724                    | Phase 1.1 + 1.2 — Safe_* primitives + Enum__* types | `2026-04-16__phase-1.1-1.2-primitives-enums.md` |
| 2026-04-16 | 08641a9                    | Phase 0 — bootstrap skeleton                    | `2026-04-16__phase-0-bootstrap.md` |
