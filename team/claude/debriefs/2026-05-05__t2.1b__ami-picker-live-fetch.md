# Debrief — T2.1b: sg-compute-ami-picker live fetch wiring

**Date:** 2026-05-05
**Branch:** `claude/sgcompute-frontend-v0.2-5sjIO`
**Commit:** (backfill by Historian)
**Status: COMPLETE**
**Brief:** `team/comms/briefs/v0.2.1__hotfix/backend/BV__ami-list-endpoint.md`

---

## Summary

`GET /api/amis` landed in dev (via `Routes__Compute__AMIs` + `AMI__Lister`). The frontend `sg-compute-ami-picker` had a stub `setSpecId()` that documented the hook but did nothing. This slice wires it up.

---

## Changes

### `sg-compute-ami-picker.js`

- `import { apiClient }` added from `../../../../../../../shared/api-client.js`
- `setSpecId(specId)` now: calls `_showLoading()`, awaits `apiClient.get('/api/amis?spec_id=...')`, calls `_populateAmis(data?.amis || [])` on success, `_showError(msg)` on failure
- `_populateAmis(amis)`:
  - empty list → hides placeholder, shows `.ami-empty`
  - non-empty → builds `<option>` elements (`name (ami_id)` label), reveals `<select>`, hides placeholder + empty
  - first option is a blank `— select an AMI —` sentinel
- `_showLoading()` — updates placeholder icon to ⏳ and text to "Loading AMIs…"
- `_showError(msg)` — updates placeholder icon to ⚠ and shows error message
- `_hidePlaceholder()` — hides `.ami-placeholder` element
- Old stub comment "GET /api/amis not yet available" removed

### `tests/ci/test_sg_compute_ami_picker__snapshot.py` (NEW)

17 structural assertions covering: file existence, custom element registration, `apiClient` import, real API call (`/api/amis?spec_id=`), `_populateAmis` method and empty handling, state helpers (`_showLoading`, `_showError`, `_hidePlaceholder`), event dispatch (`sg-compute:ami.selected`), `getSelectedAmiId()`, stub comment absent, HTML element presence, CSS coverage.

**17/17 pass.**

---

## Debrief updates

- `2026-05-05__t2.1__launch-flow-three-modes.md` — status flipped PARTIAL → COMPLETE; "PARTIAL status" section replaced with "T2.1b status update" table
- `team/roles/librarian/reality/sg-compute/index.md` — `Routes__Compute__AMIs` + `AMI__Lister` + schemas added to control_plane table; full frontend component table added as new section; history entry added; "Last updated" bumped

---

## Failures

**Good failure (none):** `GET /api/amis` was confirmed live in the routes files before writing any code. No speculation; backend schema verified against `Schema__AMI__Info` and `Schema__AMI__List__Response` before implementing `_populateAmis()`.

**Remaining PARTIAL (backend-owned):** FROM_AMI and BAKE_AMI end-to-end launch paths still depend on backend EC2 dispatch implementation — not frontend-blocked.
