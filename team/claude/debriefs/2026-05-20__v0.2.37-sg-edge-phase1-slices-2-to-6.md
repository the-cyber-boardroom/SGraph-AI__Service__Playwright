---
title: "v0.2.37 SG/Edge — Phase 1 build (Slices 2–4 + 6 core)"
date: 2026-05-20
status: PARTIAL — control plane + assets + bench core landed; live-deploy slices pending
branch: claude/implement-sg-edge-agent-c2jEp
merged-to-dev: false
---

# v0.2.37 SG/Edge — Phase 1 build

## 1. Header

| Field | Value |
|-------|-------|
| Date | 2026-05-20 |
| Status | **PARTIAL** — Slices 1–4 + bench core done & tested; Slice 5 + bench docker-stack are live-environment-bound |
| Version | v0.2.37 (in-progress) |
| Branch | `claude/implement-sg-edge-agent-c2jEp` |
| Tests | 72 passing + 6 CI-gated (FastAPI), no mocks |

## 2. TL;DR for the next agent

1. The whole `sg_edge` spec is **purely additive** — nothing outside it imports it, the `sg` CLI surface is unchanged. Verified at every commit (import-graph + CLI-registration greps).
2. Read the rewritten briefs in `briefs/05/20/sg-edge/` (no S3 lock — convergent reconciliation; state in DNS) and `briefs/05/20/fast-api/` (the Edge Waker is a `Serverless__Fast_API` Lambda).
3. The substantive control plane is **done and tested on the in-memory fakes**. The remaining work is **live-AWS / docker / py3.12** — see §5.

## 3. What landed (by slice)

| Slice | Delivered | Tests |
|---|---|---|
| 1 | primitives, enums, `Schema__SG_Edge__TXT__Record` + `Schema__SG_Edge__State__Record`, `SG_Edge__TXT__Builder` + `SG_Edge__State__Builder` | 32 |
| 2 | `SG_Edge__DNS__Helper` — `proxies.<parent>` A membership, `_state.<parent>` TXT counter, `_sg.*` active-slug count + routing read (over `sg aws dns`) | 15 |
| 3 | `SG_Edge__Fleet__Reconciler` (convergent: `ensure_booting`/`reconcile`/`idle_check`, launcher/terminator/clock seams) + `Fast_API__Edge_Waker` Lambda (routes, loading page, combined-deps `lambda_entry`, config) | 11 + 6 CI |
| 4 | `proxy/nginx.conf` static rig + `SG_Edge__Proxy__User_Data` (EC2 cloud-init); `cloudfront_function/viewer_request.js` + `SG_Edge__CloudFront__Function` | 9 |
| 6 (core) | `Edge_Bench__Stats` (percentiles) + `Edge_Bench__Runner` (acceptance-threshold verdicts) + `Schema__Edge_Bench__Metric` | 9 |

All AWS-touching logic is tested against the project's real in-memory client fakes
(`Route53__AWS__Client__In_Memory`, etc.) — no mocks, no patches.

## 4. Key design decisions (from the rewritten briefs)

- **No coordination service.** The reconciler reads DNS ground truth and converges;
  racing invocations cost one extra proxy that idle-teardown reclaims.
- **State in DNS only.** `proxies.<parent>` A (membership), `_state.<parent>` TXT
  (`zero_streak`), `_sg.<slug>` TXT (routing, read-only here).
- **Edge Waker = FastAPI Lambda** mirroring `vault_publish/lambdas/waker/` +
  `sg_compute/_for_osbot_aws/` combined-deps loader.
- **Phase 1 proxy is plain HTTP `:80`, no cert, no sidecars**; access log omits
  the client-IP variable (redaction at source).

## 5. What's left in Phase 1 — and why it isn't in this branch

These pieces are **live-environment-bound**; building them in the CI container
(Python 3.11, no AWS creds, no Docker) would produce unverifiable code, so they
were deliberately not faked in:

- **Slice 5 — proxy lifecycle + Setup orchestration.** The EC2-backed
  `_launcher`/`_terminator` that wire the reconciler's seams (RunInstances with the
  Slice-4 user-data, health-gate on `/_edge/health`, terminate-by-ip), and the
  `Setup__*` classes composing the existing `sg aws cf`/`acm`/`iam`/`lambda`/`dns`
  clients to provision the edge (wildcard ACM, CF distribution + origin group +
  attached viewer function, Edge Waker Lambda + Fn URL, `proxies.<parent>` zone).
  The reconciler is already designed for these seams; the verification method is
  **deploy-via-pytest against real AWS**, which the container can't run.
- **Bench env.** The `sg edge_bench` CLI + the doc-05 docker-compose local stack
  (OpenResty + Edge Waker as a FastAPI container + CoreDNS) + the live scenario
  bodies (P-*/F-*/X-*). The measurement *core* (percentiles + verdicts) is landed;
  the scenarios that drive real boots need Docker / AWS.

## 6. Environment notes (not code defects)

- Container is **Python 3.11**; `osbot-fast-api-serverless` requires 3.12, so the
  6 FastAPI `TestClient` tests skip locally and run in CI (same gating pattern as
  the Chromium integration tests). All non-FastAPI logic runs on 3.11.
- Pre-existing unrelated collection errors remain in other specs (e.g. `vnc`
  circular import) — environmental, untouched by this work.

## 7. Failure analysis

- **GOOD:** Type_Safe rejects bound methods for `Optional[Callable]` seams — caught
  immediately by the reconciler tests; fixed by injecting lambdas/functions (a
  constraint the Slice-5 wiring must also respect).
- **GOOD:** The `$remote_addr` literal in an nginx.conf comment tripped the
  "IP never logged" assertion — reworded the comment so the token genuinely never
  appears in the config.
- **BAD:** none.
