---
title: "v0.2.64 Playwright console — capability-driven UI + workflows + tests (P1→P7)"
date: 2026-06-23
status: DONE — console (P1-P5) + integration tests & CI gate (P6) + debrief/reality-doc (P7) landed on branch
branch: claude/amazing-pasteur-3srh6x
merged-to-dev: false
covers: "v0.2.64 dev pack library/dev_packs/v0.2.64__playwright-test-pages-and-workflows/"
---

# v0.2.64 Playwright console — P1→P7

## 1. Header

| Field | Value |
|-------|-------|
| Date | 2026-06-23 |
| Status | **DONE** on branch — console rebuild (P1-P5) committed `9fe5917` + `f4c84ec`; P6 tests + CI gate and P7 docs/reality-doc this slice |
| Version | repo root `version` = v0.2.63 (pack is the forward-looking v0.2.64) |
| Branch | `claude/amazing-pasteur-3srh6x` (no push to `dev`, no PR) |
| Tests | P6 runnable tier **16 new unit tests pass** on Python 3.12 (`.venv312`); integration + live + UI-smoke tiers skip cleanly without Chromium / a live URL. No mocks, no patches. |

## 2. TL;DR for the next agent

1. `GET /` is now a **capability-driven, agent-native console** (`Routes__Index.INDEX_HTML`): 8 endpoint-family tabs, the 24-verb sequence builder, `/inspect` + `/session/*` + `/browser/*` + PDF + DOM/a11y/text/html surfaces, a debug panel, workflow import/export with the W1–W9 example gallery, in-app docs generated from the live capability surface, and an in-page `window.__tool`. It is root_path-aware so it works identically behind `/pw` and at root.
2. **P6 added the runnable regression guards** that keep the console honest without a browser: the gallery body parser (each W deserialises into its real `Schema__*`), the verb-table drift check (UI `VERBS` == `Enum__Step__Action` == `STEP_SCHEMAS`), and the prefix-aware index check. The W1–W9 → assertion map against real Chromium + the UI execute-path smoke are gated and skip cleanly here.
3. **The Docker-image checks gate the Docker Hub publish** by extending the existing `integration-test-image` CI job (not a new job) — see §6.
4. **D1 is fixed** in the reality doc and catalogue (16→21 direct endpoints; `Routes__Session` is wired again). **D7 is a new finding, flagged not fixed** — see §5.

## 3. What landed (by phase)

| Phase | Delivered | Where |
|-------|-----------|-------|
| P1 (`9fe5917`) | Bug-fixes + `/health/capabilities` wiring (health-badge-no-key, unescaped `${url}`, null guards, html-mislabel) | `Routes__Index.py` |
| P2–P5 (`f4c84ec`) | The full console: sg-layout shell, 8 tabs, 24-verb builder, workflow import/export + W1–W9 gallery, in-app docs, in-page `window.__tool` | `Routes__Index.py`; `tests/unit/fast_api/routes/test_Routes__Index.py` (38 pass) |
| P6 (this slice) | Body-level gallery parse guard; verb-table drift guard; prefix-aware index guard; W1–W9 real-Chromium assertion map; UI execute-path headless smoke; CI Docker-image checks wired into `integration-test-image` | see §4 |
| P7 (this slice) | This debrief + index row; reality-doc D1 fix; catalogue endpoint/verb-surface fix | see §5 |

## 4. P6 test files

| File | Tier | Gate | What it proves |
|------|------|------|----------------|
| `tests/unit/fast_api/routes/test_Workflows__Gallery__Bodies.py` | unit | none (runnable) | Pulls the live `const GALLERY = [...]` out of `INDEX_HTML` (single source of truth) and asserts each W1–W9 body deserialises into its target `Schema__Sequence__Request` / `Schema__Inspect__Request` / `Schema__Screenshot__Batch__Request` and every step parses via the real `parse_step()`. **9 tests.** |
| `tests/unit/fast_api/routes/test_Routes__Index__verb_table_drift.py` | unit | none | The UI `const VERBS` keys == `Enum__Step__Action` == `STEP_SCHEMAS` (the in-app docs cannot rot — structural defence against D2/D4). **3 tests.** |
| `tests/unit/fast_api/routes/test_Routes__Index__prefix_aware.py` | unit | none | `window.API_BASE` is `''` at root and `/pw` behind `X-Forwarded-Prefix`; no absolute-rooted `/components/` or `/api/specs/` asset URL; the only external asset is CDN-absolute (brief 08 / Decision #11). **4 tests.** |
| `tests/integration/test_Workflows__Gallery.py` | integration | `SG_PLAYWRIGHT__CHROMIUM_EXECUTABLE` | W1,W3–W8 → one contract assertion each (brief 06 §3 map); W7 asserts the allowlist *failure* as a contract. Real `Browser__Launcher` via `_build_fast_api()` + TestClient. |
| `tests/integration/test_Workflows__UI_Execute_Smoke.py` | integration | Chromium + `window.__tool` | Drives `window.__tool.run(window.__tool.loadExample('W1'))` in headless Chromium against the live in-memory service (brief 06 §4). |
| `tests/integration_live/test_99_ui_console.py` | live (CI image gate) | live env vars | Numbered `test_1..test_5` (deploy-via-pytest): `GET /` console HTML, `/health/capabilities` shape, `/pw`-prefixed index, W1 execute — run against the by-digest image before publish. |

Composition uses the repo's real in-memory pattern (`_build_fast_api()` injecting `Browser__Launcher`), NOT the non-existent `register_playwright_service__in_memory()` — brief 06 already flagged that name discrepancy.

## 5. Failure classification (CLAUDE.md §27)

### Good failures (surfaced early, caught by tests, informed design)

- **`sg-tool-api` not published on the CDN (404).** Discovered during P5 — the shared `sg-tool-api` web component the pack assumed (Decision #1/#8) is not actually published. The console fell back to a **contract-compatible in-page `window.__tool` registration** (same surface, no CDN dependency) and `sg-layout` loads CDN-absolute as a graceful shell. Surfaced early, kept the agentic surface, did not block the slice. The prefix-aware test now guards that the fallback stays prefix-safe.
- **D7 — W2 gallery `wait_for` url_pattern glob is rejected.** Writing the body-level parse guard caught that W2's step `{"action":"wait_for","url_pattern":"**/dashboard**"}` (the correct Playwright `wait_for_url` glob, per brief 02) is **rejected with HTTP 400** by `/sequence/execute`, because `Schema__Step__Wait_For.url_pattern` is typed `Safe_Str__Url__Permissive` (`Schema__Step__Wait_For.py:20`) whose regex requires a full `http(s)://...` URL. Confirmed end-to-end through the in-memory TestClient (status 400, `ValueError: ... Safe_Str__Url__Permissive ... does not match required pattern`). This is a real gallery-vs-schema contract bug. **Flagged, not fixed** — it needs either a schema field-type change or a gallery edit, both runtime/design decisions outside the P6/P7 tests-and-docs scope. It is asserted AS A CONTRACT in `test_Workflows__Gallery__Bodies.py::test__D7__W2_url_pattern_glob_is_rejected_by_wait_for_schema` (the same good-failure style brief 06 uses for W7's allowlist) so the day it is fixed, a test turns red and points the next agent at it.
- **D1 — reality-doc staleness.** The reality doc + `CLAUDE.md` said "16 direct endpoints" and "`Routes__Session` removed in v0.1.24", but the code wires both `Routes__Inspect` (`Fast_API__Playwright__Service.py:110`) and `Routes__Session` (`:111`) → 21 direct endpoints. The pack had pre-flagged D1 for the Librarian (README sign-off). Fixed in the reality doc + catalogue this slice; the load-bearing `CLAUDE.md` line is flagged for the human (§7) rather than silently edited.

### Environmental quirks (worked around, not silenced)

- **Python 3.11 vs 3.12.** The default `python` is 3.11 and cannot import the stack; tests run via `.venv312`. Known.
- **Route-scan tests fail in this venv.** 10 pre-existing unit tests that scan `app().routes` for `.path` fail (empty path) for environmental reasons — verified they fail on untouched files too, so not a regression. New P6 tests deliberately use the render pattern (`Routes__Index().index(_FakeRequest()).body.decode()`) and TestClient instead of route-scan, so they pass.

### Bad failures

- None introduced. D7 is a pre-existing latent bug surfaced (good), not silenced.

## 6. How the Docker checks gate the publish

The existing `.github/workflows/ci-pipeline.yml` already has the right shape: `build-amd64`/`build-arm64` (`:122`,`:180`) push **by digest only**; `integration-test-image` (`:254`) runs the live suite against the by-digest image and **gates** `push-playwright-manifest` (`:400`, `needs`/`if` at `:404-405`). Per brief 06 §6 / README Q5, P6 **extends `integration-test-image`** rather than inventing a new job:

- New module `tests/integration_live/test_99_ui_console.py` is collected by the suite that job already runs.
- The "Run remaining live integration suite" step (`:335-340`) now `--ignore`s `test_99_ui_console.py` so it is not sharded across xdist workers.
- A new dedicated step **"UI console + Docker-image checks (gate the publish)"** runs `pytest tests/integration_live/test_99_ui_console.py -v` right after, serially. Because it is a step inside `integration-test-image`, a red check fails the job → `push-playwright-manifest` is skipped → no manifest tag. A broken console never gets published.

## 7. Open follow-ups for the human / a future slice

1. **D7** — decide: loosen `Schema__Step__Wait_For.url_pattern` to accept Playwright glob patterns, or change the W2 gallery body. Until then W2 "Load example → Execute" returns 400.
2. **`CLAUDE.md` line** — the Architecture section still says "16 direct endpoints ... Routes__Session removed in v0.1.24". It is load-bearing and now contradicted by the reality doc + catalogue (21 endpoints). Not edited here per the slice constraints; flagged for the human to reconcile.
3. **`sg-tool-api` publish** — if/when the shared component is published to the CDN, swap the in-page `window.__tool` registration for the real component (Decision #8 option A).

---

## 8. Iteration 2 addendum (2026-06-23) — UX polish + self-contained examples

Same branch (`claude/amazing-pasteur-3srh6x`). A second pass on the console driven by operator decisions #1 (theme split) and #5 (self-contained examples). One transient note up front: **the first run of this iteration died mid-way to a transient API 500** — it had created `Routes__Test_Pages.py` (untracked, complete) but committed nothing. The relaunch completed the remaining wiring + INDEX_HTML items, fixed two issues in the pre-created file (404 XSS escape; route-registration cross-check against the `Routes__Index`/`Routes__Set_Cookie` idiom — confirmed the `prefix='/'` + `@route_path('/test-pages/{name}')` + `add_route_get` form registers correctly via a TestClient probe), and shipped.

### The six items delivered

1. **Light work-area theme** — added a light palette (`--lsurface/--ltext/...`) and a `.work-light` scope applied to the **center builder**, **right result**, and **bottom console** panes; inputs/selects/buttons inside light panes restyled to read on white. The **header bar and left tab rail keep the dark/blue tokens** (Decision #1). `Routes__Index.py` CSS block + body markup (`class="builder work-light"`, `class="result work-light"`).
2. **sg-layout wiring + graceful fallback** — builder/result/console wrapped in `<sg-layout>` driven like `admin.js`: a layout JSON with `p-builder`/`p-result`/`p-console` slots, `setLayout(saved||default)`, `getLayout()` persisted to `localStorage['sg-playwright:console:layout:v1']`, restored on load. Header + rail stay outside. **Fallback detection:** after the CDN `import(...)`, a `setTimeout(1500ms)` checks `customElements.get('sg-layout')`; if still undefined (or the import rejects, or `customElements` is absent) the console swaps `#work-area` from `.split` to `.console-grid` (plain CSS grid) so it is fully usable offline. Core function never depends on the CDN.
3. **Screenshot viewer** — `shotViewerEl(src, filename)` renders a light, framed, scrollable viewer (checkerboard backdrop, natural width, `max-width:100%`, scroll when tall) with **Download** (`<a download>`) and **Open in new tab**. Wired into single (`showShot`), per-step (`stepResultEl`), and batch (`renderBatch`) shots.
4. **Clipboard fix (the real bug, good failure).** `navigator.clipboard` is **undefined on `http://0.0.0.0`** (insecure origin) — the old bare `navigator.clipboard.writeText(...)` calls threw silently and nothing copied. Added one `copyText(s)` helper: async Clipboard API when `navigator.clipboard && window.isSecureContext`, else a hidden `<textarea>` + `document.execCommand('copy')`; never throws, flashes success. All three sites (`copyJson`, `copyCurl`, trace-id click-to-copy) plus the new console route through it; the only remaining `navigator.clipboard.writeText(` is inside `copyText`'s secure branch (asserted by test).
5. **Self-contained S-series + D7 fix.** Added **S1–S5** as the headline gallery, targeting the new `/test-pages/*` fixtures via `tpUrl(name) = window.location.origin + window.API_BASE + '/test-pages/' + name` (runtime-resolved absolute URLs, no external egress): S1 screenshot `simple`; S2 inspect DOM of `simple`; S3 fill `#username`/`#password` + click `#submit` on `form` with a per-step shot; S4 `wait_for` `#ready` on `dynamic`; S5 navigate→scroll→selector capture on `links`. W1–W9 retained. **D7 fixed:** W2's `wait_for` glob `**/dashboard**` (rejected by `Safe_Str__Url__Permissive` → HTTP 400) is now the full URL `https://app.example.com/dashboard`, so W2 deserialises and parses like every other workflow.
6. **Bottom-dock `__tool` console** — a live REPL over the already-registered in-page `window.__tool` (the operator's OWN browser, **not** the server-side `evaluate` allowlist). Multiline input + Run (Ctrl/Cmd+Enter, guarded so it does not collide with the active-tab execute shortcut), evals entered JS in an async scope with `__tool` in scope, renders the return value as **escaped** pretty JSON, and if the value carries `imageSrc`/`screenshot_b64` shows it via the item-3 viewer. Quick buttons: List methods / Manifest / Skills / Log. Token never echoed.

### Test-page route wiring + auth-exclusion

`Routes__Test_Pages` mounts at prefix `/` with `@route_path('/test-pages/{name}')` (same idiom as `Routes__Index`), wired at `Fast_API__Playwright__Service.py:115`. `setup()` (`:55-57`) appends the five concrete `/test-pages/{name}` paths to `AUTH__EXCLUDED_PATHS` **before** `super().setup()` builds the middleware — the same mechanism that exempts `/auth/set-cookie-form`. The middleware matches `request.url.path` exactly, so names are enumerated (`TEST_PAGE_NAMES`), not prefix-matched. The 404 branch HTML-escapes the reflected `name` (reflected-XSS fix).

### Tests (all green in `.venv312`)

- `test_Routes__Index.py` — rewrote pinned assertions to the new markup (intent preserved) + a new `test_Routes__Index__iteration2` class covering the theme split, sg-layout wiring/localStorage-key/fallback, screenshot viewer, `copyText` (no bare clipboard call outside it), S-series + D7, and the bottom `__tool` console.
- `test_Routes__Test_Pages.py` (new) — each fixture 200 + stable ids **without** an API key (auth-excluded), path-exclusion assertion, 404 XSS-escape.
- `test_Workflows__Gallery__Bodies.py` — `tpUrl(...)` substituted before parsing; `WORKFLOWS_KNOWN_BAD` now empty (D7 resolved); S-series + W2-valid assertions added.
- Run: `pytest tests/unit/fast_api/routes/test_Routes__Index.py test_Workflows__Gallery__Bodies.py test_Routes__Test_Pages.py` → **62 passed**. The 6 `test_route_registration`/`test_route_wiring` failures in `tests/unit/fast_api` are the **pre-existing environmental route-scan quirk** (verified identical on the stashed/untouched tree) — not a regression.

### Failure classification (iteration 2)

- **Good failure — insecure-origin clipboard.** `navigator.clipboard` undefined on `http://0.0.0.0` made all three copy buttons silently no-op. Surfaced as a real bug in the brief, fixed centrally via `copyText` with an `execCommand` fallback; a test asserts no bare clipboard call survives outside the helper.
- **Good failure — sg-layout CDN may be absent.** Same condition that 404'd `sg-tool-api`. Handled by feature-detecting `customElements.get('sg-layout')` after a short delay and falling back to a CSS grid, so the console never depends on the CDN.
- **Good failure — D7 resolved.** The iteration-1 flagged W2 glob bug is fixed (full-URL pattern) and the contract test flipped from "asserts rejection" to "asserts it parses".
- **Operational note — transient 500 + relaunch.** The first run crashed to a transient API 500 after creating `Routes__Test_Pages.py` and before any commit. The relaunch kept that complete file, verified/repaired it, and finished. No bad failure introduced.
- **Bad failures:** none.

## 9. Iterations 3–5 addendum (2026-07-03) — the sg-layout saga, per-step screenshots, Output pane

Backfilled by the reviewer (Fable) — commits `9b2b24c`, `d3d8867`, `512da60`, `d562403`, `b539e2c` shipped code+tests only, leaving this debrief and the reality doc stale until this addendum (a §27 process miss, see review `team/humans/dinis_cruz/claude-code-web/07/03/08/review__v0.2.64-console-and-cookie-question.md`).

### What happened

1. **`9b2b24c`** — the iteration-2 sg-layout wiring rendered labelled-but-empty panels (panes given `slot='p-…'` with no `id`). Attempt #2 guessed an id-based projection contract from a WebFetch summary; added a verify-or-revert guard + grid-as-default so the console could no longer blank.
2. **`d3d8867`** — attempt #2 also failed to project (plus the reparenting briefly detached `#builder`, crashing `applyCapabilities` on `sess-disabled` — guard added). sg-layout dropped for an in-house splitter grid.
3. **`512da60`** — the operator supplied the authoritative guidance docs (`v0.1.92__sglayout__quickstart/full-inventory`): sg-layout **instantiates a custom element per tab from `tag`** — you never slot existing nodes. Reintegrated correctly: `sg-pane-{builder,output,gallery,console}` host elements relocate the pre-built panes from `#pane-store`; `customElements.whenDefined` before `setLayout`; persistence via the internal `events.on('layout:changed')` bus; host `tools.sgraph.ai`; light `--sgl-*` chrome; grid fallback + "⟲ Layout" reset kept. **Operator-confirmed working** (tabs, resize, drag-to-dock).
4. **`d562403`** — per-step inline screenshots finally render: `Enum__Artefact__Type` serialises to its lowercase VALUE (`"screenshot"`) but the renderer compared against `'SCREENSHOT'`; now case-insensitive, other inline artefacts (PDF) get a download link. Step-builder cards collapse via a delegated `.step-head` click (sequence/batch/inspect).
5. **`b539e2c`** — execution output split into its own **Output** pane (Builder | Output-over-Examples, Console dock); `revealOutput()`/`focusPanel('output')` on every Execute; layout key bumped to `v3` so stale saved trees cannot hide the new pane.

### Failure classification (§27)

- **Bad failure — sg-layout shipped on guessed contracts, twice** (`c27b8da`, `9b2b24c`): no browser in the dev environment and no authoritative component docs in-repo; the operator hit the breakage both times. Converted to good by the guidance docs. **Follow-ups:** mirror the sg-layout docs into `library/guides/`; add a JS-executing console render smoke to the CI image gate (the current gate only asserts the HTML is served).
- **Good failure — artefact-type case bug** surfaced by the operator's "artefact: screenshot (inline)" screenshot; fixed with a schema-grounded case-insensitive match + regression test.
- **Good failure — stale-saved-layout hazard** anticipated at `b539e2c` (v2 trees lacked the Output pane) and neutralised by the key bump.

Tests at head: **110 passed** across `tests/unit/fast_api/routes/`.
