# Debrief — T3.3b: components/sp-cli/ → components/sg-compute/ rename

**Date:** 2026-05-05
**Branch:** `claude/sgcompute-frontend-v0.2-5sjIO`
**Commit:** (backfill by Historian)
**Status: COMPLETE**
**Brief:** `team/comms/briefs/v0.2.1__hotfix/frontend/T3_3__cosmetic-rename-leftovers.md` Task 1

---

## Summary

T3.3 was marked DONE in the hotfix index but task 1 (parent directory rename) was stopped mid-session and deferred. This slice executes the deferred rename.

---

## Changes

### `git mv` — parent directory

`sgraph_ai_service_playwright__api_site/components/sp-cli/` → `sgraph_ai_service_playwright__api_site/components/sg-compute/`

All component subdirectories (`sg-compute-*`) moved atomically with git history preserved.

### String replacement — `api_site/` (3 files)

`sed -i 's|components/sp-cli/|components/sg-compute/|g'` applied to:
- `admin/index.html` — 20 `<script src>` tags
- `user/index.html` — remaining script tags
- `components/sg-compute/_shared/sg-compute-launch-form/…/sg-compute-launch-form.js` — 1 absolute import

### String replacement — `sg_compute_specs/*/ui/detail/` (8 files)

`sed -i 's|/ui/components/sp-cli/|/ui/components/sg-compute/|g'` applied to:
- `docker/ui/detail/…/sg-compute-docker-detail.js`
- `podman/ui/detail/…/sg-compute-podman-detail.js`
- `vnc/ui/detail/…/sg-compute-vnc-detail.js`
- `neko/ui/detail/…/sg-compute-neko-detail.js`
- `prometheus/ui/detail/…/sg-compute-prometheus-detail.js`
- `opensearch/ui/detail/…/sg-compute-opensearch-detail.js`
- `elastic/ui/detail/…/sg-compute-elastic-detail.js`
- `firefox/ui/detail/…/sg-compute-firefox-detail.js`

### Snapshot test path corrections (2 files)

- `tests/ci/test_sg_compute_ami_picker__snapshot.py` — `COMPONENT_DIR`: `sp-cli` → `sg-compute`
- `tests/ci/test_sg_compute_spec_detail__snapshot.py` — `COMPONENT_DIR`: `sp-cli` → `sg-compute`

---

## Acceptance criteria met

- `grep -rn "components/sp-cli/" api_site/ sg_compute_specs/` → **0 hits**
- Every `<script src="../components/sg-compute/…">` in `admin/index.html` resolves to an existing file on disk — **all 23 verified**
- 32/33 CI snapshot tests pass; 1 failure (`test_wheel_contains_ui_assets`) is pre-existing — `python -m build` is not installed in this environment

---

## Failures

**Good failure:** The "Stop and surface" check from T3.3 correctly identified that the rename must touch two trees (`api_site/` and `sg_compute_specs/*/ui/detail/`). The original T3.3 session stopped rather than renaming only one tree and leaving broken imports. This time both trees were updated atomically in one commit.

**Pre-existing test gap (not introduced here):** `test_wheel_contains_ui_assets` requires `python -m build` which is absent from this environment. Not a regression.
