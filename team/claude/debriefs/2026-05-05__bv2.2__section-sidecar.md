# Debrief — BV2.2: Section__Sidecar composable user-data fragment

**Date:** 2026-05-05
**Branch:** `claude/bv2-2-section-sidecar-MXKIP`
**Commit:** *(backfill after push)*
**Session:** MXKIP

---

## What was done

Extracted the sidecar startup bash fragment into a single composable class `Section__Sidecar` and wired it into every spec's `User_Data__Builder`.

### New file

| File | Description |
|------|-------------|
| `sg_compute/platforms/ec2/user_data/Section__Sidecar.py` | `Section__Sidecar.render()` — returns empty string when `registry=''`; full ECR-login + `docker run` block otherwise |
| `sg_compute__tests/platforms/ec2/user_data/test_Section__Sidecar.py` | 17 tests |

### Updated builders (template-based)

| Spec | Change |
|------|--------|
| `docker` | Was inline `SIDECAR_TEMPLATE`; now delegates to `Section__Sidecar` |
| `podman` | Added `{sidecar_section}` placeholder + sidecar params to `render()` |
| `opensearch` | Same |
| `prometheus` | Same |
| `neko` | Same |
| `firefox` | Same |
| `elastic` | Same |
| `vnc` | Same |

### Updated builders (parts-based)

| Spec | Change |
|------|--------|
| `ollama` | Appends `Section__Sidecar().render()` part before shutdown |
| `open_design` | Same |

### Updated tests

PLACEHOLDERS tuples updated in 8 spec test files to include `'sidecar_section'`.

---

## Failures

**None.** All 553 tests pass (25 skipped — Chromium-gated; 1 mitmproxy spec skipped due to missing `prometheus_client` package, pre-existing).

---

## Good failures (none in this slice)

All builders followed the same pattern. No structural surprises.

---

## Coverage

- `Section__Sidecar.render()` with and without registry
- Custom port, image tag, api_key_name, api_key_value
- ECR login command present
- Docker socket mount present
- Docker config cleanup present
- No unresolved `{placeholders}` in rendered output
