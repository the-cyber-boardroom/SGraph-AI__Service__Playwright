# Debrief — FV2.6: per-spec UI co-location (all 8 specs)

**Date:** 2026-05-05
**Branch:** `claude/sgcompute-frontend-v0.2-5sjIO`
**Commit:** (backfilled by Historian)

---

## What was done

Migrated all 8 spec card + detail web components out of the dashboard tree into `sg_compute_specs/<spec>/ui/`. Two commits: docker pilot, then batch of 7 remaining specs.

**Specs migrated:** docker, podman, vnc, neko, prometheus, opensearch, elastic, firefox.

### Files moved (48 total via git mv)

For each spec:
| Source | Destination |
|--------|-------------|
| `api_site/plugins/<spec>/v0/v0.1/v0.1.0/sg-compute-<spec>-card.{js,html,css}` | `sg_compute_specs/<spec>/ui/card/v0/v0.1/v0.1.0/` |
| `api_site/components/sp-cli/sg-compute-<spec>-detail/v0/v0.1/v0.1.0/sg-compute-<spec>-detail.{js,html,css}` | `sg_compute_specs/<spec>/ui/detail/v0/v0.1/v0.1.0/` |

**Deleted after emptying:**
- `api_site/plugins/` — entirely gone
- All 8 `api_site/components/sp-cli/sg-compute-<spec>-detail/` trees — gone

### Detail JS — relative → absolute imports

All detail components had relative `import` statements (from `../../../../../../shared/` and `../../../../_shared/`) that broke once the file moved out of `components/sp-cli/`. Two `sed` substitution rules applied uniformly:

- `'../../../../../../shared/` → `'/ui/shared/`
- `'../../../../_shared/` → `'/ui/components/sp-cli/_shared/`

Specs with extra imports (`shared/node-state.js`, `sg-remote-browser`): handled by the same rules — no special cases needed.

The `/ui` prefix is where `Fast_API__SP__CLI` mounts the `sgraph_ai_service_playwright__api_site` directory. This is a known temporary coupling; it will resolve when shared components also move to `tools.sgraph.ai`.

### admin/index.html

All 16 spec script tags (8 cards + 8 details) updated to `/api/specs/<id>/ui/...` paths. Comment block updated: "all specs migrated FV2.6".

---

## Decisions made

**Absolute `/ui/` paths in detail imports** — chosen over:
1. Removing the imports entirely (would require admin/index.html to explicitly load all 4 previously-implicit shared components)
2. Using `https://dev.tools.sgraph.ai/...` URLs (those components aren't there yet)

The `/ui/` coupling is accepted as a deliberate temporary state. The same pattern will be used for subsequent spec migrations (podman, vnc, etc.) until the shared components move to `tools.sgraph.ai`.

---

## What was NOT changed

- Card JS: no imports to fix — it only imports from `dev.tools.sgraph.ai` (already absolute).
- HTML/CSS files for both card and detail: no relative references, pure markup/CSS.
- Other spec cards/details in admin/index.html: left unchanged (podman, elastic, vnc, etc. — their FV2.6 sessions follow).
- Shared components in `api_site/components/sp-cli/_shared/`: remain in dashboard tree; not part of this phase.

---

## Failures

None — clean migration.

---

## Next steps

- FV2.6 is complete. All 8 specs migrated; dashboard tree cleaned.
- FV2.13 (dashboard move to `sg_compute/frontend/`) is the next structural phase — currently deferred to v0.3.
- The `/ui/` import coupling in detail components will resolve when shared components move to `tools.sgraph.ai`.
