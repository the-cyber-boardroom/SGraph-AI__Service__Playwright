# Debrief — T2.4 settings-bus dual dispatch

**Date:** 2026-05-05
**Status:** COMPLETE
**Commit:** ae5fca2

---

## What shipped

1. **`settings-bus.js` `setPluginEnabled`** now dispatches two events per call:
   - `sp-cli:spec.toggled` — CANONICAL (ACTIVE as of this PR)
   - `sp-cli:plugin.toggled` — DEPRECATED alias; removal target v0.3.0

2. **`sg-compute-events-log.js`** FAMILIES nav updated:
   - `sp-cli:spec.toggled` added as canonical entry
   - `sp-cli:plugin.toggled` marked DEPRECATED in the nav list

3. **`library/docs/specs/v0.2.0__ui-event-vocabulary.md`** updated:
   - `sp-cli:spec.toggled` status: RESERVED → ACTIVE
   - `sp-cli:plugin.toggled` deprecation note with removal target v0.3.0

4. **`admin.js`** wires both `sp-cli:spec.toggled` and `sp-cli:plugin.toggled` to the same `_onSpecToggled(e)` handler.

---

## Failures

**Good failure (caught immediately):** When converting the anonymous `plugin.toggled` handler to a named function, the closing `})` was left in admin.js instead of `}`. Caught on review before commit; fixed in the same pass.

---

## Follow-up

Remove `sp-cli:plugin.toggled` from settings-bus, events-log, admin.js, and event-vocabulary in v0.3.0.
