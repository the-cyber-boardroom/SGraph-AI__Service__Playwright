# BV2.1 — Delete orphan `sgraph_ai_service_playwright__host/`

**Date:** 2026-05-04
**Phase:** BV2.1
**Commit:** TBD (backfill after merge)
**Branch:** `claude/bv2-1-delete-orphan-host-MXKIP`

---

## What shipped

- `git rm -r sgraph_ai_service_playwright__host/` — 40+ files removed (containers, shell, host schemas, fast_api routes, lambda_handler).
- `pyproject.toml` — removed `{ include = "sgraph_ai_service_playwright__host" }` from the `packages` array.
- `.github/workflows/ci__host_control.yml` — removed `run-unit-tests` job (pointed at the deleted tests); updated `build-and-push-image` `needs:` to drop `run-unit-tests`; renumbered job comments.
- `tests/unit/sgraph_ai_service_playwright__host/` — three legacy test files deleted (`test_Container__Runtime__Docker.py`, `test_Fast_API__Host__Control.py`, `test_Shell__Executor.py`).
- `team/roles/librarian/reality/host-control/index.md` — updated: deletion noted at top; authoritative path updated to `sg_compute/host_plane/`; port corrected `:9000` → `:19009`.
- `team/roles/librarian/reality/changelog.md` — BV2.1 pointer entry appended.

## Test results

- `sg_compute__tests/` — **231 passed, 25 skipped, 0 failed** (after environment deps installed in the uv/pytest environment).
- `tests/` — failures are pre-existing missing-dependency errors (`playwright`, `osbot_fast_api_serverless`, `cryptography`, `prometheus_client`) — none reference `sgraph_ai_service_playwright__host`. Confirmed by `grep`.

## Failures classified

### Good failures (none)

The orphan had no external importers — the phase file's pre-condition held exactly. No surprises.

### Bad failures (none)

No bad failures. The 25 skipped tests in `sg_compute__tests/` are pre-existing skips (AWS-credential-gated integration tests that need `SG_PLAYWRIGHT__CHROMIUM_EXECUTABLE` or live AWS).

## Environment note (for next session)

The session's pytest runs via `~/.local/bin/pytest` (uv-managed, Python 3.11 isolated environment). `osbot-utils`, `osbot-aws`, `osbot-fast-api`, `httpx`, `typer`, `fastapi` had to be installed into that environment via `uv pip install ... --python /root/.local/share/uv/tools/pytest/bin/python`. This is an environment-setup gap; the next session will need to repeat if the env is cold. Track under B-XXX (new backlog entry: "document pytest env setup for web sessions").

## Acceptance criteria — checked

- [x] `sgraph_ai_service_playwright__host/` does not exist on disk.
- [x] `pyproject.toml` does not reference the legacy path.
- [x] No external `.py`, `.toml`, `.yml`, or `Dockerfile*` file imports from the legacy path (confirmed by grep — only a comment in the CI workflow).
- [x] `sg_compute__tests/` exits 0 (231 passed).
- [x] `team/roles/librarian/reality/changelog.md` entry added (commit hash to backfill).
- [x] `host-control/index.md` updated — migration-shim note replaced with deletion note; `sg_compute/host_plane/` is the only authority.
