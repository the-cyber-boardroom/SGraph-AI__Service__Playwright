# FV2.8 — Confirm zero /containers/* URL references in dashboard

**Date:** 2026-05-05
**Phase:** FV2.8
**Branch:** `claude/fv2-8-containers-sweep-5sjIO`
**Commit:** (backfill after merge)
**Status:** SHIPPED
**Unblocked by:** FV2.7 (pods tab via control-plane proxy)

---

## What was done

Verification sweep: confirmed the dashboard has zero `/containers/*` URL constructions after FV2.7. Single cosmetic fix applied.

### Sweep results

| Check | Result |
|-------|--------|
| `grep -rn "/containers/" sgraph_ai_service_playwright__api_site/` | **ZERO hits** |
| `grep "containers" nodes-view/ \| grep -v Pods/pods/` | 2 internal IDs (`data-tab`, JS check) — not URL builders |
| `grep "container_count"` | **ZERO hits** |
| Visible "Container" UI labels | **ZERO** — tab already reads "Pods"; `ct-empty` reads "No pods running" |

### Files changed

| File | Change |
|------|--------|
| `sp-cli-nodes-view.css` | CSS comment `/* ── Containers tab */` → `/* ── Pods tab */` |
| `sg-compute/index.md` | Reality doc: removed `BV2.5` from PROPOSED (it shipped); updated `Routes__Compute__Nodes` to list `POST /api/nodes`; added FV2.8 + BV2.5 history rows |

---

## Acceptance criteria verification

- `grep -rn "/containers/" sgraph_ai_service_playwright__api_site/` returns zero hits ✅
- Pods tab reflects "Pods" label everywhere (no user-visible "Container") ✅
- `container_count` field not consumed anywhere in the dashboard ✅
- Reality doc updated ✅

---

## What was NOT changed (intentional)

- `data-tab="containers"`, `id="tab-containers"`, `id="panel-containers"` — internal routing keys, not UI labels; renaming is a separate cosmetic chore outside this brief's scope
- `_fetchContainers()`, `_renderContainers()` — internal method names, not user-visible
- `sp-cli-host-shell.js`: `'Container logs (control)'` — this is a shell command label, not a pod-view label; out of scope

---

## Failures

None.

## Next phase

BV2.17 (sidecar alias deletion) is now unblocked. Next frontend phase: FV2.6 (per-spec UI co-location) — pending Architect lock.
