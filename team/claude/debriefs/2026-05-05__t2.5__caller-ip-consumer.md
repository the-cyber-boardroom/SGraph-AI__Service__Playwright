# Debrief — T2.5 caller-IP consumer

**Date:** 2026-05-05
**Status:** COMPLETE — backend `GET /catalog/caller-ip` shipped in `04c3f74`

---

## What shipped (Option B → Option A)

`sg-compute-launch-form._seedCallerIp()` shipped in two stages:

**Stage 1 (Option B — T2.5):**
- `localhost` / `127.0.0.1` / `0.0.0.0` → sets `127.0.0.1`
- Remote host → leaves field empty; helper text + "Find my public IP ↗" link to Google

**Stage 2 (Option A — BV__caller-ip-endpoint, `04c3f74`):**
- Remote hosts: calls `GET /catalog/caller-ip` (auth-free) which reads `X-Forwarded-For`
- Populates field only if not already filled (safe for user overrides)
- "Find my public IP ↗" link removed (no longer needed)
- `.caller-ip-lookup` dead CSS removed

Backend: `Routes__Compute__Catalog` at `/catalog`; `Schema__Caller__IP`; 4 tests.

---

## Failures

None — no mocks, no patches. Option B degraded gracefully while backend was pending.
