# OSBot-Utils Safe Primitives Complete Reference Guide

**Version:** v3.28.0    
**Created:** 11/Nov/2025

## Overview

OSBot-Utils provides a comprehensive collection of type-safe primitive classes that enforce validation rules at instantiation time. These primitives prevent invalid data from entering your system and provide clear error messages when validation fails.

**Key Benefits:**
- **Compile-time safety**: Catch invalid data at creation time, not runtime
- **Self-documenting**: Type annotations clearly communicate constraints
- **Zero performance overhead**: Validation happens once at instantiation
- **Composable**: Use in Type_Safe classes for complete type safety

## Quick Reference: All Available Primitives

### Core Base Classes

| Type | Base | Description | Key Features |
|------|------|-------------|--------------|
| `Safe_Int` | `int` | Type-safe integer with validation | min/max values, allow_str, clamp_to_range |
| `Safe_UInt` | `Safe_Int` | Unsigned integer (≥0) | Inherits Safe_Int, min_value=0 |
| `Safe_Float` | `float` | Type-safe float with exact arithmetic | Decimal support, precision control, min/max |
| `Safe_Str` | `str` | Type-safe string with regex validation | Regex sanitization, length limits, case control |

### Common Domain Types

| Type | Base | Description | Constraints |
|------|------|-------------|-------------|
| `Safe_Str__Code__Snippet` | `Safe_Str` | Code snippets with programming chars | Max 1024 chars, preserves indentation |
| `Safe_Str__Text` | `Safe_Str` | General text with punctuation | Max 4096 chars |
| `Safe_Str__Text__Dangerous` | `Safe_Str` | Text with wide character support | Max 65536 chars |
| `Safe_Str__Version` | `Safe_Str` | Semantic version strings | Format: v{major}.{minor}.{patch} |
| `Safe_UInt__Byte` | `Safe_UInt` | Single byte value | Range: 0-255 |

### Cryptography Types

| Type | Base | Description | Format |
|------|------|-------------|--------|
| `Safe_Str__Hash` | `Safe_Str` | Fixed-length hash | Exactly 10 hex chars |
| `Safe_Str__Cache_Hash` | `Safe_Str` | Variable-length cache hash | 10-96 hex chars |
| `Safe_Str__SHA1` | `Safe_Str` | Full Git commit SHA | Exactly 40 hex chars |
| `Safe_Str__SHA1__Short` | `Safe_Str` | Short Git commit SHA | Exactly 7 hex chars |
| `Safe_Str__NaCl__Private_Key` | `Safe_Str` | NaCl/Curve25519 private key | Exactly 64 hex chars (32 bytes) |
| `Safe_Str__NaCl__Public_Key` | `Safe_Str` | NaCl/Curve25519 public key | Exactly 64 hex chars (32 bytes) |

### File System Types

| Type | Base | Description | Features |
|------|------|-------------|----------|
| `Safe_Str__File__Name` | `Safe_Str` | Safe filename | Alphanumerics, underscores, hyphens, dots, spaces |
| `Safe_Str__File__Path` | `Safe_Str` | Safe file path | Max 1024 chars, cross-platform separators |
| `Safe_UInt__FileSize` | `Safe_UInt` | File size in bytes | to_kb(), to_mb(), to_gb() methods |

### Git/GitHub Types

| Type | Base | Description | Validation |
|------|------|-------------|------------|
| `Safe_Str__Git__Branch` | `Safe_Str` | Git branch name | git-check-ref-format rules |
| `Safe_Str__Git__Tag` | `Safe_Str` | Git tag name | git-check-ref-format rules |
| `Safe_Str__Git__Ref` | `Safe_Str` | Generic Git reference | Branch, tag, or SHA |
| `Safe_Str__GitHub__Repo_Owner` | `Safe_Str` | GitHub username/org | Max 39 chars, no consecutive hyphens |
| `Safe_Str__GitHub__Repo_Name` | `Safe_Str` | GitHub repository name | Max 100 chars |
| `Safe_Str__GitHub__Repo` | `Safe_Str` | Full repo identifier | Format: owner/repo |

### HTTP Types

| Type | Base | Description | Max Length |
|------|------|-------------|------------|
| `Enum__Http__Method` | `Enum` | HTTP method enumeration | GET, POST, PUT, DELETE, PATCH, HEAD, OPTIONS |
| `Safe_Str__Http__Header__Name` | `Safe_Str` | HTTP header names | 128 chars, lowercase |
| `Safe_Str__Http__Header__Value` | `Safe_Str` | HTTP header values | 8192 chars |
| `Safe_Str__Http__Content_Type` | `Safe_Str` | Content-Type header | 256 chars |
| `Safe_Str__Http__Accept` | `Safe_Str` | Accept header | 512 chars |
| `Safe_Str__Http__Authorization` | `Safe_Str` | Authorization header | 2048 chars |
| `Safe_Str__Http__Cookie` | `Safe_Str` | Cookie header | 32768 chars |
| `Safe_Str__Http__User_Agent` | `Safe_Str` | User-Agent header | 512 chars |
| `Safe_Str__Http__Text` | `Safe_Str` | HTTP body text | 1 MB |
| `Safe_Str__Http__ETag` | `Safe_Str` | ETag header | 128 chars |
| `Safe_Str__Http__Cache_Control` | `Safe_Str` | Cache-Control header | 256 chars |
| `Safe_Str__Http__Location` | `Safe_Str` | Location header (redirects) | 2048 chars |
| `Safe_Str__Http__Last_Modified` | `Safe_Str` | Last-Modified header | 64 chars |

### Identifier Types

| Type | Base | Description | Format/Features |
|------|------|-------------|-----------------|
| `Guid` | `str` | UUID5-based identifier | Deterministic from input |
| `Random_Guid` | `str` | Random UUID4 identifier | Random generation |
| `Obj_Id` | `str` | 8-char hex object ID | Auto-generates if not provided |
| `Random_Guid_Short` | `str` | Short random identifier | Compact format |
| `Random_Hash` | `Safe_Str__Cache_Hash` | Random 16-char hash | From random bytes |
| `Safe_Id` | `str` | General safe identifier | Max 512 chars, alphanumerics + - _ |
| `Timestamp_Now` | `Safe_Int` | Timestamp in milliseconds | UTC, ISO format parsing |
| `Safe_Str__Id` | `Safe_Str` | Base identifier string | Max 128 chars, alphanumerics + - _ |
| `Safe_Str__Key` | `Safe_Str__Id` | Dictionary/map key | Allows dots for hierarchy |
| `Safe_Str__Label` | `Safe_Str__Topic` | Display label | Allows dots, colons, spaces |
| `Safe_Str__Topic` | `Safe_Str` | Topic name | Max 512 chars, allows spaces |
| `Safe_Str__Slug` | `Safe_Str__Id` | URL-friendly slug | Lowercase, hyphens only |
| `Safe_Str__Namespace` | `Safe_Str__Id` | Namespace identifier | Dot notation support |
| `Safe_Str__Display_Name` | `Safe_Str__Id` | User-facing names | Allows parentheses, apostrophes |
| `Safe_Str__Python__Identifier` | `Safe_Str` | Valid Python identifier | Handles keywords, fixes invalid starts |
| `Safe_Str__Python__Module` | `Safe_Str` | Python module name | Max 64 chars, lowercase |

### LLM (Language Model) Types

| Type | Base | Description | Constraints |
|------|------|-------------|-------------|
| `Enum__LLM__Role` | `Enum` | Message role | SYSTEM, USER, ASSISTANT, TOOL |
| `Safe_Str__LLM__Message__System` | `Safe_Str` | System prompt | Max 4096 chars |
| `Safe_Str__LLM__Message__User` | `Safe_Str` | User message | Max 32768 chars |
| `Safe_Str__LLM__Message__Assistant` | `Safe_Str` | Assistant response | Max 32768 chars |
| `Safe_Str__LLM__Message__Tool` | `Safe_Str` | Tool/function response | Max 16384 chars |
| `Safe_Str__LLM__Prompt` | `Safe_Str` | General LLM prompt | Max 32768 chars |
| `Safe_Str__LLM__Model_Id` | `Safe_Str` | Model identifier | Max 256 chars, supports provider/model |
| `Safe_Str__LLM__Model_Name` | `Safe_Str` | Human-readable model name | Max 256 chars |
| `Safe_Str__LLM__Model_Slug` | `Safe_Str` | URL-safe model slug | Max 256 chars |
| `Safe_Str__LLM__Description` | `Safe_Str` | Model/feature description | Max 4096 chars |
| `Safe_Str__LLM__Modality` | `Safe_Str` | Model modality | e.g., "text->image" |
| `Safe_Str__LLM__Tokenizer` | `Safe_Str` | Tokenizer type | Max 64 chars |
| `Safe_Float__LLM__Temperature` | `Safe_Float` | Temperature parameter | Range: 0.0-2.0, 2 decimals |
| `Safe_Float__LLM__Top_P` | `Safe_Float` | Top-p/nucleus sampling | Range: 0.0-1.0, default 0.9 |
| `Safe_UInt__LLM__Max_Tokens` | `Safe_UInt` | Max tokens parameter | Range: 1-200000, default 5000 |

### Network Types

| Type | Base | Description | Features |
|------|------|-------------|----------|
| `Safe_Str__IP_Address` | `str` | IP address validation | IPv4 and IPv6 support |
| `Safe_UInt__Port` | `Safe_UInt` | Network port number | Range: 0-65535 |

### Numerical Types

| Type | Base | Description | Features |
|------|------|-------------|----------|
| `Safe_Float__Money` | `Safe_Float` | Money calculations | 2 decimals, exact arithmetic, min 0.0 |
| `Safe_Float__Financial` | `Safe_Float` | Financial calculations | 2 decimals, exact arithmetic |
| `Safe_Float__Percentage_Exact` | `Safe_Float` | Exact percentage | Range: 0.0-100.0, 2 decimals |
| `Safe_UInt__Percentage` | `Safe_UInt` | Integer percentage | Range: 0-100 |
| `Safe_Float__Engineering` | `Safe_Float` | Engineering calculations | 6 decimals, epsilon 1e-6 |
| `Safe_Float__Scientific` | `Safe_Float` | Scientific calculations | 15 decimals, allows inf/nan |

### Web Types

| Type | Base | Description | Constraints |
|------|------|-------------|-------------|
| `Safe_Str__Email` | `Safe_Str` | Email address | Max 256 chars, requires @ |
| `Safe_Str__Username` | `Safe_Str` | Username | Max 32 chars, alphanumerics + _ |
| `Safe_Str__Password` | `Safe_Str` | Password | Min 8 chars |
| `Safe_Str__Url` | `Safe_Str` | URL | Max 2048 chars, requires http(s):// |
| `Safe_Str__Html` | `Safe_Str` | HTML content | Max 10 MB |
| `Safe_Str__API__Parameter` | `Safe_Str` | API parameter name | Max 64 chars, alphanumerics + _ |

**Total: 100+ specialized primitives** covering all common use cases for type-safe data validation.

## Installation

```python
pip install osbot-utils
```

## Core Primitive Types

### Safe_Int

Base class for type-safe integers with validation rules.

```python
from osbot_utils.type_safe.primitives.core.Safe_Int import Safe_Int

class Age(Safe_Int):
    min_value = 0
    max_value = 150
    
age = Age(25)           # ✓ Valid
age = Age(-5)           # ✗ Raises ValueError
age = Age(200)          # ✗ Raises ValueError
```

**Configuration Options:**
- `min_value`: Minimum allowed value (inclusive)
- `max_value`: Maximum allowed value (inclusive)
- `allow_none`: Whether None is allowed (default: True)
- `allow_bool`: Whether bool values are allowed (default: False)
- `allow_str`: Whether string conversion is allowed (default: True)
- `strict_type`: If True, only accept int type (default: False)
- `clamp_to_range`: Clamp to min/max instead of raising error (default: False)

**Arithmetic Operations:**
All arithmetic operations maintain type safety and return instances of the same class:
```python
result = Age(25) + 5    # Returns Age(30)
result = Age(100) * 2   # Returns Age(200) or raises if > max_value
```

### Safe_UInt

Unsigned integer - only accepts non-negative values.

```python
from osbot_utils.type_safe.primitives.core.Safe_UInt import Safe_UInt

class Count(Safe_UInt):
    max_value = 1000

count = Count(50)       # ✓ Valid
count = Count(-1)       # ✗ Raises ValueError (min_value is 0)
```

**Configuration:**
- Inherits all `Safe_Int` options
- `min_value` automatically set to 0

### Safe_Float

Base class for type-safe floats with precise decimal handling.

```python
from osbot_utils.type_safe.primitives.core.Safe_Float import Safe_Float

class Price(Safe_Float):
    min_value = 0.0
    decimal_places = 2
    use_decimal = True    # Use Decimal for exact arithmetic

price = Price(19.99)
price = Price("19.99")   # String conversion supported
```

**Configuration Options:**
- `min_value`: Minimum allowed value
- `max_value`: Maximum allowed value
- `allow_none`: Whether None is allowed (default: True)
- `allow_bool`: Whether bool values allowed (default: False)
- `allow_str`: Whether string conversion allowed (default: True)
- `allow_int`: Whether int conversion allowed (default: True)
- `allow_inf`: Whether infinite values allowed (default: False)
- `strict_type`: If True, only accept float type (default: False)
- `decimal_places`: Number of decimal places (default: None)
- `use_decimal`: Use Python Decimal for exact arithmetic (default: True)
- `epsilon`: Tolerance for equality comparisons (default: 1e-9)
- `round_output`: Round to decimal_places (default: True)
- `clamp_to_range`: Clamp to min/max range (default: False)

**Why use_decimal=True?**
```python
# Standard Python float issues:
0.1 + 0.2 == 0.3  # False! (floating point error)

# With Safe_Float and use_decimal=True:
class Money(Safe_Float):
    decimal_places = 2
    use_decimal = True

Money(0.1) + Money(0.2) == Money(0.3)  # True!
```

**Arithmetic Operations:**
All operations preserve type safety:
```python
price1 = Price(10.50)
price2 = Price(5.25)
total = price1 + price2    # Returns Price(15.75)
```

### Safe_Str

Base class for type-safe strings with regex validation and sanitization.

```python
from osbot_utils.type_safe.primitives.core.Safe_Str import Safe_Str
import re

class Username(Safe_Str):
    regex = re.compile(r'[^a-zA-Z0-9_]')  # Remove non-alphanumeric
    max_length = 32
    replacement_char = '_'
    to_lower_case = True

username = Username("John_Doe123")      # ✓ Valid
username = Username("john@doe!")        # Converts to "john_doe_"
```

**Configuration Options:**
- `allow_all_replacement_char`: Allow strings of only replacement chars (default: True)
- `allow_empty`: Whether empty strings allowed (default: True)
- `exact_length`: Require exact length match (default: False)
- `max_length`: Maximum string length (default: 512)
- `regex`: Pattern for validation/replacement
- `regex_mode`: REPLACE or MATCH mode
- `replacement_char`: Character to replace invalid chars (default: '_')
- `strict_validation`: Raise error instead of replacing (default: False)
- `to_lower_case`: Convert to lowercase (default: False)
- `trim_whitespace`: Trim leading/trailing whitespace (default: False)

**Regex Modes:**
```python
from osbot_utils.type_safe.primitives.core.enums.Enum__Safe_Str__Regex_Mode import Enum__Safe_Str__Regex_Mode

# REPLACE mode: regex defines INVALID characters to replace
class SafeName(Safe_Str):
    regex = re.compile(r'[^a-zA-Z0-9]')  # Replace anything not alphanumeric
    regex_mode = Enum__Safe_Str__Regex_Mode.REPLACE

# MATCH mode: regex defines VALID pattern
class Version(Safe_Str):
    regex = re.compile(r'^v\d+\.\d+\.\d+$')  # Must match version pattern
    regex_mode = Enum__Safe_Str__Regex_Mode.MATCH
    strict_validation = True  # Required with MATCH mode
```

## Domain-Specific Primitives

### Common Types

#### Safe_Str__Code__Snippet
For code snippets with programming characters.

```python
from osbot_utils.type_safe.primitives.domains.common.safe_str.Safe_Str__Code__Snippet import Safe_Str__Code__Snippet

code = Safe_Str__Code__Snippet('def hello():\n    print("world")')
```

**Features:**
- Max length: 1024 characters
- Allows: alphanumerics, underscores, hyphens, dots, spaces, common programming symbols
- Preserves indentation (no trim_whitespace)

#### Safe_Str__Text
For general text content with punctuation.

```python
from osbot_utils.type_safe.primitives.domains.common.safe_str.Safe_Str__Text import Safe_Str__Text

text = Safe_Str__Text("Hello, world! How are you?")
```

**Features:**
- Max length: 4096 characters
- Allows: alphanumerics, spaces, common punctuation
- Sanitizes special characters

#### Safe_Str__Text__Dangerous
For text that may contain a wider range of characters (use cautiously).

```python
from osbot_utils.type_safe.primitives.domains.common.safe_str.Safe_Str__Text__Dangerous import Safe_Str__Text__Dangerous

text = Safe_Str__Text__Dangerous("Text with <special> & unusual characters!")
```

**Features:**
- Max length: 65536 characters
- Allows: most printable characters including special symbols
- Use when you need to preserve more characters

#### Safe_Str__Version
For semantic version strings.

```python
from osbot_utils.type_safe.primitives.domains.common.safe_str.Safe_Str__Version import Safe_Str__Version

version = Safe_Str__Version("v1.2.3")    # ✓ Valid
version = Safe_Str__Version("v999.999.999")  # ✓ Valid
version = Safe_Str__Version("1.2.3")     # ✗ Must start with 'v'
```

**Features:**
- Format: `v{major}.{minor}.{patch}`
- Each component: 1-3 digits
- Strict validation with MATCH mode

#### Safe_UInt__Byte
Single byte value (0-255).

```python
from osbot_utils.type_safe.primitives.domains.common.safe_uint.Safe_UInt__Byte import Safe_UInt__Byte

byte = Safe_UInt__Byte(255)    # ✓ Valid
byte = Safe_UInt__Byte(256)    # ✗ Exceeds max
```

### Cryptography Types

#### Safe_Str__Hash
Fixed-length hash (10 hex characters).

```python
from osbot_utils.type_safe.primitives.domains.cryptography.safe_str.Safe_Str__Hash import Safe_Str__Hash, safe_str_hash

# Create hash from data
hash_obj = safe_str_hash("my data")
hash_obj = safe_str_hash(b"my bytes")

# Direct creation
hash_obj = Safe_Str__Hash("a1b2c3d4e5")
```

**Features:**
- Exactly 10 hexadecimal characters
- Strict validation
- Helper function for hash generation

#### Safe_Str__Cache_Hash
Variable-length cache hash (10-96 hex characters).

```python
from osbot_utils.type_safe.primitives.domains.cryptography.safe_str.Safe_Str__Cache_Hash import Safe_Str__Cache_Hash

cache_key = Safe_Str__Cache_Hash("a1b2c3d4e5f6")  # 12 chars
cache_key = Safe_Str__Cache_Hash("a" * 96)       # Max length
```

#### Safe_Str__SHA1
Full 40-character Git commit SHA.

```python
from osbot_utils.type_safe.primitives.domains.cryptography.safe_str.Safe_Str__SHA1 import Safe_Str__SHA1

sha = Safe_Str__SHA1("7fd1a60b01f91b314f59955a4e4d4e80d8edf11d")
```

#### Safe_Str__SHA1__Short
Short 7-character Git commit SHA.

```python
from osbot_utils.type_safe.primitives.domains.cryptography.safe_str.Safe_Str__SHA1__Short import Safe_Str__SHA1__Short

short_sha = Safe_Str__SHA1__Short("7fd1a60")
```

#### Safe_Str__NaCl__Private_Key / Safe_Str__NaCl__Public_Key
NaCl (Curve25519) cryptographic keys in hexadecimal.

```python
from osbot_utils.type_safe.primitives.domains.cryptography.safe_str.Safe_Str__NaCl__Private_Key import Safe_Str__NaCl__Private_Key
from osbot_utils.type_safe.primitives.domains.cryptography.safe_str.Safe_Str__NaCl__Public_Key import Safe_Str__NaCl__Public_Key

private_key = Safe_Str__NaCl__Private_Key("d4c8f7e3a2b1c9d8e7f6a5b4c3d2e1f0a9b8c7d6e5f4a3b2c1d0e9f8a7b6c5d4e3")
public_key = Safe_Str__NaCl__Public_Key("a1b2c3d4e5f6789012345678901234567890abcdefabcdefabcdefabcdefabcd")
```

**Features:**
- Exactly 64 hexadecimal characters (32 bytes)
- Used with libsodium/PyNaCl

### File System Types

#### Safe_Str__File__Name
Safe filename with restricted characters.

```python
from osbot_utils.type_safe.primitives.domains.files.safe_str.Safe_Str__File__Name import Safe_Str__File__Name

filename = Safe_Str__File__Name("document.txt")
filename = Safe_Str__File__Name("my file (2023).pdf")
```

**Features:**
- Allows: alphanumerics, underscores, hyphens, dots, spaces
- Sanitizes invalid characters
- Prevents all-replacement-char names

#### Safe_Str__File__Path
Safe file path with platform-compatible characters.

```python
from osbot_utils.type_safe.primitives.domains.files.safe_str.Safe_Str__File__Path import Safe_Str__File__Path

path = Safe_Str__File__Path("/home/user/documents/file.txt")
path = Safe_Str__File__Path("C:\\Users\\User\\Documents\\file.txt")
```

**Features:**
- Max length: 1024 characters
- Allows: path separators (/, \), alphanumerics, spaces
- Cross-platform compatible

#### Safe_UInt__FileSize
File size in bytes with utility methods.

```python
from osbot_utils.type_safe.primitives.domains.files.safe_uint.Safe_UInt__FileSize import Safe_UInt__FileSize

size = Safe_UInt__FileSize(1048576)  # 1 MB in bytes

# Utility methods
print(size.to_kb())  # 1024.0
print(size.to_mb())  # 1.0
print(size.to_gb())  # 0.0009765625
```

**Features:**
- Max value: 2^63 - 1 (largest file size on most systems)
- Conversion methods for KB, MB, GB

### Git/GitHub Types

#### Safe_Str__Git__Branch
Git branch name following git-check-ref-format rules.

```python
from osbot_utils.type_safe.primitives.domains.git.safe_str.Safe_Str__Git__Branch import Safe_Str__Git__Branch

branch = Safe_Str__Git__Branch("feature/oauth-login")
branch = Safe_Str__Git__Branch("bugfix/issue-123")
```

**Validation Rules:**
- Cannot start with dash
- No consecutive dots (..)
- No sequence @{
- Cannot be single character @
- No consecutive slashes
- Path components cannot start with dot

#### Safe_Str__Git__Tag
Git tag name.

```python
from osbot_utils.type_safe.primitives.domains.git.safe_str.Safe_Str__Git__Tag import Safe_Str__Git__Tag

tag = Safe_Str__Git__Tag("v1.0.0")
tag = Safe_Str__Git__Tag("release-2024.01.15")
```

**Features:**
- Follows git-check-ref-format rules
- Same rules as branches (without dash restriction)

#### Safe_Str__Git__Ref
Generic Git reference (branch, tag, or SHA).

```python
from osbot_utils.type_safe.primitives.domains.git.safe_str.Safe_Str__Git__Ref import Safe_Str__Git__Ref

ref = Safe_Str__Git__Ref("main")                     # Branch
ref = Safe_Str__Git__Ref("v1.0.0")                   # Tag
ref = Safe_Str__Git__Ref("7fd1a60")                  # Short SHA
ref = Safe_Str__Git__Ref("7fd1a60b01f91b314...")     # Full SHA
```

**Features:**
- Accepts any valid Git reference type
- Validates against multiple reference types

#### Safe_Str__GitHub__Repo_Owner
GitHub username or organization name.

```python
from osbot_utils.type_safe.primitives.domains.git.github.safe_str.Safe_Str__GitHub__Repo_Owner import Safe_Str__GitHub__Repo_Owner

owner = Safe_Str__GitHub__Repo_Owner("octocat")
owner = Safe_Str__GitHub__Repo_Owner("my-org")
```

**Validation Rules:**
- Max 39 characters
- Alphanumerics and hyphens only
- Cannot start or end with hyphen
- No consecutive hyphens

#### Safe_Str__GitHub__Repo_Name
GitHub repository name.

```python
from osbot_utils.type_safe.primitives.domains.git.github.safe_str.Safe_Str__GitHub__Repo_Name import Safe_Str__GitHub__Repo_Name

repo = Safe_Str__GitHub__Repo_Name("Hello-World")
repo = Safe_Str__GitHub__Repo_Name("my.repo_v2")
```

**Features:**
- Max 100 characters
- Allows: alphanumerics, hyphens, underscores, dots
- Cannot be just dots (. or ..)

#### Safe_Str__GitHub__Repo
Full GitHub repository identifier (owner/repo).

```python
from osbot_utils.type_safe.primitives.domains.git.github.safe_str.Safe_Str__GitHub__Repo import Safe_Str__GitHub__Repo

repo = Safe_Str__GitHub__Repo("octocat/Hello-World")
repo = Safe_Str__GitHub__Repo("microsoft/vscode")

# Access components
print(repo.repo_owner)  # Safe_Str__GitHub__Repo_Owner instance
print(repo.repo_name)   # Safe_Str__GitHub__Repo_Name instance
```

**Features:**
- Validates both owner and repo components
- Provides typed access to components
- Max 140 characters total

### HTTP Types

#### Enum__Http__Method
HTTP method enumeration.

```python
from osbot_utils.type_safe.primitives.domains.http.enums.Enum__Http__Method import Enum__Http__Method

method = Enum__Http__Method.GET
method = Enum__Http__Method.POST
```

**Available Methods:**
- GET, POST, PUT, DELETE, PATCH, HEAD, OPTIONS

#### Safe_Str__Http__Header__Name
HTTP header names (lowercase as per HTTP/2 and HTTP/3).

```python
from osbot_utils.type_safe.primitives.domains.http.safe_str.Safe_Str__Http__Header__Name import Safe_Str__Http__Header__Name

header = Safe_Str__Http__Header__Name("Content-Type")    # Converts to "content-type"
header = Safe_Str__Http__Header__Name("X-Custom-Header") # Converts to "x-custom-header"
```

**Features:**
- Automatically converts to lowercase
- Allows: alphanumerics and hyphens
- Max 128 characters

#### Safe_Str__Http__Header__Value
HTTP header values.

```python
from osbot_utils.type_safe.primitives.domains.http.safe_str.Safe_Str__Http__Header__Value import Safe_Str__Http__Header__Value

value = Safe_Str__Http__Header__Value("application/json; charset=utf-8")
```

**Features:**
- Max 8192 characters
- Filters control characters (except tab)
- Allows visible ASCII and spaces

#### Safe_Str__Http__Content_Type
Content-Type header values.

```python
from osbot_utils.type_safe.primitives.domains.http.safe_str.Safe_Str__Http__Content_Type import Safe_Str__Http__Content_Type

content_type = Safe_Str__Http__Content_Type("application/json")
content_type = Safe_Str__Http__Content_Type("text/html; charset=utf-8")
```

#### Safe_Str__Http__Accept
Accept header values.

```python
from osbot_utils.type_safe.primitives.domains.http.safe_str.Safe_Str__Http__Accept import Safe_Str__Http__Accept

accept = Safe_Str__Http__Accept("text/html,application/json;q=0.9")
accept = Safe_Str__Http__Accept("application/*")
```

#### Safe_Str__Http__Authorization
Authorization header values.

```python
from osbot_utils.type_safe.primitives.domains.http.safe_str.Safe_Str__Http__Authorization import Safe_Str__Http__Authorization

auth = Safe_Str__Http__Authorization("Bearer eyJ...")
auth = Safe_Str__Http__Authorization("Basic dXNlcjpwYXNz")
```

**Features:**
- Max 2048 characters
- Filters control characters
- Supports Bearer, Basic, and other auth schemes

#### Safe_Str__Http__Cookie
Cookie header values.

```python
from osbot_utils.type_safe.primitives.domains.http.safe_str.Safe_Str__Http__Cookie import Safe_Str__Http__Cookie

cookie = Safe_Str__Http__Cookie("session=abc123; user_id=456")
```

**Features:**
- Max 32768 characters (flexible for multiple cookies)
- Filters control characters

#### Safe_Str__Http__User_Agent
User-Agent header values.

```python
from osbot_utils.type_safe.primitives.domains.http.safe_str.Safe_Str__Http__User_Agent import Safe_Str__Http__User_Agent

ua = Safe_Str__Http__User_Agent("Mozilla/5.0 (Windows NT 10.0; Win64; x64)")
```

#### Safe_Str__Http__Text
Large text content for HTTP bodies.

```python
from osbot_utils.type_safe.primitives.domains.http.safe_str.Safe_Str__Http__Text import Safe_Str__Http__Text

body = Safe_Str__Http__Text("Large text content...")
```

**Features:**
- Max 1 MB (1,048,576 characters)
- Normalizes newlines
- Filters control characters
- Preserves tabs, line feeds, carriage returns

#### Safe_Str__Http__ETag, Safe_Str__Http__Cache_Control, Safe_Str__Http__Location, Safe_Str__Http__Last_Modified
Additional HTTP header value types with appropriate validation.

### Identifier Types

#### Guid
UUID5-based globally unique identifier.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.Guid import Guid

guid = Guid("my-unique-string")  # Generates UUID5
# Always produces same GUID for same input
```

#### Random_Guid
Random UUID4 identifier.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.Random_Guid import Random_Guid

guid = Random_Guid()  # Generates new random UUID4
```

#### Obj_Id
8-character hexadecimal object identifier.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.Obj_Id import Obj_Id, new_obj_id

obj_id = Obj_Id()                    # Auto-generates
obj_id = Obj_Id("a1b2c3d4")         # From existing
new_id = new_obj_id()                # Generate without class
```

#### Random_Guid_Short
Short random identifier.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.Random_Guid_Short import Random_Guid_Short

short_id = Random_Guid_Short()  # Auto-generates short ID
```

#### Random_Hash
Random 16-character hash.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.Random_Hash import Random_Hash

hash_id = Random_Hash()  # Auto-generates from random bytes
```

#### Safe_Id
General-purpose safe identifier.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.Safe_Id import Safe_Id, safe_id

id_obj = Safe_Id()                     # Auto-generates
id_obj = Safe_Id("my-identifier")      # From string
id_str = safe_id("unsafe!@#string")    # Returns sanitized string
```

**Features:**
- Max 512 characters (configurable)
- Sanitizes to alphanumerics, underscores, hyphens
- Auto-generation with prefix

#### Timestamp_Now
Current timestamp in milliseconds.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.safe_int.Timestamp_Now import Timestamp_Now

ts = Timestamp_Now()                           # Current time
ts = Timestamp_Now(1640000000000)             # From milliseconds
ts = Timestamp_Now("2024-01-15T10:30:00Z")    # From ISO format
```

**Features:**
- Stores milliseconds (not fractional seconds)
- Parses ISO date strings
- UTC timezone handling

#### Safe_Str__Id
Base identifier string type.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.safe_str.Safe_Str__Id import Safe_Str__Id

id_str = Safe_Str__Id("my-identifier-123")
```

**Features:**
- Max 128 characters
- Alphanumerics, underscores, hyphens

#### Safe_Str__Key
Dictionary/map key identifier.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.safe_str.Safe_Str__Key import Safe_Str__Key

key = Safe_Str__Key("config.database.host")
```

**Features:**
- Allows dots for hierarchical keys
- Alphanumerics, underscores, hyphens, dots

#### Safe_Str__Label
Display label with hierarchical support.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.safe_str.Safe_Str__Label import Safe_Str__Label

label = Safe_Str__Label("System: Database Connection")
```

**Features:**
- Allows: alphanumerics, underscores, hyphens, dots, colons, spaces
- For user-facing hierarchical labels

#### Safe_Str__Topic
Topic name with spaces allowed.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.safe_str.Safe_Str__Topic import Safe_Str__Topic

topic = Safe_Str__Topic("User Authentication Flow")
```

**Features:**
- Max 512 characters
- Allows: alphanumerics, underscores, hyphens, spaces

#### Safe_Str__Slug
URL-friendly slug.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.safe_str.Safe_Str__Slug import Safe_Str__Slug

slug = Safe_Str__Slug("my-blog-post-title")
```

**Features:**
- Lowercase only
- Alphanumerics and hyphens only

#### Safe_Str__Namespace
Namespace identifier with dot notation.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.safe_str.Safe_Str__Namespace import Safe_Str__Namespace

namespace = Safe_Str__Namespace("com.example.module")
```

#### Safe_Str__Display_Name
User-facing display names.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.safe_str.Safe_Str__Display_Name import Safe_Str__Display_Name

name = Safe_Str__Display_Name("John Doe (Admin)")
```

**Features:**
- Allows: alphanumerics, underscores, hyphens, spaces, parentheses, dots, apostrophes, hash

#### Safe_Str__Python__Identifier
Valid Python identifier.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.safe_str.Safe_Str__Python__Identifier import Safe_Str__Python__Identifier

ident = Safe_Str__Python__Identifier("my_variable")
ident = Safe_Str__Python__Identifier("8invalid")   # Converts to "_8invalid"
ident = Safe_Str__Python__Identifier("class")      # Converts to "_class"
```

**Features:**
- Ensures valid Python identifier
- Handles keywords by prefixing with underscore
- Fixes invalid starting characters

#### Safe_Str__Python__Module
Python module/package name.

```python
from osbot_utils.type_safe.primitives.domains.identifiers.safe_str.Safe_Str__Python__Module import Safe_Str__Python__Module

module = Safe_Str__Python__Module("my_module")
module = Safe_Str__Python__Module("api_v1")
```

**Features:**
- Lowercase only
- Valid as Python identifier and file system path
- Max 64 characters

### LLM (Language Model) Types

#### Enum__LLM__Role
LLM message role enumeration.

```python
from osbot_utils.type_safe.primitives.domains.llm.enums.Enum__LLM__Role import Enum__LLM__Role

role = Enum__LLM__Role.SYSTEM
role = Enum__LLM__Role.USER
role = Enum__LLM__Role.ASSISTANT
role = Enum__LLM__Role.TOOL
```

#### Safe_Str__LLM__Message__System
System prompt messages.

```python
from osbot_utils.type_safe.primitives.domains.llm.safe_str.Safe_Str__LLM__Message__System import Safe_Str__LLM__Message__System

system = Safe_Str__LLM__Message__System("You are a helpful assistant.")
```

**Features:**
- Max 4096 characters
- Filters control characters (preserves tab, newline, CR)

#### Safe_Str__LLM__Message__User
User messages.

```python
from osbot_utils.type_safe.primitives.domains.llm.safe_str.Safe_Str__LLM__Message__User import Safe_Str__LLM__Message__User

user_msg = Safe_Str__LLM__Message__User("What is the weather today?")
```

**Features:**
- Max 32768 characters (full context window)

#### Safe_Str__LLM__Message__Assistant
Assistant/AI response messages.

```python
from osbot_utils.type_safe.primitives.domains.llm.safe_str.Safe_Str__LLM__Message__Assistant import Safe_Str__LLM__Message__Assistant

assistant_msg = Safe_Str__LLM__Message__Assistant("The weather today is sunny.")
```

**Features:**
- Max 32768 characters

#### Safe_Str__LLM__Message__Tool
Tool/function response messages.

```python
from osbot_utils.type_safe.primitives.domains.llm.safe_str.Safe_Str__LLM__Message__Tool import Safe_Str__LLM__Message__Tool

tool_msg = Safe_Str__LLM__Message__Tool('{"result": "success"}')
```

**Features:**
- Max 16384 characters
- Permissive for JSON/structured data

#### Safe_Str__LLM__Prompt
General LLM prompt.

```python
from osbot_utils.type_safe.primitives.domains.llm.safe_str.Safe_Str__LLM__Prompt import Safe_Str__LLM__Prompt

prompt = Safe_Str__LLM__Prompt("Explain quantum computing in simple terms.")
```

#### Safe_Str__LLM__Model_Id
LLM model identifier.

```python
from osbot_utils.type_safe.primitives.domains.llm.safe_str.Safe_Str__LLM__Model_Id import Safe_Str__LLM__Model_Id

model = Safe_Str__LLM__Model_Id("openai/gpt-4")
model = Safe_Str__LLM__Model_Id("anthropic/claude-3-opus")
model = Safe_Str__LLM__Model_Id("gpt-3.5-turbo")
```

**Features:**
- Max 256 characters
- Supports various formats: provider/model, model-only
- Allows: alphanumerics, /, -, ., :, _, @

#### Safe_Str__LLM__Model_Name
Human-readable model name.

```python
from osbot_utils.type_safe.primitives.domains.llm.safe_str.Safe_Str__LLM__Model_Name import Safe_Str__LLM__Model_Name

name = Safe_Str__LLM__Model_Name("GPT-4 Turbo")
name = Safe_Str__LLM__Model_Name("Claude 3 Opus (Latest)")
```

#### Safe_Str__LLM__Model_Slug
URL-safe model slug.

```python
from osbot_utils.type_safe.primitives.domains.llm.safe_str.Safe_Str__LLM__Model_Slug import Safe_Str__LLM__Model_Slug

slug = Safe_Str__LLM__Model_Slug("gpt-4-turbo")
slug = Safe_Str__LLM__Model_Slug("claude-3-opus")
```

#### Safe_Str__LLM__Description
Model or feature description.

```python
from osbot_utils.type_safe.primitives.domains.llm.safe_str.Safe_Str__LLM__Description import Safe_Str__LLM__Description

desc = Safe_Str__LLM__Description("Advanced language model with multimodal capabilities")
```

**Features:**
- Max 4096 characters
- Allows rich descriptions with formatting characters

#### Safe_Str__LLM__Modality
Model modality descriptor.

```python
from osbot_utils.type_safe.primitives.domains.llm.safe_str.Safe_Str__LLM__Modality import Safe_Str__LLM__Modality

modality = Safe_Str__LLM__Modality("text")
modality = Safe_Str__LLM__Modality("text->image")
modality = Safe_Str__LLM__Modality("image+text->text")
```

#### Safe_Str__LLM__Tokenizer
Tokenizer type identifier.

```python
from osbot_utils.type_safe.primitives.domains.llm.safe_str.Safe_Str__LLM__Tokenizer import Safe_Str__LLM__Tokenizer

tokenizer = Safe_Str__LLM__Tokenizer("cl100k_base")
tokenizer = Safe_Str__LLM__Tokenizer("tiktoken")
```

#### Safe_Float__LLM__Temperature
LLM temperature parameter.

```python
from osbot_utils.type_safe.primitives.domains.llm.safe_float.Safe_Float__LLM__Temperature import Safe_Float__LLM__Temperature

temp = Safe_Float__LLM__Temperature(0.7)
temp = Safe_Float__LLM__Temperature(0.0)   # Deterministic
temp = Safe_Float__LLM__Temperature(2.0)   # Maximum creativity
```

**Features:**
- Range: 0.0 to 2.0
- 2 decimal places
- Auto-clamps to valid range

#### Safe_Float__LLM__Top_P
LLM nucleus sampling parameter.

```python
from osbot_utils.type_safe.primitives.domains.llm.safe_float.Safe_Float__LLM__Top_P import Safe_Float__LLM__Top_P

top_p = Safe_Float__LLM__Top_P(0.9)  # Default
top_p = Safe_Float__LLM__Top_P(1.0)  # Consider all tokens
```

**Features:**
- Range: 0.0 to 1.0
- Default value: 0.9
- Auto-clamps to valid range

#### Safe_UInt__LLM__Max_Tokens
LLM max tokens parameter.

```python
from osbot_utils.type_safe.primitives.domains.llm.safe_uint.Safe_UInt__LLM__Max_Tokens import Safe_UInt__LLM__Max_Tokens

max_tokens = Safe_UInt__LLM__Max_Tokens(5000)   # Default
max_tokens = Safe_UInt__LLM__Max_Tokens(200000) # Maximum (Claude's limit)
```

**Features:**
- Range: 1 to 200,000
- Default: 5000

### Network Types

#### Safe_Str__IP_Address
IP address (IPv4 or IPv6).

```python
from osbot_utils.type_safe.primitives.domains.network.safe_str.Safe_Str__IP_Address import Safe_Str__IP_Address

ip = Safe_Str__IP_Address("192.168.1.1")
ip = Safe_Str__IP_Address("2001:0db8::1")
ip = Safe_Str__IP_Address("  10.0.0.1  ")  # Trims whitespace
```

**Features:**
- Validates using Python's ipaddress module
- Supports IPv4 and IPv6
- Trims whitespace

#### Safe_UInt__Port
Network port number.

```python
from osbot_utils.type_safe.primitives.domains.network.safe_uint.Safe_UInt__Port import Safe_UInt__Port

port = Safe_UInt__Port(8080)
port = Safe_UInt__Port(443)
```

**Features:**
- Range: 0 to 65535
- Does not allow None (port 0 is technically valid but unusual)

### Numerical Types

#### Safe_Float__Money
Money with exact decimal arithmetic.

```python
from osbot_utils.type_safe.primitives.domains.numerical.safe_float.Safe_Float__Money import Safe_Float__Money

price = Safe_Float__Money(19.99)
total = price + Safe_Float__Money(5.00)  # Exact: 24.99
```

**Features:**
- 2 decimal places
- Uses Decimal for exact arithmetic
- Min value: 0.0
- No infinity or NaN

#### Safe_Float__Financial
Financial calculations (2 decimal places).

```python
from osbot_utils.type_safe.primitives.domains.numerical.safe_float.Safe_Float__Financial import Safe_Float__Financial

amount = Safe_Float__Financial(1234.56)
```

**Features:**
- 2 decimal places
- Exact Decimal arithmetic
- No infinity or NaN

#### Safe_Float__Percentage_Exact
Exact percentage (0-100).

```python
from osbot_utils.type_safe.primitives.domains.numerical.safe_float.Safe_Float__Percentage_Exact import Safe_Float__Percentage_Exact

percent = Safe_Float__Percentage_Exact(75.50)
```

**Features:**
- Range: 0.0 to 100.0
- 2 decimal places
- Exact Decimal arithmetic

#### Safe_UInt__Percentage
Integer percentage (0-100).

```python
from osbot_utils.type_safe.primitives.domains.numerical.safe_uint.Safe_UInt__Percentage import Safe_UInt__Percentage

percent = Safe_UInt__Percentage(75)
```

**Features:**
- Range: 0 to 100
- Integer values only

#### Safe_Float__Engineering
Engineering calculations with controlled precision.

```python
from osbot_utils.type_safe.primitives.domains.numerical.safe_float.Safe_Float__Engineering import Safe_Float__Engineering

value = Safe_Float__Engineering(123.456789)
```

**Features:**
- 6 decimal places (configurable)
- Epsilon: 1e-6
- Performance-optimized (no Decimal)

#### Safe_Float__Scientific
Scientific calculations with high precision.

```python
from osbot_utils.type_safe.primitives.domains.numerical.safe_float.Safe_Float__Scientific import Safe_Float__Scientific

value = Safe_Float__Scientific(1.23456789012345)
```

**Features:**
- 15 decimal places
- Allows infinity and NaN
- No Decimal (for performance)

### Web Types

#### Safe_Str__Email
Email address.

```python
from osbot_utils.type_safe.primitives.domains.web.safe_str.Safe_Str__Email import Safe_Str__Email

email = Safe_Str__Email("user@example.com")
email = Safe_Str__Email("john.doe+tag@domain.co.uk")
```

**Features:**
- Max 256 characters
- Requires @ symbol
- Sanitizes invalid characters

#### Safe_Str__Username
Username with restricted characters.

```python
from osbot_utils.type_safe.primitives.domains.web.safe_str.Safe_Str__Username import Safe_Str__Username

username = Safe_Str__Username("john_doe_123")
```

**Features:**
- Max 32 characters
- Alphanumerics and underscores only

#### Safe_Str__Password
Password with minimum requirements.

```python
from osbot_utils.type_safe.primitives.domains.web.safe_str.Safe_Str__Password import Safe_Str__Password

password = Safe_Str__Password("MyP@ssw0rd!")
```

**Features:**
- Min 8 characters
- Allows: alphanumerics, underscores, hyphens, dots, spaces, common symbols

#### Safe_Str__Url
URL with validation.

```python
from osbot_utils.type_safe.primitives.domains.web.safe_str.Safe_Str__Url import Safe_Str__Url

url = Safe_Str__Url("https://example.com/path?query=value")
```

**Features:**
- Max 2048 characters
- Requires http:// or https://
- Allows URL-safe characters

#### Safe_Str__Html
HTML content.

```python
from osbot_utils.type_safe.primitives.domains.web.safe_str.Safe_Str__Html import Safe_Str__Html

html = Safe_Str__Html("<div>Hello <strong>World</strong></div>")
```

**Features:**
- Max 10 MB
- Preserves HTML tags and attributes
- Normalizes newlines
- Filters control characters

#### Safe_Str__API__Parameter
API parameter name.

```python
from osbot_utils.type_safe.primitives.domains.web.safe_str.Safe_Str__API__Parameter import Safe_Str__API__Parameter

param = Safe_Str__API__Parameter("max_tokens")
param = Safe_Str__API__Parameter("temperature")
```

**Features:**
- Max 64 characters
- Alphanumerics and underscores only

## Using Primitives in Type_Safe Classes

Primitives integrate seamlessly with Type_Safe classes:

```python
from osbot_utils.type_safe.Type_Safe import Type_Safe
from osbot_utils.type_safe.primitives.domains.identifiers.safe_str.Safe_Str__Id import Safe_Str__Id
from osbot_utils.type_safe.primitives.domains.numerical.safe_float.Safe_Float__Money import Safe_Float__Money
from osbot_utils.type_safe.primitives.domains.identifiers.Timestamp_Now import Timestamp_Now

class Order(Type_Safe):
    order_id: Safe_Str__Id
    amount: Safe_Float__Money
    created_at: Timestamp_Now
    
# Automatic validation on creation
order = Order(
    order_id="order-123",
    amount=99.99,
    created_at=Timestamp_Now()
)

# JSON serialization preserves types
json_data = order.json()
# {"order_id": "order-123", "amount": 99.99, "created_at": 1640000000000}

# Deserialization recreates typed objects
order2 = Order.from_json(json_data)
assert isinstance(order2.order_id, Safe_Str__Id)
assert isinstance(order2.amount, Safe_Float__Money)
```

## Creating Custom Primitives

### Custom Safe_Str

```python
import re
from osbot_utils.type_safe.primitives.core.Safe_Str import Safe_Str

class Phone_Number(Safe_Str):
    regex = re.compile(r'[^0-9\-\+\(\) ]')  # Only phone chars
    max_length = 20
    trim_whitespace = True
    
phone = Phone_Number("+1 (555) 123-4567")
```

### Custom Safe_Int

```python
from osbot_utils.type_safe.primitives.core.Safe_Int import Safe_Int

class Age(Safe_Int):
    min_value = 0
    max_value = 150
    allow_str = True  # Allow "25" -> 25
    
age = Age(25)
age = Age("30")  # Automatic conversion
```

### Custom Safe_Float

```python
from osbot_utils.type_safe.primitives.core.Safe_Float import Safe_Float

class Temperature_Celsius(Safe_Float):
    min_value = -273.15  # Absolute zero
    max_value = 1000.0
    decimal_places = 1
    use_decimal = True
    
temp = Temperature_Celsius(20.5)
```

## Best Practices

1. **Choose the right primitive**: Use domain-specific types (Safe_Str__Email, Safe_UInt__Port) over generic ones
2. **Leverage type annotations**: Use primitives in Type_Safe classes for full validation
3. **Handle errors gracefully**: Primitives raise ValueError with descriptive messages
4. **Use strict_validation wisely**: Enable for critical data, disable for flexible input
5. **Consider performance**: use_decimal=False for high-performance float operations
6. **Document constraints**: Custom primitive classes are self-documenting

## Performance Considerations

- **Primitive instantiation**: One-time cost at object creation
- **use_decimal=True**: ~2-3x slower than native float, but exact
- **Regex validation**: Cost depends on pattern complexity
- **String sanitization**: Replace mode faster than match mode
- **Type_Safe integration**: Minimal overhead with caching

## Common Patterns

### Configuration Objects

```python
from osbot_utils.type_safe.Type_Safe import Type_Safe
from osbot_utils.type_safe.primitives.domains.network.safe_str.Safe_Str__IP_Address import Safe_Str__IP_Address
from osbot_utils.type_safe.primitives.domains.network.safe_uint.Safe_UInt__Port import Safe_UInt__Port

class Database_Config(Type_Safe):
    host: Safe_Str__IP_Address
    port: Safe_UInt__Port = Safe_UInt__Port(5432)
    
config = Database_Config(host="192.168.1.100")
```

### API Requests

```python
from osbot_utils.type_safe.primitives.domains.llm.enums.Enum__LLM__Role import Enum__LLM__Role
from osbot_utils.type_safe.primitives.domains.llm.safe_str.Safe_Str__LLM__Message__User import Safe_Str__LLM__Message__User
from osbot_utils.type_safe.primitives.domains.llm.safe_float.Safe_Float__LLM__Temperature import Safe_Float__LLM__Temperature

class LLM_Request(Type_Safe):
    role: Enum__LLM__Role
    content: Safe_Str__LLM__Message__User
    temperature: Safe_Float__LLM__Temperature = Safe_Float__LLM__Temperature(0.7)
```

### File Operations

```python
from osbot_utils.type_safe.primitives.domains.files.safe_str.Safe_Str__File__Path import Safe_Str__File__Path
from osbot_utils.type_safe.primitives.domains.files.safe_uint.Safe_UInt__FileSize import Safe_UInt__FileSize

class File_Info(Type_Safe):
    path: Safe_Str__File__Path
    size: Safe_UInt__FileSize
    
    def size_mb(self):
        return self.size.to_mb()
```

## Version History

- **v3.28.0**: Current version with comprehensive primitive coverage
- Enhanced LLM primitives
- Improved HTTP header validation
- Extended identifier types

---

**For more information**: https://github.com/owasp-sbot/OSBot-Utils