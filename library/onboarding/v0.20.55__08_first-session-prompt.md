# First Session Prompt

**Version:** v0.20.55
**Date:** 16 April 2026
**Purpose:** Copy-paste this into the first Claude Code session to bootstrap the SGraph-AI__Service__Playwright repo

---

## The Prompt

Copy everything below the line and paste it as your first message in a new Claude Code session:

---

```
You are bootstrapping **SGraph-AI__Service__Playwright** — a browser automation API service that runs on laptop, CI, Claude Web, Fargate, and Lambda.

The complete architecture has already been designed and specified in a dev pack stored in an encrypted vault. Your job is to implement the specs, not design them.

## Step 1: Install sgit and clone the dev pack

pip3 install sgit-ai --break-system-packages
sgit clone b8cvhl2kk1tg0fw7cokp9xdh:ozgwi1rr /tmp/playwright-dev-pack

## Step 2: Read the dev pack (in this order)

Read these files from /tmp/playwright-dev-pack/:

1. `README.md` — index of all documents
2. `07_first-session-brief.md` — your orientation
3. `01_project-context.md` — what the service is, the 5 deployment targets
4. `02_mission-brief.md` — phased deliverables, success criteria, non-negotiable rules
5. `04_practices-reference.md` — Type_Safe quick rules (critical)
6. `05_technical-bootstrap-guide.md` — step-by-step repo setup

Read the guidance docs (MUST READ before writing any code):
7. `guidance/type_safe.md` — Type_Safe core guide
8. `guidance/python_formatting.md` — alignment, headers, inline comments

Read the definitive specs:
9. `dev-specs/schema-catalogue-v2.md` — ALL schemas, enums, primitives, collections
10. `dev-specs/routes-catalogue-v2.md` — ALL routes, service classes, execution flows
11. `dev-specs/ci-pipeline.md` — Docker infrastructure, CI pipeline, deploy-via-pytest

Read the role definitions:
12. `03_role-definitions/ROLE__dev.md`
13. `03_role-definitions/ROLE__devops.md`
14. `03_role-definitions/ROLE__architect.md`

## Step 3: Create the repo and copy the dev pack into it

Create the SGraph-AI__Service__Playwright repo structure. Then copy the entire dev pack into the repo so it's self-contained:

mkdir -p library/dev_pack/v0.20.55__playwright-service-bootstrap
cp -r /tmp/playwright-dev-pack/* library/dev_pack/v0.20.55__playwright-service-bootstrap/

Also set up the CLAUDE.md files from the templates:

mkdir -p .claude/explorer
cp library/dev_pack/v0.20.55__playwright-service-bootstrap/claude-md-templates/CLAUDE.md .claude/CLAUDE.md
cp library/dev_pack/v0.20.55__playwright-service-bootstrap/claude-md-templates/explorer__CLAUDE.md .claude/explorer/CLAUDE.md

## Step 4: Clone reference repos

git clone --depth 1 -b dev https://github.com/owasp-sbot/OSBot-Playwright /tmp/osbot-playwright-ref
git clone --depth 1 -b dev https://github.com/the-cyber-boardroom/SG_Send__QA /tmp/sg-send-qa-ref

Read these key reference files:
- `/tmp/osbot-playwright-ref/osbot_playwright/docker/images/osbot_playwright/dockerfile` — THE Dockerfile to refresh
- `/tmp/osbot-playwright-ref/osbot_playwright/docker/images/osbot_playwright/handler.py` — boot script + LWA workaround
- `/tmp/sg-send-qa-ref/sg_send_qa/api/Fast_API__QA__Server.py` — FastAPI service class pattern

## Step 5: Build (Phase 0 — Docker first)

After reading all documents, implement in this order:

1. Repo skeleton — directory structure, pyproject.toml, version file, README.md
2. Team structure — `team/explorer/{architect,dev,devops,qa,librarian,historian}/` with ROLE files
3. Dockerfile — refresh from OSBot-Playwright: Playwright 1.58.2-noble, LWA 1.0.0
4. Docker infrastructure classes — Docker__Base + Build + Local (see ci-pipeline.md Section 3)
5. lambda_handler.py — separate from service class (see routes-catalogue-v2.md Section 2.2)
6. CI pipeline — at least Job 1 (unit tests) passing on push to dev
7. All custom Safe_* primitives with validation tests
8. All Enum__* types (17 enums)
9. All Schema__* types with round-trip .json() → .from_json() tests
10. Step schema registries (STEP_SCHEMAS + STEP_RESULT_SCHEMAS)

You are the Explorer team: Architect, Dev, DevOps, QA, Librarian, Historian.

**Non-negotiable rules:**
- Zero raw primitives — no str, int, float, list, dict in Type_Safe classes
- No Literals — all Enums
- No Pydantic — ever
- No boto3 — use osbot-aws
- Schemas are pure data — no methods
- Docker image before service code
- Separate lambda_handler.py — service class importable without side effects
- Every route returns .json() on a Type_Safe schema — no raw dicts
- Step__Executor is the ONLY class touching page.*
- Deploy-via-pytest — deployment steps are pytest tests
```
