# Skills

**Purpose:** Reusable skill packs — procedural knowledge that can be invoked as a Claude Code slash command or loaded by any role.

Empty today. Candidates once the service is operational:

- `use_sgit-and-vaults` — cloning / sharing vaults (mirroring App__Send)
- `run-integration-tests` — Docker build + local integration run
- `deploy-to-dev` — dev-branch deploy-via-pytest runner
- `reality-doc-update` — Librarian helper for keeping the reality doc current
